# DEC-106 — Contract tests expansion slice 4 (401 / 422)

> **Status:** **Accepted** (slice 4 landed)
> **Date:** 2026-08-01
> **Story adjacency:** DEC-094 contract expansion (post STORY-03-04)
> **Validation label:** **light validated** — host `poetry run pytest tests/contract/ -m contract` → **14 passed**, 31 deselected

---

## Decision

Land identity auth/validation error contracts on `GET /api/v1/decisions` using **actual** FastAPI error shapes:

| Status | Shape | Wiring |
|---|---|---|
| **401** | `{"detail": string}` | Document via `DetailStringError` on `list_decisions` (`responses={401: …}`); runtime = `UnauthorizedError` |
| **422** | `HTTPValidationError` / `ValidationError[]` | Already auto-generated by FastAPI for `limit` / confidence Query bounds; contract asserts live body |

**Rejected:** inventing a custom `ErrorResponse` envelope; forcing 401 onto unrelated public probes; editing `get_db()` tenant GUC (DEC-085).

### Code

- `domains/decision_center/router.py` — `DetailStringError` + `responses={401: …}` on `GET /decisions`
- `tests/contract/test_openapi_auth_errors.py` — 401 (unauthenticated client), 422 (`limit=0` with auth fixture), OpenAPI schema assertions

Companion update: [`DEC-094-CONTRACT-TESTS-EXPANSION.md`](DEC-094-CONTRACT-TESTS-EXPANSION.md).

---

## Honesty

- Production GA / External pilot = **NO-GO**
- **CI GREEN not met** (CI-08 GHCR ops-blocked)
- Auth / CSRF / RBAC not weakened
- DEC-085 `set_config` intact
- Does **not** claim full API surface coverage
