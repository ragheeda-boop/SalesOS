# Risk Register

> **Canonical source** for every program risk (R-01 through R-10) and every constraint-derived risk referenced elsewhere in this program (`MASTER_EXECUTION_PLAN.md` §7, `IMPLEMENTATION_SEQUENCE.md` §9/§6, sprint files under `SPRINT_PLAN/`). Those documents cross-reference risk IDs from here rather than redefining them — if a risk's likelihood, impact, or mitigation changes, update it **here first**.
> **Scoring:** Likelihood (1-5) × Impact (1-5) = Score. Score ≥15 is escalated to CTO/CPO review at the next phase gate regardless of mitigation status.
> **Status values:** `Open` (actively unmitigated), `Mitigating` (mitigation in progress, tracked against a specific sprint), `Monitoring` (mitigation shipped, watching for recurrence), `Closed` (mitigation verified, risk retired).

---

## Register

| ID | Risk | Likelihood | Impact | Score | Status | Mitigation | Owner | Tracked at |
|---|---|---|---|---|---|---|---|---|
| **R-01** | Cross-tenant IDOR-class bug recurs in a new commercial-layer table (Subscription, UsageMeter, or any table added in Phases 1-5), the way it did in Decision Center | 3 | 5 | 15 | **Closed — DB-layer coverage confirmed present** | `postgres_repo.py` filters on a real, indexed `tenant_id` column sourced from `Depends(get_current_tenant_id)` (trusted, not client-supplied). Service-layer coverage verified in Sprint 01 (`test_decision_center.py`, direct/audit/feedback/listing/aggregation isolation). **Verified Sprint 02:** `domains/decision_center/tests/test_postgres_repo.py` — a file discovered during Sprint 02's baseline test run, not written under this program's tracked sprint work but present and passing (9/9 tests, all against `PostgresDecisionCenterRepository` with a real `db_session`, covering the identical direct/audit/feedback/listing/aggregation surface against actual SQL, not the in-memory repo) — independently re-run via `docker compose exec backend pytest domains/decision_center/tests/`, 64/64 pass. The `salesos_test` non-existence blocker noted in Sprint 01 no longer applies: the database exists in the running local Docker stack (`docker compose exec backend` reaches it directly); the earlier failures were an artifact of running bare `pytest` outside the container with no `POSTGRES_HOST` override, not a missing database. | Security Eng / CTO | Sprint 01 (service layer), Sprint 02 (DB layer, confirmed) |
| **R-02** | Odoo remains the only proven connector at GA, silently invalidating the Integration Hub's "generic framework" claim | 3 | 4 | 12 | Open until Sprint 19 | Second connector (SAP or HubSpot) scoped no later than Sprint 16, certified by an engineer other than `OdooAdapter`'s author, explicit go/no-go gate at Phase 4 exit | Chief Architect | Sprint 16-19 (EPIC-11); **the single most important risk-closure event in the plan** — see `IMPLEMENTATION_SEQUENCE.md` M5 |
| **R-03** | Team headcount growth (roster curve in `ENGINEERING_ROADMAP.md`) doesn't materialize on schedule | 3 | 4 | 12 | Monitoring, reviewed every phase gate | Phase 4/5 scope is the designated flex point if headcount lags; Phase 0-2 scope is not negotiable | Program Director | Reviewed at every Phase Go/No-Go gate |
| **R-04** | Test debt (Grade D, 13.8% test-to-source ratio per `CANONICAL_ARCHITECTURE.md` §17) is carried forward into new commercial-layer code | 4 | 4 | 16 | **Mitigating, mechanism shipped Sprint 02** | New-code coverage gate (≥80%) implemented as `scripts/check_diff_coverage.py` (self-contained, no new dependency), wired into `.github/workflows/ci.yml`'s `test-backend` job (PR-only). Verified 19/19 unit tests pass (`tests/unit/test_check_diff_coverage.py`) and separately hand-verified against a real 35-file historical commit, which caught 2 real bugs before shipping: a `UnicodeDecodeError` from platform-default subprocess encoding against this Arabic-first codebase, and a repo-root-vs-CWD path mismatch between `git diff` and `coverage.xml`, both fixed (`--relative`, explicit `encoding="utf-8"`). Not yet observed gating an actual PR in production CI (that requires a real PR event) — mechanism-verified, not yet field-verified. | QA Lead | Sprint 02 (`TEST_STRATEGY.md` §0.1) |
| **R-05** | Billing integration (Stripe) edge cases (proration, dunning, failed-payment retries) under-tested, causing revenue leakage or wrongful tenant suspension | 3 | 4 | 12 | Mitigating Sprint 05-07, Monitoring thereafter | Dedicated EPIC-05 test matrix covering every subscription-state transition before Partner Beta; 20-transaction sandbox soak required before production billing mode | Backend Lead | Sprint 05-07 (EPIC-05); production-mode cutover Sprint 19 |
| **R-06** | AI cost overrun once tenant-facing AI Studio and GTM Intelligence (LLM-heavy) go live without per-plan token ceilings enforced | 3 | 3 | 9 | Mitigating from Sprint 07 | Token-ceiling enforcement deliberately pulled forward to Sprint 07 (EPIC-06), ahead of the AI Studio feature itself (Sprint 20, EPIC-12) that makes it visible to tenants | AI/ML Lead | Sprint 07 (mechanism); Sprint 20 (visible feature) |
| **R-07** | Marketplace certification pipeline becomes a bottleneck once external parties want to publish | 2 (pre-GA) / High (post-GA) | 3 | 6 (pre-GA) | Monitoring | Explicitly scoped as first-party-only through GA; third-party publishing is named Phase 7+ backlog, not promised at GA | CPO | Post-GA backlog (see `IMPLEMENTATION_SEQUENCE.md` §4 Blocked Work) |
| **R-08** | Frontend build failure (pre-existing, documented gap) blocks CI entirely once more frontend work lands on top of it | 4 | 4 | 16 | **Closed, independently verified** | Fixed in Sprint 01; `npx tsc --noEmit` re-run independently 2026-08-xx, zero errors, suppression flags confirmed removed from `next.config.js` | Frontend Lead | Sprint 01 (EPIC-03) |
| **R-09** | Alembic drift (5 revisions behind) causes a migration collision once commercial-layer tables are added | 4 | 4 | 16 | **Mitigating — partial fix only** *(superseded in practice by **R-20** / **DB-05**)* | Migration 0052 backfills `tenant_id` correctly (reviewed, sound upgrade/downgrade pattern), but the underlying drift audit found **13 items**, not the originally-documented 5, and only 1 of 13 is resolved (`decision_center_decisions`/`decision_center_templates` tenant_id columns). 12 tables (`webhook_endpoints`, `scoring_scorecards`, `revenue_analytics_snapshots`, and 9 more) still have no `create_table` migration at all — explicitly deferred to Sprint 02 in `SPRINT_01_REPORT.md`, not silently dropped. **Footnote:** the "5 revisions behind / Sprint 02 remainder" framing is stale; systemic ORM↔DB drift is now tracked as **R-20** under Repository Schema Reconciliation Program Story **DB-05** — keep this row for history, escalate/mitigate via R-20/DB-05. Do not mark `Closed` until DB-05 closes the drift class. **STORY-02-01 link (2026-08-01):** of 9 ORM `tenant_id` tables missing from `ALL_TENANT_TABLES`, **8** are blocked here (no CREATE TABLE) — do not ENABLE RLS on them until DB-05 lands migrations; see **R-25** for the separate 72-AC / Category B design gap. | Backend Lead | Sprint 01 (partial); **active track → R-20 / DB-05** (Slice 0 inventory DEC-111; next Slice 1 CREATE for 8) |
| **R-11** | `pyproject.toml` `testpaths` excludes several `domains/*/tests/` directories (including `domains/workflow/tests/` and `domains/decision_center/tests/`) — meaning "tests pass" claims can be true for the measured scope while missing real regressions in unlisted directories | 4 (confirmed occurred twice now) | 4 | 16 | **Closed for the two directories that mattered to Sprint 01; open as a general audit item** | `domains/workflow/tests` and `domains/decision_center/tests` added to `testpaths` (verified via diff) — and immediately paid off: re-running the newly-discovered `domains/workflow/tests` independently surfaced a genuine pre-existing bug (see R-12) that the prior "1665 passed" figure had never touched. Still open: whether any *other* domain directories share this blind spot hasn't been audited. | QA Lead | Directories fixed Sprint 01 (ad hoc); full audit still not a scheduled story in `PROGRAM_PLAN.md` |
| **R-12** | `WorkflowExecution` IDs (`domains/workflow/engine.py:118`) are generated from a raw `datetime.now(timezone.utc).timestamp()` float with no additional entropy — two `execute()` calls close enough together can receive an identical ID and collide/overwrite in the repository, silently losing an execution record | 3 | 3 | 9 | **Closed, independently verified** | Fixed: `engine.py:118` now uses `id=f"exec_{workflow.id}_{uuid.uuid4().hex}"` (full UUIDv4 hex, 32 chars, no truncation). Also fixed the identical pattern found one method away at `engine.py`'s `_execute_step` (`WorkflowExecutionStep.id`) — same defect class, same file, disclosed as an in-scope extension rather than left half-fixed. **Compatibility verified before applying:** `WorkflowExecutionModel.id` is `String(64)`; new format is 50 chars (`exec_` + 12-char workflow id + `_` + 32-char uuid hex), 14 chars of margin; no regex/pattern validators on execution IDs anywhere in schemas/router; no code parses or splits the ID string; frontend does not consume `execution_id` at all. **Test evidence:** added 3 new regression tests to `tests/unit/test_workflow_engine.py` (50-iteration rapid-sequential uniqueness, 50-way concurrent `asyncio.gather` uniqueness, within-run step-id uniqueness) — all pass. Re-ran `domains/workflow/tests/`: 143/143 pass (was 142/143 before the fix — the exact failure closed, nothing else moved). Re-ran the combined non-integration suite: 1813 passed/14 failed (was 1809/15 — same delta, confirms no new regression introduced). Postgres-repo integration path remains untested end-to-end (blocked by the pre-existing missing `salesos_test` DB, tracked jointly under R-01/R-11, unchanged by this fix). | Backend Lead | Closed same session as discovery, ahead of Sprint 02 |
| **R-13** | 7 `tests/unit/test_graphql.py` tests fail in a bare `poetry install` environment but were not listed in the Sprint 01 Docker-based validation report's "Remaining Failures" — environment parity between local dev and the Docker/CI image is unconfirmed | 2 | 2 | 4 | **Open — needs a parity check, not urgent** | Confirm whether the Docker image's resolved dependency versions (note: `poetry.lock` is currently untracked/new in this repo) match a bare `poetry install`; if they diverge, that's a reproducibility risk for every future sprint's "tests pass" claim, not just this one | QA Lead / DevOps-SRE | Not yet scheduled |
| **R-14** | The application's database role (`salesos`) is a Postgres **superuser with BYPASSRLS** (`SELECT rolsuper, rolbypassrls FROM pg_roles` → both `t`). Superusers and BYPASSRLS roles unconditionally bypass every RLS policy — `FORCE ROW LEVEL SECURITY` has no effect on them. **Sprint 03's entire "RLS rollout, complete" story cannot achieve real tenant isolation as designed until this changes.** | 5 (confirmed, not probabilistic) | 5 | 25 | **CLOSED — Local/CI/compose staging/prod-template + Railway staging + Railway production remediated & verified (DEC-016)** *(Ops 2026-08-01 Option A via CLI: salesos_app NOSUPERUSER/NOBYPASSRLS; bypass-probe PASS staging+prod; APP_POSTGRES_* set; health 200; no app image promote; S04-04 CLOSED)* | **Local dev (2026-07-31):** `infra/docker/postgres/init/02-app-role.sql` provisions `salesos_app` (`NOSUPERUSER NOBYPASSRLS NOCREATEROLE NOCREATEDB NOREPLICATION LOGIN`), `app/config.py`'s `app_database_url` / `app/database.py`'s `engine`+`owner_engine` split, end-to-end proof (bypass-probe isolates, full regression unchanged). See prior row detail / `R-14_REMEDIATION_REPORT.md`. **Enterprise rollout (2026-07-31, same day):** the same `02-app-role.sql` is already auto-provisioned by every compose file's postgres service (`docker-compose.yml`, `docker-compose.prod.yml`, `infra/staging/docker-compose.staging.yml`, `infra/staging/docker-compose.staging-virtual.yml` — all mount `infra/docker/postgres/init` as `docker-entrypoint-initdb.d`); the enterprise-rollout task was mainly wiring `APP_POSTGRES_USER`/`APP_POSTGRES_PASSWORD` into each environment's env file/template (`.env.staging`, `.env.staging.local`, `.env.production`, `.env.production.template`, `backend/.env.production.template`, `docker-smoke.yml`'s generated `.env`) plus an explicit provisioning step for **CI** (`.github/workflows/ci.yml`'s `test-backend`/`integration-backend` jobs use GitHub Actions' ephemeral `services:` Postgres, which can't bind-mount an init-script directory before checkout — added an explicit `psql -f 02-app-role.sql` step instead). **A real portability bug was found and fixed during this rollout:** the script hardcoded `GRANT CONNECT ON DATABASE salesos`, which only worked by coincidence where a database literally named `salesos` existed on the same server — it failed outright (`database "salesos" does not exist`) against CI's `salesos_test`-only instance. Fixed to `EXECUTE format('GRANT CONNECT ON DATABASE %I ...', current_database())`. **Verification (all local simulations, not live GH Actions/VPS/executed deploys):** a throwaway `pgvector/pgvector:pg16` container matching CI's exact image/env, `infra/staging/docker-compose.staging-virtual.yml`'s postgres service (fresh volume), and `docker-compose.prod.yml`'s postgres service (fresh volume, isolated `-p` project name) each independently re-ran the identical bypass-probe: owner role (`salesos`) leaks both tenants, `salesos_app` isolates to one — confirmed on all three. Full regression suite re-run post-rollout: 2,604 passed / 29 failed / 4 skipped (larger total than the local-dev-only baseline because `pyproject.toml` testpaths — uncommitted, from earlier Sprint 02 work — now collects additional module/domain test directories); all 29 failures classified and confirmed unrelated to database roles/RLS (7 are the already-tracked R-13 GraphQL issue, 22 are pre-existing fixture/business-logic gaps e.g. missing `tenant_id` in contact-creation test fixtures) — zero failures reference roles, permissions, or RLS, and today's diff touches no Python source at all, only SQL/YAML/env/docs. **Railway CLOSED (DEC-016, 2026-08-01):** Option A executed via Railway CLI on project `responsible-comfort` (MCP Unauthorized). Staging then production: provisioned `salesos_app` on managed Postgres (owner role `postgres`, DB `railway`); set `APP_POSTGRES_*` + host/db from existing `DATABASE_URL`/plugin; bypass-probe PASS both envs; `/health` 200; env-triggered redeploy of existing images only (**no app image promote**). Secrets not committed. See `docs/program/decisions/DEC-016-RAILWAY-R14-OPTION-A.md`. | Chief Architect / DevOps-SRE (closed); Program Director (Phase 0 gate — DEC-086) | Discovered Sprint 02 (STORY-02-01 hand-test); escalated via `DECISION_LOG.md` DEC-013; local dev remediated (DEC-014); **CI/staging/prod-template remediated (DEC-015)**; **Railway CLOSED (DEC-016)**; Phase 0 (DEC-008 / R-14) exit **GO** (DEC-086) — production GA still **NO-GO** (**CI GREEN not met**) |
| **R-10** | Odoo write-back feedback loop (SalesOS writes an AI score back to Odoo, then re-reads it as if fresh) corrupts a risk model | 2 | 3 | 6 | Closed at Sprint 09-10 | `ConflictResolutionPolicy` (OBJ-333) explicitly excludes SalesOS-authored fields from reverse-mapping, enforced in `FieldMappingConfig`, not developer memory | Backend Lead | Sprint 09-10 (EPIC-08, STORY-08-06) |
| **R-16** | Deploy Production's pre-deploy branch guard hardcoded `refs/heads/main`, failing unconditionally on this repo's `master` — 100% of production deploys blocked at the gate regardless of CI or infrastructure state | 5 (confirmed) | 4 | 20 | **Closed, independently verified** | Fixed by Sprint 05 CI-01: guard corrected to `refs/heads/master` (commit `61e08d4`), verified on real GitHub Actions run `30648063788` — `pre-deploy-check` PASS; pipeline now reaches the deployment stage. The step still only checks the branch, not CI conclusion; a genuine Checks-API CI-status gate is deferred to tech-debt backlog until CI-02…CI-13 make CI green | DevOps-SRE | Closed Sprint 05 (CI-01), 2026-07-31 |
| **R-17** | Production deploy infrastructure configuration gaps: missing `VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY` secrets (Deploy Blue Slot SSH step failed in run `30648063788`; rollback SSH host missing, triage #13) and GHCR 403 blocking staging image pushes (triage #15/#16) — deployments cannot reach or publish to infrastructure | 5 (confirmed) | 4 | 20 | Open — mitigating (direction) | Tracked as Sprint 05 stories CI-09 (SSH/VPS secrets) and CI-08 (GHCR 403). **DEC-104 (2026-08-01):** GHCR leg → prefer **Option A** (org Packages write for Actions); interim honesty **Option D** (code path vs full+publish CI GREEN); do **not** unauthorized push soften / alternate registry. Field: Stage 6 **build** OK / push **403** @ `927276f` / `30690622307`. YAML already has `packages: write` — ops outside repo-code. SSH/VPS leg still CI-09 | DevOps-SRE | Sprint 05 (CI-08, CI-09); DEC-104 |
| **R-18** | Residual high-severity npm advisories in the frontend — primarily the ESLint/Jest dev-toolchain DoS chain (`brace-expansion`/`minimatch`; npm frames eslint→10 / jest→25 / ts-jest→27 — **jest→25 is a STOP downgrade** vs locked jest 29). Cluster B (`sharp <0.35.0` libvips / Trivy GHSA-f88m-g3jw-g9cj) addressed in lock via CI-14 Slice 1. Cluster A ESLint leg cleared via CI-14 Slice 2 (eslint **10.8.0** @ `240f9a8` / DEC-072). Host `npm audit --audit-level=high` → **0** vulnerabilities post Slice 1+2. Optional Jest **29→30** modernization is **not** an R-18 advisory residual (DEC-100 STOP; DEC-108 backlog) | 4 | 3 | 12 | **Closed** (DEC-100; advisory residual cleared; CI-14 **CLOSED** DEC-108) | Closed after CI-14 Slice 1 (sharp) + Slice 2 (eslint 10). Evidence: host `npm audit` **0** high on tip `240f9a8`. **CI-14 CLOSED** under DEC-108 revised AC (security modernization complete; Jest 30 optional backlog — not R-18). Do not `--force`; do not silent Jest major without dedicated Stage 3 **0**-fail evidence package | Frontend Lead | Closed Sprint 06 (CI-14 Slice 1–2 + DEC-108 executive close) |
| **R-19** | Analytics ORM↔DB drift — migration `app/alembic/versions/0014_analytics.py` creates `analytics_reports` with 9 columns; `ReportModel` (`domains/analytics/infrastructure/models.py`) defines 14 (`metrics`, `dimensions`, `filters`, `visualization_type`, `created_by` exist only in the ORM); live `salesos` DB has the 9 migrated columns (verified via `information_schema`). Any ORM write touching the 5 missing columns fails with `UndefinedColumnError`; blocks S04-01 adversarial RLS verification (7/7) | 5 (confirmed) | 4 | 20 | **Closed** (CI-15, 2026-07-31) | Resolved by CI-15 migration `07e3ec4084fc_analytics_schema_reconciliation` (down_revision `0afbf3e6ae53`): 5 columns added with `ReportModel`-matching types/defaults; upgrade/downgrade/re-upgrade PASS; `analytics_reports` ORM=DB verified (14 columns); adversarial suite 7/7 PASS — see DEC-021 | Backend Lead | Sprint 05 (CI-15) — CLOSED; blocked S04-01 completion resolved |
| **R-20** | Systemic ORM↔Database drift at head — full `alembic check` against the head database (`0afbf3e6ae53`) shows repository-wide divergence beyond analytics: `emails`/`meetings` `id`/`tenant_id` UUID vs String(36); `dead_letter_queue.id` INTEGER vs UUID; `companies` ORM-removed columns (`branch_count`, `revenue_prev_year`, `tsv`, `search_vector`, `parent_company_id`…); index renames `ix_rev_*`→`ix_*` across opportunities/tasks/contacts/webhooks; nullable/type deltas on workflow, notifications, scheduled_jobs, feature tables. Discovered by CI-15 | 4 | 5 | 20 | Open — mitigating (inventory) | Dedicated Repository Schema Reconciliation Program Story **DB-05** (multi-sprint). **Slice 1 CREATE CLOSED (DEC-113):** Alembic head `b8d4f02a1c06`; P0 R-09 missing CREATE **0/8** (CREATE landed; still no RLS). P1 = emails/meetings type drift + companies/index/nullable; DLQ Integer PK now ORM+migration aligned (historic UUID note stale until re-check). Slice 0 inventory DEC-111 retained. Scope discipline (DEC-020/CI-15): local stories fix local drift only | Backend Platform | Next: DB-05 Slice 1 additive CREATE for 8 deferred tables (no RLS yet); companion DEC-111 |
| **R-21** | Backend dependency vulnerabilities revealed by pip-audit once CI-02 made the tool run to completion (poetry export of salesos/backend, 98 deps): python-multipart 0.0.9 (DoS in multipart header parsing, PYSEC-2026-3036/3037/3038/3039/3040/1851/1852, fix 0.0.27), strawberry-graphql 0.243.1 (WS auth-bypass PYSEC-2026-133, WS DoS 134, relay type confusion 1946, depth/alias-limit bypasses 2283/2284, fixes up to 0.315.7), starlette 0.37.2 (PYSEC-2026-161/248/249/1941/1943/2280/2281 — clear-all floor **>=1.3.1**), ecdsa 0.19.2 (PYSEC-2026-1325). Historical: CI security-pip-audit --strict failed on these findings (exit 1) — real findings, not tooling | 4 | 4 | 16 | Open — mitigating | Dedicated Backend Dependency Security Remediation Story **CI-16** (standalone, NOT part of CI-02; DEC-023). **Mitigating progress (DEC-051):** Slice 1 COMPLETE at 1e73a2f — python-multipart **0.0.9 → 0.0.32**. **Starlette → CI-22 CLOSED (DEC-109)** (DEC-052 / DEC-054; **DEC-081 Phase 1** @ 442af64: fastapi **0.141.1** / starlette **1.3.1** / pydantic **2.13.4** — starlette floor met; CI-22 CLOSED DEC-109; run 30681284601 @ 2c7587 pip-audit showed **0** starlette findings). **Mitigating progress (DEC-056):** Slice 3 COMPLETE at d3f1eef — strawberry-graphql **0.243.1 → 0.323.2**. **ecdsa disposition (DEC-057 Option A Accepted):** residual risk accepted (upstream Minerva/PYSEC-2026-1325 **no planned fix**; JWT paths **RS256/HS256 only**). **CI policy align (DEC-090):** CI security-pip-audit adds **named** --ignore-vuln PYSEC-2026-1325 only; --strict retained (no silent weaken). Evidence: run 30681284601 sole finding was ecdsa PYSEC-2026-1325. **Trivy align (DEC-098):** `.trivyignore` named **CVE-2024-23342** only (same residual; Trivy ID namespace) + CI Secrets Scan `trivyignores`; exit-code 1 retained. Evidence: run 30684813480 sole HIGH was ecdsa CVE-2024-23342. **CI-16 CLOSED**. Residual package remains in lock (monitor; Option B PyJWT deferred). Stage 5 pip-audit + Secrets Scan expected **green** after DEC-090/098; **whole-pipeline CI GREEN not met** (other gates) | Backend | Stage 5 pip-audit DEC-090 + Trivy DEC-098 named ignores; ecdsa residual monitored; starlette floor met
| **R-15** | `POST /meetings/{opportunity_id}/brief` (`app/routers/meetings.py::get_meeting_brief`) fetched an opportunity via `PostgresOpportunityRepository.get(opportunity_id)` — which has no `tenant_id` parameter and applies no tenant filter — and never compared the result's `tenant_id` against the caller's authenticated tenant. Any authenticated user of any tenant could request another tenant's meeting brief (company name, recent signals, contacts, deal value/stage) by opportunity_id alone. Every sibling endpoint in `app/routers/opportunities.py` already carried the compensating check; this one caller had been missed. | 5 (confirmed, live, exploitable as found) | 4 | 20 | **Closed, fixed and tested same session** | Discovered opportunistically while selecting a real demonstration target for STORY-01-04 (Sprint 02) — not one of Sprint 02's assigned stories, but qualifies for the Sprint Execution Contract's small-fix carve-out (2-line change, isolated to one function, no API/schema change, exactly mirrors the existing `if not opp or getattr(opp, "tenant_id", None) != tenant_id: raise 404` pattern already used 4 times in `opportunities.py`) — fixed inline rather than left live. Regression test added: `tests/unit/test_meeting_brief_tenant_isolation.py`, using the new `tests/support/tenant_isolation.py` harness (its first real, non-demo consumer). The underlying repository method (`PostgresOpportunityRepository.get()`) is deliberately left unfixed — adding a required `tenant_id` parameter touches its abstract interface, the in-memory counterpart, and every other call site, which is architecture debt outside this fix's scope, not absorbed here. | Backend Lead | Discovered and closed Sprint 02, same session |
| **R-22** | MyPy debt — ~308 type errors keep Stage 2 Backend Types red after CI-07 removed the masking `cli/` path; real type debt now visible and unremediated | 5 (confirmed) | 3 | 15 | **Closed** (DEC-096 field Types 0) | Phased Backend Types remediation under program story **CI-20** (not mechanical this sprint). Evidence: CI run `30670339985` (commit `3801151`) — Backend Lint SUCCESS (CI-10), Backend Types FAILURE with **308 mypy errors**. **Mitigating progress (DEC-046):** CI-20 **Phase 1 COMPLETE** at `65296174` — `app/modules/admin` **34 → 0**; overall expected **~308 → ~274**. **Mitigating progress (DEC-047):** CI-20 **Phase 2 COMPLETE** at `01b6a8ae` — `app/modules/company` **25 → 0**; overall expected **~274 → ~249**. **Mitigating progress (DEC-048):** CI-20 **Phase 3 COMPLETE** at `e5b4471` — `app/modules/entity_resolution` **14 → 0**; overall expected **~249 → ~235**. **Mitigating progress (DEC-049):** CI-20 **Phase 4 COMPLETE** at `4b3a436` — `app/modules/identity` **16 → 0**; overall expected **~235 → ~219**. **Mitigating progress (DEC-050):** CI-20 **Phase 5 COMPLETE** at `7d8126e` — `app/modules/revenue_execution` **10 → 0**; overall expected **~219 → ~209**. **Mitigating progress (DEC-053):** CI-20 **Phase 6 COMPLETE** at `38127af` — `app/modules/sso` **8 → 0**; overall expected **~209 → ~201**. **Mitigating progress (DEC-055):** CI-20 **Phase 7 COMPLETE** at `802cde5` — `app/routers` **44 → 0**; overall expected **~201 → ~157**. **Mitigating progress (DEC-058):** CI-20 **Phase 8 COMPLETE** at `3e7fadb` — `app/main.py` **15 → 0** + `sdk/` **31 → 0** (host light: main **3 → 0**, sdk **33 → 0**); overall expected **~157 → ~111**. **Mitigating progress (DEC-059):** CI-20 **Phase 9 COMPLETE** at `821aad5` — `app/modules/demo_mode` **11 → 0**; overall expected **~111 → ~100**; field-verify `30677025355` **104** / 0 demo_mode. **Mitigating progress (DEC-060):** CI-20 **Phase 10 COMPLETE** at `ca76f9c` — `app/modules/communication_hub` **11 → 0**; overall expected **~100 → ~89** (field **104 → ~93**); validation **light validated** (host mypy) + CI Observer field-verify: run `30677381493` (commit `cbea4be`, includes types `ca76f9c`; workflow later cancelled by tip) Backend Types **failure** **93** errors / **0** `communication_hub` (baseline `30677189129` @ `1c33c1b`: **104** / **11** hub). Tip corroboration `30677457937` (`deab399`) also **93** / 0 hub. Classification: **pre-existing** residual (not Phase 10 regression; not infra). No hub code fix. **Mitigating progress (DEC-061):** CI-20 **Phase 11 COMPLETE** at `86b4094` — `app/modules/work_intelligence` **5 → 0**; overall expected **~89 → ~84** (field **93 → ~88**); validation **light validated** (Docker mypy). **Mitigating progress (DEC-064):** CI-20 **Phase 12 COMPLETE** at `e44b7f3` — `app/boot/startup.py` **7 → 0** + `app/database.py` **7 → 0**; overall expected **~84 → ~70** (field **88 → ~74**); validation **light validated** (host mypy). **Mitigating progress (DEC-066):** CI-20 **Phase 13 COMPLETE** at `5edb6d6` — `app/application` **9 → 0**; overall expected **~70 → ~61** (field **~74 → ~65**); validation **light validated** (host mypy `--follow-imports=skip`). **Mitigating progress (DEC-068):** CI-20 **Phase 14 COMPLETE** at `1f14337` — SSO remnants **11 → 0**; overall expected **~61 → ~50** (field **~65 → ~54**); validation **light validated** (host mypy SSO `--follow-imports=skip`) + CI Observer field-verify: run `30679062993` (`1f14337`; job `91312218365`) Backend Types **failure** **54** errors / **0** SSO (baseline `30678653664` @ `1bf30d2`: **65** / **11** SSO). Docs tip `30679120749` / tip `30679150001` also **54** / 0 SSO. Classification: **pre-existing** residual (not Phase 14 SSO regression; not infra). No SSO code fix. **Mitigating progress (DEC-071):** CI-20 **Phase 15 COMPLETE** at `7fed3dc` — sla_monitor+redis_client **5 → 0**; overall expected **~46 → ~41** (field **~50 → ~45**); validation **light validated**. **Mitigating progress (DEC-070):** CI-20 **Phase 16 COMPLETE** at `26156df` — `app/startup.py` **4 → 0**; overall expected **~50 → ~46** (field **~54 → ~50**); validation **light validated** (host mypy skip+silent). **Mitigating progress (DEC-078):** CI-20 **Phase 17 COMPLETE** at `d5e4de2` — `app/modules/decision` **5 → 0**; overall expected **~41 → ~36** (field **~45 → ~41**); validation **light validated** (host mypy decision `--follow-imports=skip`). **CLOSED DEC-096:** field Backend Types **0** on `220d91a` (run `30684023356` / job `91326366120`); tip `af4835f` (`30684308678` / `91327119501`); docs close `7cd00c2`. | Backend Lead | Sprint 05+ (**CI-20**); evidence run `30670339985`; Phase 1 `65296174`; Phase 2 `01b6a8ae`; Phase 3 `e5b4471`; Phase 4 `4b3a436`; Phase 5 `7d8126e`; Phase 6 `38127af`; Phase 7 `802cde5`; Phase 8 `3e7fadb`; Phase 9 `821aad5`; Phase 10 `ca76f9c`; Phase 11 `86b4094`; Phase 12 `e44b7f3`; Phase 13 `5edb6d6`; Phase 14 `1f14337`; Phase 15 `7fed3dc`; Phase 16 `26156df`; Phase 17 `d5e4de2`; **Mitigating progress (DEC-079):** CI-20 **Phase 18 COMPLETE** — `app/modules/entity_resolution` remnants **10 → 0**; overall expected **~36 → ~26** (field **~41 → ~31**); validation **light validated** (host mypy entity `--follow-imports=skip`). field-verify Phase 9 `30677025355`; field-verify Phase 10 `30677381493` / tip `30677457937`; field-verify Phase 14 `30679062993` / tip `30679150001` |
| **R-23** | Jest 33-suite debt — Frontend Unit Tests fail deterministically on 33 suites / 163 tests (194 total), blocking Stage 3 and overall CI GREEN | 5 (confirmed) | 3 | 15 | **Closed — Stage 3 field-verified** (DEC-077) | Tracked as **Sprint 01 Jest-debt** remediation story (**not CI-14**). Historical: CI-13 `30664173050` (33 failed) → §8 `30677189129` / `1c33c1b` (**11** failed). **Closure evidence §10:** GHA `30679804383` / `465c638` (Stage 3 job `91314523292`): **0** failing suites (196 passed / 196 total; 2278 passed / 1 skipped / 2279 tests); Stage 1 Frontend Lint **success**; Stage 3 Frontend Unit Tests **success**. Path: `4fdc1d8` + `5bba606` + `556304d` + Prettier gate `11470b1`. **Jest-debt CLOSED**. Score retained as historical severity. **Whole-pipeline CI GREEN not met** (Backend Lint/Types, pip-audit, Secrets Scan still red on `30679804383` — other risks) | Frontend Lead | Sprint 01 Jest-debt CLOSED (DEC-077); evidence `JEST_BASELINE.md` §10 |
| **R-24** | Semgrep **254** open findings now visible in GitHub Code Scanning (live count per triage refresh; CI-18 first-upload historical baseline was 253) after CI-18 fixed SARIF upload — mix of real P0-class issues (e.g. `run-shell-injection`), likely FPs (`avoid-sqlalchemy-text`), and hardening noise | 4 | 4 | 16 | **Closed — mitigating residual (DEC-105)** | Findings triage + phased remediation under **CI-19**. Evidence: `salesos/docs/audit/ga-engineering-audit/CI_19_SEMGREP_TRIAGE.md` (triage refresh `800753f`; prior `249a130`); CI-18 first-upload run `30660116232` (253 blocking / 2806 files / 595 rules). **CI-19 Wave 1 COMPLETE** at `d5c9b57` (8 GHA injections). **CI-19 Wave 3 COMPLETE** — SHA-pin DEC-069 (`556304d`) + residual DEC-074 (`465c638`). **CI-19 Wave 4 COMPLETE** — DEC-076 (`5c27470`) `.semgrepignore` + secrets-doc redact; **DEC-083** restores Semgrep built-in defaults after Observer regression ~116→~130. **CI-19 Wave 5 COMPLETE** — DEC-082 residual singletons (xml/websocket/urllib/regexp×3/prototype×2; 8 cleared). Live open after Wave 4 ~**130** (pre–Wave 5; pre–DEC-083). Wave 2 Slice 1–6 COMPLETE (DEC-091/097/099/101/102/103; Slice 6 cleared **4** app; alembic RLS accepted residual **7**) + **field-verify** Security Scan `30686789458` / `abaae85` (app text **0**; residual-only); Wave 2 PARKED; story OPEN then **DEC-105 CLOSED with residual; field-verify `30693735860`/`91352893256` @ `b9062d6` (CS/CLI **11** alembic-only)** (burned 8 non-alembic; alembic residual 11 accepted). Semgrep ERROR/WARNING gates unchanged. Triage-first — no dishonest suppressions| Security Eng / Backend | Sprint 05+ (**CI-19**); evidence `CI_19_SEMGREP_TRIAGE.md` / DEC-069 / DEC-074 / DEC-076 / DEC-082 / DEC-083 / DEC-102 / DEC-103 / field `30686789458` |
| **R-25** | STORY-02-01 “72-table” AC was **unachievable without Category B design + a pinned inventory** — was 46 policies; ORM `tenant_id` **55** (9 missing; only `company_features` additive-safe; 8 wait on R-09/DB-05); Category B → Sprint 04; 55+14≈**69**, not 72 | 5 (confirmed inventory) | 4 | 20 | **Closed-as-accepted-scope** (DEC-044 Option B) | Human accepted Option B: revised AC closes STORY-02-01 at **47** (46 + `company_features`); draft [`DEC-DRAFT-STORY-02-01-RLS-72`](decisions/DEC-DRAFT-STORY-02-01-RLS-72.md) **Superseded** by [`DEC-044`](decisions/DEC-044-STORY-02-01-RLS-OPTION-B.md). Residual gaps tracked under Sprint 04 (Category B) and **R-09** / **R-20** / DB-05 (8 tables) — not reopened as R-25. Phase 0 remains **NO-GO** (Railway R-14) | Chief Architect / Backend Lead | Sprint 03 STORY-02-01 DONE (revised AC); residual → Sprint 04 / DB-05 |

---

## Highest-Priority Cluster (Score ≥15)

R-04, R-08, R-09, R-11 (16), R-01 (15, now closed) are **all Phase 0 items** — this is the entire justification for Phase 0 (`PRODUCT_ROADMAP.md` Phase 0) existing as a dedicated, non-skippable phase before any commercial feature work begins.

**Open ≥15 CI/security debt (Sprint 05 register delta):** **R-22 Closed** (DEC-096): Backend Types field **0** on `220d91a` / tip `af4835f` -- CI-20 CLOSED; score 15 historical), **R-23 Closed** (DEC-077): Stage 3 field verify `30679804383` / `465c638` = **0** failing suites — Jest-debt no longer blocks CI GREEN; whole-pipeline still red on other gates. **R-24** (Semgrep ~254→~130 live after Wave 4; Wave 5 cleared **8** residuals via DEC-082; score 16, CI-19 — Waves 1/3/4/5 COMPLETE; Wave 2 Slice 1–6 DEC-091/097/099/101/102/103 + field-verify `30686789458` / `abaae85` app text **0** / alembic residual parked; **DEC-105 CLOSED with residual** / R-24 Closed — mitigating residual). Remaining open ≥15 items block overall CI GREEN / security-scan finding clearance independently of Phase 0 commercial-layer gates.

**R-25 (score 20, Closed-as-accepted-scope)** — STORY-02-01 72-table AC vs inventory reality resolved by **DEC-044** Option B (revised AC at 47). Distinct from R-09 (missing CREATE TABLE — still open) and R-14 (BYPASSRLS / Railway — still open for Phase 0). Residual Category B inventory **pinned DEC-110**; **B1 CLOSED DEC-112** (`branches`/`licenses`); **B2 CLOSED DEC-114** (`commercial_activities`/`commercial_quote_lines`); **B3 CLOSED DEC-115** (`analytics_report_executions`/`analytics_report_shares`); **B4 CLOSED DEC-116** (`decision_center_audits`/`decision_center_feedback`); **B5 CLOSED DEC-117** (`password_reset_tokens`/`refresh_token_families`, live policies **57**); B6–B7 READY; eight CREATE via DEC-113 (ENABLE RLS still out of Category B).

**R-14 (score 25) is the highest-scored risk in this entire register**, discovered during Sprint 02's STORY-02-01 hand-test. It is scored higher than everything else because it was not a probabilistic risk — it was a confirmed, present-tense fact about the database configuration (`salesos` is a superuser with BYPASSRLS) that made Sprint 03's stated Phase 0 exit criterion ("100% of 72 tenant-scoped tables have an RLS policy; adversarial suite passes 100%") unachievable in any meaningful sense. **Now CLOSED** — local/CI/compose staging/prod-template plus Railway staging+production remediated and independently verified under **DEC-016** (Option A; CLI evidence; see the R-14 row). The score stays 25 in this table as the historical record of what was found, not retroactively softened; the Status column is what reflects current reality. Railway R-14 / S04-04 closed under DEC-016 with bypass-probe + health evidence.

**R-15 (score 20, closed same session) was the second-highest score this register has ever carried** — a live, exploitable cross-tenant IDOR in `app/routers/meetings.py`, discovered opportunistically (not part of any assigned story) while picking a demonstration target for STORY-01-04, and fixed the same session under the Sprint Execution Contract's small-fix carve-out rather than left open pending a future sprint. Recorded here at its full pre-fix severity, not retroactively downgraded, so the register reflects what was actually found, not just what remains.

**R-26 (score 20, Closed — see DEC-085)** — S04-07's tenant_id SQL-injection fix (`6bc261c`) landed a form (`SET LOCAL app.tenant_id = :tenant_id` with a bind parameter) that Postgres itself rejects (`syntax error at or near "$1"`, reproduced directly) — it would have raised on every tenant-scoped request rather than fixing anything, and shipped unnoticed because no test exercises the real, unmocked `get_db()`. Fixed by switching to `set_config('app.tenant_id', :tenant_id, true)`, a function call Postgres does parameterize safely, verified directly (including an injection-shaped value stored inertly, not executed). **Likelihood 4 (confirmed, not probabilistic — every request with a tenant_id would hit this) × Impact 5 (would break all tenant-scoped traffic if actually deployed) = 20.** Also notable: this exact line was silently reverted three times within about an hour by CI-20 Phase 12's automated mypy pass regenerating `app/database.py` from a stale base — a real lost-update race between concurrent automated work, not a one-off; documented in DEC-085 specifically so it isn't reintroduced a fourth time. **2026-08-01 HARD STOP:** parallel agents reintroduced `SET LOCAL` a **4th** time on tip while local WIP already had `set_config`; S04-07 RLS isolation tests fail under the broken form. DEC-085 is non-negotiable — do not revert `get_db()` to `SET LOCAL` for mypy/ruff/style. Guard comment in `app/database.py`. **CI guard (2026-08-01):** `tests/unit/test_dec085_set_config_guard.py` source-inspects `get_db()` and fails if tenant GUC uses `SET LOCAL` instead of `set_config` — blocks a 5th regression without weakening RLS suites.

## Escalation Rule

Any risk scoring ≥15 that is still `Open` (not `Mitigating` or better) at its designated phase gate automatically blocks that gate's Go/No-Go decision — per `IMPLEMENTATION_SEQUENCE.md` §10 Decision Gates, this requires Program Director + the risk's named Owner to jointly present a revised mitigation plan before the gate can pass, rather than the gate silently proceeding.

## Adding a New Risk

Any newly discovered risk during execution (a Sprint's "Technical Debt Identified" note, a postmortem finding, a chaos-drill discovery) gets assigned the next sequential ID (`R-11`, `R-12`, …) and added to the table above — never silently tracked only in a sprint file or a postmortem doc, since this register is the single place `MASTER_EXECUTION_PLAN.md` and `IMPLEMENTATION_SEQUENCE.md` expect to find the complete, current set.
