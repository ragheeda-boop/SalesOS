# Sprint 05 ? Enterprise Delivery Board (Program)

> **Canonical living board** for the Sprint 05 program entry package delivered under the Enterprise Delivery Mode governance (Sprint 04 ? Sprint 05 transition package). Each story follows the governed cycle: pre-task package ? executive approval ? Phase 1 (implement + validate + local commit) ? Phase 2 (controlled push/CI evidence) ? executive close ? record updates (DECISION_LOG / RISK_REGISTER).
> Status values: `PENDING`, `IN PROGRESS`, `BLOCKED` (with reason), `REGISTERED` (standalone story created by executive decision), `COMPLETE`.

| ID | Story | Priority | Status | Blocked on / Notes |
|---|---|---|---|---|
| CI-01 | Deploy Production branch guard (`deploy.yml`) | P0 | COMPLETE | Closed DEC-017; commit `61e08d4`, run `30648063788`; R-16 closed |
| CI-08 | GHCR 403 (staging image push) | P0 | GOVERNANCE COMPLETED | **GOVERNANCE COMPLETED — SUPERSEDED DEC-150 Option B (2026-08-02):** ARB Accepted Option B — Stage 6 GHCR publish is **no longer** a required Phase 0 capability after DEC-149. Criteria **3.6 / 3.10 CLOSED — SUPERSEDED** (governance; not field GHCR green). Residual Deploy Staging [`30721601875`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30721601875) GHCR **403** = **legacy/non-blocking**. `ci.yml` Stage 6 quarantined (`if: false`). Canonical deploy path = Railway+Vercel (**3.11** / CI-09). Companion [`decisions/DEC-150-STAGE-6-GHCR-POST-DEC-149.md`](decisions/DEC-150-STAGE-6-GHCR-POST-DEC-149.md). Do **not** claim ops GHCR SUCCESS / Production GO / CI GREEN. |
| CI-11 | npm audit remediation (patch-only) | P0 | COMPLETE | Closed DEC-019; commit `060c946`, run `30649799993`; residual 30 high ? CI-14 |
| S04-04 | Railway R-14 closure | P0 | CLOSED CONDITIONAL | **CLOSED CONDITIONAL DEC-154 (2026-08-02):** Validation/Orchestrator accept DEC-126 CONDITIONAL as Phase 0 Complete (plan §3 path b). **A–E evidence retained** — D: deploy **`9664e9fc`** / `b62252a`; `salesos_app` / `rolbypassrls=False`; E: PASS_WITH_SINGLE_TENANT_CAVEAT; tip-align `d1a8c35e7f09` / policies **67**. Residual *multi-tenant live split* → Phase 1 tech debt. Companion [`DEC-154`](decisions/DEC-154-CRITERION-2-3-CONDITIONAL-SCOREBOARD-COMPLETE.md) · [`DEC-120`](decisions/DEC-120-DEC016-RAILWAY-R14-CONTRADICTED.md) §7. **Not unconditional. No Production GO. Rotate Postgres passwords** — human/ops |
| S04-01 | Adversarial RLS suite `tests/integration/test_adversarial_rls.py` | P0 | COMPLETE | 7/7 PASS after CI-15 (07e3ec4084fc); closed DEC-021; uncommitted fixes committed with CI-15 |
| CI-02 | pip-audit (Poetry) in CI | P0 | CLOSED | Closed DEC-025; commits `b330d52`+`a4e880c`; re-run `30655650484` proves chain: Poetry 2.x ? plugin ? `poetry export` ? `pip-audit` ? **24 vulns / 4 packages** ? `--strict` exit 1 on findings (toolchain failure eliminated); Security Scan `pip-audit` SUCCESS (`30655650490`); residual findings ? R-21/CI-16 |
| CI-03 | docker-smoke env var (`GF_SECURITY_ADMIN_PASSWORD`) | P0 | CLOSED | Closed DEC-027; commit `83e703e`; run `30656335600` ? **full Docker E2E Smoke Test job SUCCESS** (Validate Compose File 1s ? Build 111s ? Start 79s ? Smoke Tests 30s); interpolation issue eliminated |
| CI-07 | MyPy/Ruff `cli/` path in CI (triage #1/#2 partial) ? remove non-existent `cli/` from `mypy`/`ruff` invocations | P1 | CLOSED | Closed DEC-032; commit `b0c0069`; CI run `30661932842` (contains CI-06 change; superseded the CI-07-only run via concurrency) ? Backend Types + Backend Lint logs **0** `cannot read file 'cli'` / **0** E902 (both previously present in every run); gates now fail only on real code debt (Ruff body 3,611 ? CI-10; mypy findings to classify) |
| CI-04 | Bandit SARIF format (triage #5/#10): install `bandit[sarif]` in both workflows | P1 | CLOSED | Closed DEC-028; commit `ce59efd`; runs `30658782384`/`30658782394` ? `Upload bandit results` **SUCCESS in both**; no `Path does not exist` for Bandit; surfaced pre-existing high/high finding B324 (weak MD5, `roles_permissions.py:40`) ? gate had been SKIPPED in baseline, now executes ? CI-17; semgrep upload failure separate ? CI-18 |
| CI-05 | Trivy SARIF category collision (triage #11): distinct `category` per `upload-sarif` in `secret-scan` | P1 | CLOSED | Closed DEC-029; commit `f34bef2`; run `30659372944` ? `secret-scan` **SUCCESS** (Upload Trivy results + Upload Trivy config results PASS); 0 `Aborting upload` messages; only collision in repo was the Trivy pair |
| CI-06 | Deploy Production commit-comment permissions (triage #14) ? add `permissions: contents: write` | P1 | CLOSED | Closed DEC-033; commit `20f88bc`; Deploy Production run `30661932918` ? `Deploy Notification` **SUCCESS**, commit comment actually created by `github-actions[bot]` on `20f88bc` (0 HttpError/403); job-level least-privilege scope (notify only); remaining run failures are pre-existing blocked items (Deploy Blue GHCR 403 ? CI-08/R-17; Rollback SSH ? CI-09) |
| CI-13 | Jest suite baseline | P1 | CLOSED | Closed DEC-035; baseline captured from real CI run `30664173050` (commit `d48cc80`) - 33/194 suites failing (163 failed / 1 skipped / 2092 passed / 2256 total), full failing-suite inventory + root-cause categories in `salesos/docs/audit/ga-engineering-audit/JEST_BASELINE.md`; dependency contract for CI-14: failing suites <= 33, no new failures beyond inventory; remediation of the 33 suites = Sprint 01 Jest-debt story (not CI-14) |
| S04-05 | Adversarial write-protection tests | P1 | COMPLETE | Closed DEC-039; commit `8699796` ? suite `tests/integration/test_adversarial_write_protection.py` on master; validation **build validated** ? Docker `pytest tests/integration/test_adversarial_write_protection.py` ? **8/8 PASS in 4.88s**; **CI GREEN not met** |
| S04-06 | Adversarial suite (remaining) | P2 | COMPLETE | Closed DEC-045; commit `119df9e` ? suite `tests/integration/test_adversarial_rls_remaining.py` on master (7 tables: contacts, company_features, commercial_opportunities, opportunities, tasks, tenant_configs, webhook_subscriptions); validation **build validated** ? Docker pytest **15/15 PASS**; POLICY_COUNT **47** intact (DEC-044); S04-01/S04-05 unchanged; RLS inventory **not** reopened; **CI GREEN not met** |
| CI-12 | Trivy filesystem scan silent failure (triage #3) ? investigate/verbose reproduce before fix | P2 | CLOSED | Closed DEC-034; root cause reproduced locally on clean `git archive` snapshot with exact CI flags ? `--format sarif --output <file>` suppresses the findings table (stdout shows only npm/poetry detecting lines, exit 1); gate split into visible blocking table (exit-code 1) + SARIF export leg (exit-code 0, upload preserved); 11 real HIGH surfaced: poetry.lock 10 (ecdsa CVE-2024-23342 ? CI-16/R-21) + package-lock.json 1 (sharp GHSA-f88m-g3jw-g9cj ? CI-14); gate stays red while those exist, now visibly |
| CI-09 | Deploy path (Railway+Vercel; was VPS SSH) | P2 | CLOSED CONDITIONAL | **CLOSED CONDITIONAL DEC-149a (2026-08-02):** Arch prior PASS + Validation PASS @ [30723120473](https://github.com/ragheeda-boop/SalesOS/actions/runs/30723120473) / tip `c3507ed` — Railway up ✓; health HTTP 200 ✓; Vercel FE Git-primary ✓. Residuals: FE Git-primary (not CLI); staging deferred (single-env DEC-149); no VPS. Does **not** close CI-08 / **3.6/3.9/3.10**. Phase 0 **45/54 → 46/54 NO-GO**. No Production GO / CI GREEN / ARB invent. |
| CI-10 | Backend Lint (Ruff) remediation ? 3,611 violations body (triage #2) | P2 | CLOSED | Closed DEC-036; commit `3801151`; local ruff 0.4.10 pass (3,611?0, `app/alembic/versions/` excluded); CI run `30670339985` ? **Stage 1: Backend Lint SUCCESS**; overall CI workflow still red (MyPy, pip-audit, npm audit, Jest, etc.) ? **CI GREEN not met** |
| compose prod name fix | docker-compose production project name (`name: salesos-prod`) | P2 | CLOSED | Closed DEC-037; commit `cb707be`; validation light ? `docker compose config --quiet` fails on missing env secrets (expected); YAML `name:` valid; resolves implicit project-name collision with dev stack (DEC-015 hazard) |
| CI-20 | Backend Types remediation ? 308 mypy errors (triage #1 body, post CI-07) | P2 | CLOSED | Surfaced by CI-10 field-verify run `30670339985` (Backend Lint SUCCESS, Backend Types FAILURE); **Phase 1 COMPLETE** at `65296174` ? `app/modules/admin` **34 ? 0**; **Phase 2 COMPLETE** at `01b6a8ae` ? `app/modules/company` **25 ? 0**; **Phase 3 COMPLETE** at `e5b4471` ? `app/modules/entity_resolution` **14 ? 0**; **Phase 4 COMPLETE** at `4b3a436` ? `app/modules/identity` **16 ? 0**; **Phase 5 COMPLETE** at `7d8126e` ? `app/modules/revenue_execution` **10 ? 0**; **Phase 6 COMPLETE** at `38127af` ? `app/modules/sso` **8 ? 0**; **Phase 7 COMPLETE** at `802cde5` ? `app/routers` **44 ? 0**; **Phase 8 COMPLETE** at `3e7fadb` ? `app/main.py` **15 ? 0** + `sdk/` **31 ? 0** (host light: main **3 ? 0**, sdk **33 ? 0**); **Phase 9 COMPLETE** at `821aad5` ? `app/modules/demo_mode` **11 ? 0**; **Phase 10 COMPLETE** at `ca76f9c` ? `app/modules/communication_hub` **11 ? 0**; **Phase 11 COMPLETE** at `86b4094` ? `app/modules/work_intelligence` **5 ? 0**; **Phase 12 COMPLETE** at `e44b7f3` ? `app/boot/startup.py` **7 ? 0** + `app/database.py` **7 ? 0**; **Phase 13 COMPLETE** at `5edb6d6` ? `app/application` **9 ? 0**; **Phase 14 COMPLETE** at `1f14337` ? SSO remnants **11 ? 0**; **Phase 15 COMPLETE** at `7fed3dc` ? sla_monitor+redis_client **5 ? 0**; **Phase 16 COMPLETE** at `26156df` ? `app/startup.py` **4 ? 0**; **Phase 17 COMPLETE** at `d5e4de2` ? `app/modules/decision` **5 ? 0**; **Phase 18 COMPLETE** ? `app/modules/entity_resolution` remnants **10 ? 0**; overall expected **~308 ? ? ? ~36 ? ~26** (field **~41 ? ~31**); Phases 1?21 COMPLETE; **CLOSED DEC-096** ? field Backend Types **0** on tip `220d91a` (run `30684023356` / job `91326366120` SUCCESS); tip corroboration `af4835f` run `30684308678` / job `91327119501` SUCCESS). Path: Phase 21 `17c1eee` ? syntax `5588bb7` ? audit imports `a636c69` ? `getattr` registry `220d91a`. DEC-085 `set_config` intact. Owner Backend Lead; **whole-pipeline CI GREEN not met** |
| CI-14 | Frontend Dependency Modernization | P1 | CLOSED | **CLOSED DEC-108** (executive AC). Security modernization MET: Slice 1 sharp **0.35.3** (DEC-063 @ `435ba5d`); Slice 2 eslint **10.8.0** (DEC-072 @ `240f9a8`); host npm audit **0**; R-18 **Closed**. Slice 3 Jest 29->30 **STOPPED** (DEC-100) - optional backlog, not close gate. No silent Jest major. Not CI-08/CI-22/Railway. **whole-pipeline CI GREEN not met** |
| CI-15 | Analytics Schema Reconciliation (add `metrics`, `dimensions`, `filters`, `visualization_type`, `created_by` to `analytics_reports` via Alembic) | P0 | CLOSED | Closed DEC-022; migration `07e3ec4084fc`; Phase 1 validation PASS + Phase 2 push `4793b08` ? CI run `30652813475` (matrix identical to baseline `30649799993`; only delta = 10 new Ruff style violations from the migration file in the pre-existing red Backend Lint gate, transferred to the lint backlog, not fixed in CI-15); R-19 closed |
| DB-05 | Repository Schema Reconciliation Program - systemic ORM/DB drift (R-20) | P1 | COMPLETE | **Slice 4 / 7.5 CLOSED** (DEC-123a); **7.4 CLOSED** (DEC-129a KEEP) @ `4aacd6d`. **7.6 VERIFIED/CLOSED** (DEC-130h) @ `250bcb5` / DEC-130g — Arch+Val PASS; live Docker `alembic check` **exit 0** @ head `a4f7c29e1b80`; phased 5a–5g; residual `ix_graph_nodes_search` KEEP (non-blocking). Companions [`DEC-123`](decisions/DEC-123-DB-05-SLICE-4-DEFERRED-8-RLS.md) · [`DEC-129`](decisions/DEC-129-DB-05-COMPANIES-DEAD-COLUMN-KEEP.md) · [`DEC-130`](decisions/DEC-130-DB-05-CRITERION-7-6-ALEMBIC-CHECK-PHASED.md) · [`DEC-130b`](decisions/DEC-130b-DB-05-SLICE-5B-METADATA-CLASSIFY.md) · [`DEC-130c`](decisions/DEC-130c-DB-05-SLICE-5C-ADMIN-GLOBAL-CREATE.md) · [`DEC-130d`](decisions/DEC-130d-DB-05-SLICE-5D-INDEX-TYPE-NULLABLE.md) · [`DEC-130e`](decisions/DEC-130e-DB-05-SLICE-5E-COMPANIES-RESIDUAL-KEEP.md) · [`DEC-130f`](decisions/DEC-130f-DB-05-SLICE-5F-ORPHAN-KEEP-REGISTER.md) · [`DEC-130g`](decisions/DEC-130g-DB-05-SLICE-5G-INDEX-FK-COMMENT.md). Phase 0 criteria **7.1–7.6 CLOSED**. **CI GREEN not met** |
| CI-16 | Backend Dependency Security Remediation ? remediate `python-multipart`, `strawberry-graphql`, `starlette`, `ecdsa` vulnerabilities found by `pip-audit` (R-21) | P1 | CLOSED | Standalone, DEC-023; NOT part of CI-02. **Slice 1 COMPLETE** at `1e73a2f` (DEC-051): multipart **0.0.9 ? 0.0.32**. **Slice 2 BLOCKED ? CI-22** (DEC-052/DEC-054; Phase 1 DEC-081 cleared starlette floor). **Slice 3 COMPLETE** at `d3f1eef` (DEC-056): strawberry **0.243.1 ? 0.323.2**. **ecdsa:** Accepted residual **Option A** (DEC-057). **CI policy (DEC-090):** named `--ignore-vuln PYSEC-2026-1325` on CI Stage 5 pip-audit; `--strict` retained. **Trivy (DEC-098):** named `CVE-2024-23342` in `.trivyignore` (same residual; evidence `30684813480`). Package [`DEC-057-CI-16-ECDSA-DISPOSITION.md`](decisions/DEC-057-CI-16-ECDSA-DISPOSITION.md). **Stage 5 pip-audit + Secrets Scan expected green**; **whole-pipeline CI GREEN not met** |
| CI-17 | Bandit B324 remediation ? weak MD5 in `roles_permissions.py:40` (`hashlib.md5` used to derive `role_id`; add `usedforsecurity=False`) | P1 | CLOSED | Closed DEC-031; commit `b87fb22`; run `30661609555` ? **Stage 5: Bandit SAST fully GREEN** (incl. fail-on-high gate, 0 high findings); digest byte-identical, 45 admin tests pass; no regression |
| CI-18 | Semgrep SARIF upload ? fix `--severity ERROR,WARNING` (invalid comma list ? repeatable flags) in `sast-scan` | P2 | CLOSED | Closed DEC-030; commit `6ab1d0e`; run `30660116232` ? **entire Security Scan workflow GREEN for first time** (secret-scan, sbom, sast-scan, pip-audit, npm-audit, report all SUCCESS); `Upload semgrep results` PASS, 0 CLI parse errors; semgrep surfaced **253 blocking findings** (2806 files, 595 rules) ? CI-19 |
| CI-19 | Semgrep findings remediation ? 253 blocking findings surfaced by CI-18 run 30660116232 (2806 files, 595 rules); now visible in GitHub code scanning | P2 | CLOSED | Triage + Waves 1/3/4/5 COMPLETE; Wave 2 Slice 1?6 + field-verify PARKED (DEC-103). **DEC-105 CLOSED with residual** + **field-verify** Security Scan [`30693735860`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30693735860) / sast `91352893256` @ `b9062d6`: Semgrep CLI **11** blocking; CS open Semgrep **11** alembic-only; burned **8** **fixed** (`fixed_at=2026-08-01T09:27:31Z`); unexpected non-alembic **0**. Risk **R-24** Closed. Semgrep gates unchanged. **CI GREEN not met** (CI-08) |
| CI-21 | Gitleaks JWT fixture leak ? `test_post_middleware.py` jwt.io sample Bearer token tripped secret-scan (`leaks found: 1`) | P1 | CLOSED | Closed DEC-041; commit `b03ffbf`; Security Scan run `30671556546` ? **entire workflow SUCCESS** (secret-scan Gitleaks PASS); fixture ? `Bearer test-fake-token-not-a-jwt`; no allowlist/global disable; **CI GREEN not met** (main CI workflow separate) |
| CI-22 | FastAPI / Starlette / Pydantic modernization program ? scoped cascade to clear starlette `pip-audit` (floor ?1.3.1) via FastAPI ~0.135+ and pydantic ?2.9 | P1 | CLOSED | **CLOSED DEC-109**. Phase 1 (DEC-081 @ `442af64`): fastapi **0.141.1** / starlette **1.3.1** / pydantic **2.13.4**. Field pip-audit `30688863161` @ `3084e5b`: **NO starlette** (`No known vulnerabilities found, 1 ignored` = ecdsa DEC-090). Unit RBAC override field-green same run. ecdsa residual ? DEC-057 (not CI-22). **whole-pipeline CI GREEN not met** (CI-08) |

## Progress
- **Tip CI Stages 1–5 SUCCESS (2026-08-02, Execution Orchestrator):** Tip `37c6826` CI [30727445665](https://github.com/ragheeda-boop/SalesOS/actions/runs/30727445665) Stages 1–5 same-run **SUCCESS** (Stage 6 quarantined SKIPPED DEC-150 B; Stage 7 SKIPPED → standalone `e2e-stage7.yml`). Operating State **PHASE 1 PARALLEL EXECUTION ACTIVE**. Checklist **54/54**. **No Production GO / GA GO / Stages 1–7 whole-pipeline invent.** DEC-085 untouched.
- **Stream B Frontend FE-S04-09/10/11 (2026-08-02, Frontend Lead):** Soft-delete honesty (not permanent); activity + provisioning list filters; hard-delete with confirm checkbox + `DELETE .../hard-delete`. `TenantList` untouched. Crumb [`PHASE1_FE_STREAM_B_CRUMB.md`](PHASE1_FE_STREAM_B_CRUMB.md). Validation **light**. **No Production GO.**
- **Stream B Frontend FE-S04-06/07/08 (2026-08-02, Frontend Lead):** Suspend parity via `POST /tenants/{id}/suspend`; provision toast surfaces slug/provisioning/plan_id/region/residency; E2E hooks `28-admin-tenants-owner-platform.spec.ts` (nav+create modal; no mutate). `TenantList` untouched. Crumb [`PHASE1_FE_STREAM_B_CRUMB.md`](PHASE1_FE_STREAM_B_CRUMB.md). Validation **light**. **No Production GO.**
- **Stream B FE Validation PASS (2026-08-02, Validation Lead):** Spot-check lands `a8fd06e` + `b6ea2ef` — **PASS · light validated**. `TenantList.tsx` untouched; Decision STUB + `feature_ai_copilot` default False affirmed; scope matches crumbs. Land `b6ea2ef` CI [30726962533](https://github.com/ragheeda-boop/SalesOS/actions/runs/30726962533) **failure** (Prettier + TS2353 `plan_id` create). Tip `9fa6830` CI [30727147746](https://github.com/ragheeda-boop/SalesOS/actions/runs/30727147746) **in_progress**; Deploy Prod + Security Scan success. Crumbs [`PHASE1_FE_VALIDATION_CRUMB.md`](PHASE1_FE_VALIDATION_CRUMB.md) · [`PHASE1_VALIDATION_STREAM_CRUMB.md`](PHASE1_VALIDATION_STREAM_CRUMB.md). **No Production GO / CI GREEN.**
- **Stream B Frontend B4 CONTRACT SYNC (2026-08-02, Frontend Lead):** Backend A2 ready @ `64b44e9`. FE types `AdminTenantCreate`/`Update` + required-nullable Owner Platform fields; create wires `admin_email`/`trial_ends_at`; contract + admin-queries fixtures synced. Companion [`PHASE1_FE_B4_CONTRACT_SYNC.md`](PHASE1_FE_B4_CONTRACT_SYNC.md). Plan B4 lint/tsc holdouts still skipped (needs approval). `TenantList` untouched. Jest **3 suites / 102 PASS**. Validation **light**. **No Production GO.**
- **Stream B Frontend B3 AFFIRMED / B5 WRITE-PATH LANDED (2026-08-02, Frontend Lead):** Follow-on after B2 `a8fd06e`. B3: `feature_ai_copilot` default False + Decision STUB affirmed. B5: Owner Platform edit+create on `/admin/tenants`; `TenantOwnerPlatformFields` write mode; create API payload fields; Jest **6/6 PASS**. Provisioning Studio seed remains script-first (STORY-04-02). `TenantList` untouched. Crumb [`PHASE1_FE_STREAM_B_CRUMB.md`](PHASE1_FE_STREAM_B_CRUMB.md). Validation **light**. **No Production GO.**
- **Stream B Frontend B1 COMPLETE / B2 LANDED (2026-08-02, Frontend Lead):** Phase 1 ACTIVE (`c19b098`). B1 inventory [`PHASE1_FE_TENANT_SURFACE_INVENTORY.md`](PHASE1_FE_TENANT_SURFACE_INVENTORY.md) · crumb [`PHASE1_FE_STREAM_B_CRUMB.md`](PHASE1_FE_STREAM_B_CRUMB.md). B2 read-path: `AdminTenantOwnerPlatformFields` types; `TenantOwnerPlatformFields` + provisioning column on `/admin/tenants` (**not** `TenantList.tsx`). A1 contract. Decision STUB; `feature_ai_copilot` default False. Validation **light**. **No Production GO.**
- **Phase 1 Stream A — STORY-04-01/04-02 Backend (2026-08-02, Backend Lead):** Tip `64b44e9` A2/A3 land + follow-up PUT null-clear align with FE B5 write-path `b6ea2ef`. A1 COMPLETE (keep `plan` + opaque `plan_id`). Alembic `f6b2e84c1a90`. `provision_workflow` + Studio seed + `scripts/provision_tenant.py`. Admin create/update/list/detail expose Owner Platform fields. DEC-085 untouched. **No Production GO.** Next: non-prod Alembic upgrade proof + D3 adversarial RLS.
- **PHASE 1 PARALLEL EXECUTION ACTIVE (2026-08-02, Execution Orchestrator / Watchdog):** Evidence tip `53a4aa7`. Checklist **54/54** confirmed (D1 PASS). **3.7 CLOSED** DEC-155 — Stage 7 [30726085801](https://github.com/ragheeda-boop/SalesOS/actions/runs/30726085801) @ `909230d`. Premature withdraw commit `a08d7c0` **reversed** (Watchdog: TRIGGER confirmed). Plan [`POST_PHASE0_PARALLEL_EXECUTION_PLAN.md`](POST_PHASE0_PARALLEL_EXECUTION_PLAN.md) **ACTIVE**. Operating State → **PHASE 1 PARALLEL EXECUTION ACTIVE**. Streams A/B/C/D — [`PHASE1_STREAM_LAUNCH_CRUMB.md`](PHASE1_STREAM_LAUNCH_CRUMB.md). **No Production GO / GA GO.** DEC-085 untouched.
- **TRIGGER_POST_PHASE0_PLAN / Phase 0 COMPLETE — post-54/54 parallel execution STARTED (2026-08-02):** Score **54/54**. **3.7 CLOSED** DEC-155 — Stage 7 [30726085801](https://github.com/ragheeda-boop/SalesOS/actions/runs/30726085801) @ `909230d` SUCCESS (real postgres/redis + smoke-auth-ui). Plan [`POST_PHASE0_PARALLEL_EXECUTION_PLAN.md`](POST_PHASE0_PARALLEL_EXECUTION_PLAN.md) **ARMED → ACTIVE**. Spawning Backend / FE / DevOps / Validation streams (calendar Sprint 04 / Owner Platform). **No Production GO / GA GO.** DEC-085 untouched.
- **DEC-155 / criterion 3.7 CLOSED (2026-08-02):** **VERIFIED/CLOSED**. Companion [`decisions/DEC-155-CRITERION-3-7-STAGE7-E2E-CLOSED.md`](decisions/DEC-155-CRITERION-3-7-STAGE7-E2E-CLOSED.md). Phase 0 **53/54 → 54/54**. Hard OPEN ⬜ → **0**.
- **DEC-154 / 2.3 scoreboard Complete (2026-08-02):** Path (b) — CONDITIONAL retained; Open cell disposed. Companion [`decisions/DEC-154-CRITERION-2-3-CONDITIONAL-SCOREBOARD-COMPLETE.md`](decisions/DEC-154-CRITERION-2-3-CONDITIONAL-SCOREBOARD-COMPLETE.md). **51/54 → 53/54 NO-GO**. Hard OPEN **3.7** only. **No Production GO / 54/54 invent.**
- **WATCHDOG ACTIVE — continuous monitor (2026-08-02):** Chasing tip Stage 7 @ `909230d` (tenant GUC + JWKS prewarm). Score **53/54**. Plan ARMED. No post-Phase0 until **54/54**.
- **DEC-154 / criterion 2.3 scoreboard Complete (2026-08-02, Validation / RLS lead):** **CLOSED CONDITIONAL stands**; special-case Open cell **disposed**. Path (b) per post-54 plan §3. S04-04 → **CLOSED CONDITIONAL**. Phase 0 **51/54 → 53/54 NO-GO**. Hard OPEN ⬜ = **3.7** only. Companion [`decisions/DEC-154-CRITERION-2-3-CONDITIONAL-SCOREBOARD-COMPLETE.md`](decisions/DEC-154-CRITERION-2-3-CONDITIONAL-SCOREBOARD-COMPLETE.md). **No unconditional 2.3 / Production GO / 54/54 invent. DEC-085 untouched. DEC-151 freeze held.**
- **ARMED FOR POST-54/54 PARALLEL SPRINT EXECUTION (2026-08-02, Execution Orchestrator):** Operating State → `CONTINUOUS AUTONOMOUS MODE — FREEZE COMPLIANT` + **ARMED FOR POST-54/54 PARALLEL SPRINT EXECUTION**. Tip `8600f68`. Phase 0 **51/54 NO-GO** (not 54/54). Hard OPEN ⬜ = **3.7** only. Planner plan [`POST_PHASE0_PARALLEL_EXECUTION_PLAN.md`](POST_PHASE0_PARALLEL_EXECUTION_PLAN.md) coordinated. **Orchestrator trigger protocol:** true checklist **54/54** → spawn Backend / FE / DevOps / Validation streams **without human wait**; until then Stream B **3.7** chase continues. **No fake 54/54 / Production GO / Phase 0 COMPLETE. DEC-085 untouched. DEC-151 freeze held.**
- **ARMED — execute on 54/54 (2026-08-02, Sprint Program Planner):** Post–Phase 0 parallel plan landed [`POST_PHASE0_PARALLEL_EXECUTION_PLAN.md`](POST_PHASE0_PARALLEL_EXECUTION_PLAN.md) — **ARMED only**. Sprint map + stop point (**51/54 NO-GO**, hard OPEN **3.7**, board **26/26**, S04-04 REOPENED / **2.3** CONDITIONAL residual) + gap math + first 48–72h workstreams (BE/FE/DevOps/Validation/Docs) ready to launch the instant tip checklist hits **54/54**. **Do not start Phase 1 / calendar Sprint 04 implementation yet. Do not invent 3.7 CLOSE / 54/54 / Production GO.** DEC-151 freeze held; Continuous Autonomous continues Stream B (Stage 7) until trigger. Operating State wording superseded by Orchestrator ARMED crumb above (score unchanged).
- **WATCHDOG ACTIVE — continuous monitor (2026-08-02):** Sole hard OPEN **3.7** Stage 7 E2E. Standalone workflow .github/workflows/e2e-stage7.yml landed (own concurrency; docs pushes must not cancel E2E). Hardened APP_DATABASE_URL_OVERRIDE + health/live + register timeout. Score **51/54 NO-GO**. Sprint **26/26**. No Production GO / Phase 0 COMPLETE until 54/54.
- **CONTINUOUS AUTONOMOUS — Stage 7 chase (2026-08-02, Execution Orchestrator):** Post DEC-153 tip `8ff782f` (+ Stage 7 harden `d973cba`; tip now `8600f68`). Phase 0 **51/54 NO-GO**. Sprint stories **26/26** (S04-04 REOPENED residual). Hard OPEN ⬜ = **3.7** only. Operating State → continuous + **ARMED** post-54/54 (see Orchestrator crumb above). Primary chase: **Stream B — Stage 7 E2E**. Streams A/C CLOSED (DEC-152 / DEC-153). Stream D QUEUED. **Do not invent 3.7 CLOSE / Production GO / Phase 0 COMPLETE / Stages 1–7 green. DEC-085 untouched. DEC-151 freeze held.**
- **EOS 4.1 / 4.8 CLOSED — ARB PASS (2026-08-02, DEC-153 / independent ARB lane):** **VERIFIED/CLOSED**. Independent re-audit [`.engineering/34_EOS_REAUDIT_2026-08-02.md`](../../.engineering/34_EOS_REAUDIT_2026-08-02.md): **4.1 PASS** (B1–B7 resolved) · **4.8 PASS** (CRITICAL **0**). Measurement corroboration @ tip `d973cba` (first land `74f698b`; Alembic head `a4f7c29e1b80`; modules/domains/runtime/pages/migrations/tests match; lock free; Measured 33 / Repository Verified 0). Pack [`ARB_PHASE0_4_1_4_8_EVIDENCE_PACK.md`](ARB_PHASE0_4_1_4_8_EVIDENCE_PACK.md). Non-blocking: pin lag 35 commits / tracked 3252→3288 / bootstrap B5–B6 label swap. Historical FAIL `32` intact. Phase 0 **49/54 → 51/54 NO-GO**. Hard OPEN ⬜ → **3.7** only. Companion [`decisions/DEC-153-CRITERION-4-1-4-8-ARB-REAUDIT-PASS.md`](decisions/DEC-153-CRITERION-4-1-4-8-ARB-REAUDIT-PASS.md). **No Production GO / Phase 0 COMPLETE / Stages 1–7 green. DEC-085 untouched. DEC-151 freeze held.**
- **CONTINUOUS AUTONOMOUS MODE — FREEZE COMPLIANT (2026-08-02, Execution Orchestrator):** User escalation — continuous loop under DEC-151 until hard OPEN closed or ARB/human-blocked. Operating State → `CONTINUOUS AUTONOMOUS MODE — FREEZE COMPLIANT`. Score superseded to **51/54** by DEC-153. Sprint stories **26/26** (S04-04 REOPENED residual). Streams: **(A) 3.9 CLOSED CONDITIONAL** DEC-152; **(B) 3.7 ACTIVE — chasing Stage 7** DevOps/Backend; **(C) 4.1/4.8 CLOSED** DEC-153 ARB PASS; **(D) QUEUED** freeze-compliant backlog. Loop: identify next OPEN → assign/fix → land board/DAG/checklist → push. **Never invent 3.7 CLOSE / Production GO / Phase 0 COMPLETE unless 54/54 with evidence.**
- **DEC-152 / criterion 3.9 CLOSED CONDITIONAL (2026-08-02, CI/DevOps lead):** **VERIFIED/CLOSED CONDITIONAL**. Tip `5fafbe9` (includes xdist fix `3547177`): CI Stages 1–5 same-run **SUCCESS** [30724762973](https://github.com/ragheeda-boop/SalesOS/actions/runs/30724762973); Stage 4 Integration **SUCCESS**; Stage 6 **SKIPPED** (quarantined DEC-150 B); Stage 7 **FAILURE** → **3.7** remains OPEN. Same-tip Deploy Production **SUCCESS** [30724762967](https://github.com/ragheeda-boop/SalesOS/actions/runs/30724762967); Security Scan **SUCCESS** [30724762982](https://github.com/ragheeda-boop/SalesOS/actions/runs/30724762982). Residuals: overall CI red on Stage 7; FE Git-primary; staging deferred. Phase 0 **48/54 → 49/54 NO-GO**. Hard OPEN ⬜ → **3.7**, **4.1**, **4.8**. Companion [`decisions/DEC-152-CRITERION-3-9-CI-GREEN-TOPOLOGY-FIELD-VERIFY.md`](decisions/DEC-152-CRITERION-3-9-CI-GREEN-TOPOLOGY-FIELD-VERIFY.md). **No Production GO / Stages 1–7 green / ARB invent. DEC-085 untouched.**
- **EOS 4.1 / 4.8 READY FOR ARB (2026-08-02, program/ARB preparation scribe):** **SUPERSEDED CLOSED** by DEC-153 crumb above. Pack remained the briefing input.
- **PARALLEL EXECUTION — FREEZE COMPLIANT (2026-08-02, Execution Orchestrator):** Superseded by CONTINUOUS AUTONOMOUS MODE crumb above. Streams A/B/C; C closed via DEC-153. Score **51/54**.
- **DEC-151 Phase 0 Governance Freeze — GOVERNANCE FROZEN (2026-08-02, program/governance scribe):** **Accepted.** Freeze org redesign, deployment-topology DECs superseding DEC-149, and criterion rewrites. Allowed: field evidence for hard OPEN **3.7 / 3.9 / 4.1 / 4.8** + bugfixes / evidence crumbs that do not change architecture or supersede DEC-149/150. Forbidden without ARB reverse: new deploy topology, reopening GHCR as Phase 0 gate, inventing ARB PASS. Aligns Architecture = FROZEN + AI Runtime = DEFERRED. Phase 0 pin **unchanged 48/54 NO-GO** (superseded to **49/54** by DEC-152). Companion [`decisions/DEC-151-PHASE-0-GOVERNANCE-FREEZE.md`](decisions/DEC-151-PHASE-0-GOVERNANCE-FREEZE.md). Validation: **governance freeze documentation only**. **No Phase 0 COMPLETE / Production GO / CI GREEN / 4.1/4.8 invent.**
- **DEC-150 Option B Accepted — CI-08 GOVERNANCE COMPLETED (2026-08-02, program/governance + devops scribe):** **Accepted — Option B (ARB Approved).** Stage 6 GHCR publish **no longer** Phase 0 required after DEC-149. **3.6 / 3.10 CLOSED — SUPERSEDED** (governance; not field GHCR green). **3.9** redefined (Stages 1–5 + Railway/Vercel; no GHCR) — remains OPEN. **3.7** decoupled from Stage 6 — remains OPEN (no E2E services). CI-08 → **GOVERNANCE COMPLETED**. `ci.yml` Stage 6 quarantined (`if: false`); Stage 7 `needs` decoupled. Phase 0 **46/54 → 48/54 NO-GO**. CI/CD Complete **7 → 9** / Blocked **1 → 0**. Companion [`decisions/DEC-150-STAGE-6-GHCR-POST-DEC-149.md`](decisions/DEC-150-STAGE-6-GHCR-POST-DEC-149.md). Validation: **governance implementation only**; runtime pending. **No Production GO / CI GREEN / field GHCR green / 4.1/4.8 invent. DEC-085 untouched.**
- **DEC-150 Stage 6 GHCR necessity Proposed (2026-08-02, governance scribe):** **Superseded by Accepted Option B crumb above.** Was Proposed pending ARB; ARB selected B.
- **CI-09 / criterion 3.11 CLOSED CONDITIONAL (2026-08-02, DEC-149a):** **VERIFIED/CLOSED CONDITIONAL**. Arch prior PASS (DEC-149 + single-env) + Validation PASS ([Validate CI-09](8d5bad88-a487-471b-80b9-e8eb2c56a8cf)) @ Deploy [30723120473](https://github.com/ragheeda-boop/SalesOS/actions/runs/30723120473) / tip `c3507ed`: Railway up ✓; Health Gate HTTP 200 ✓; Vercel FE Git-primary ✓; §6 production secret names present (repo). Residuals: *FE Git-primary (not Vercel CLI prod); staging deferred (single-env); no VPS*. Canonical deploy evidence for post–DEC-150 B **3.6** supersession. Does **not** auto-close **3.9** / **3.7**. DEC-085 untouched. Phase 0 **45/54 → 46/54** (superseded to **48/54** by DEC-150 B). Companion [`decisions/DEC-149-CANONICAL-DEPLOY-RAILWAY-VERCEL.md`](decisions/DEC-149-CANONICAL-DEPLOY-RAILWAY-VERCEL.md). **Not unconditional CLOSED. Production GO not claimed. CI GREEN not met. Phase 0 NO-GO.**
- **Validation PASS (2026-08-02, Validation Lead):** Deploy Production [30723120473](https://github.com/ragheeda-boop/SalesOS/actions/runs/30723120473) @ tip c3507ed SUCCESS — Pre-deploy ✓; Railway up ✓; Backend Health Gate ✓ (Attempt 1 HTTP 200); Frontend Vercel job ✓ (Git primary path; optional CLI not required). Repo secret names present: RAILWAY_TOKEN, RAILWAY_PROJECT_ID, RAILWAY_SERVICE_ID, RAILWAY_ENVIRONMENT_ID, RAILWAY_HEALTH_URL. Env production inventory still empty (repo-scoped secrets used). Staging deferred. Superseded by DEC-149a CLOSED CONDITIONAL crumb above. No Production GO / CI GREEN / ARB invent.
- **DEC-149 single-env amend (2026-08-02, devops/ci-worker):** **READY_FOR_REVIEW** — User ruling: one environment only (production Railway). `deploy-staging.yml` no longer auto-runs on push; `workflow_dispatch` + `CONFIRM-STAGING-DEPLOY` + soft-skip when `RAILWAY_STAGING_*` absent. DEC-149 §6 splits production (required) vs staging (deferred). `deploy.yml` production-only. CI-09 / **3.11** remains **BLOCKED** (production secrets) — **not CLOSED**. Companion DEC-149 §1a/§6. **No Production GO / CI GREEN / ARB invent.**
- **DEC-149 Validation field-verify (2026-08-02, Validation Lead):** **VALIDATION_CONDITIONAL** — tip 85d91e8 workflow land aligns DEC-149 (Railway BE + Vercel FE; K8s quarantined; no VPS_*). Deploy Prod 30722044834 / Staging 30722044830 **FAILURE** at secret-name gate (expected). Secrets/vars: **0** names present (repo + production/staging). CI-09 / **3.11** → **BLOCKED** (ops provision DEC-149 §6) — **not CLOSED**. Superseded for staging-requirement by single-env amend crumb above. No Production GO / CI GREEN / ARB invent / Orchestrator VERIFIED.
- **DEC-149 workflow migration (2026-08-02, devops/ci-worker):** **READY_FOR_REVIEW** — Active path Railway (`railway up` cwd `salesos/`) + Vercel (Git primary / optional CLI). Removed `appleboy/ssh-action` + `VPS_*` / `STAGING_*` from `deploy.yml` / `deploy-staging.yml`. Quarantined K8s `deploy-production.yml` (tag push off; ack gate). CI Stage 6 GHCR **unchanged** (CI-08 orthogonal). Secret **names** in DEC-149 §6 — values out-of-band. CI-09 / **3.11** **not CLOSED**. Companion [`decisions/DEC-149-CANONICAL-DEPLOY-RAILWAY-VERCEL.md`](decisions/DEC-149-CANONICAL-DEPLOY-RAILWAY-VERCEL.md) §6. Phase 0 **45/54 NO-GO**. **No Production GO / CI GREEN / ARB invent.**
- **DEC-149 canonical deploy Railway+Vercel (2026-08-02, governance scribe):** **Accepted** — Backend→Railway, Frontend→Vercel. CI-09 / **3.11** was **BLOCKED BY GOVERNANCE** until workflow assignment; superseded by READY_FOR_REVIEW crumb above after migration. Companion [`decisions/DEC-149-CANONICAL-DEPLOY-RAILWAY-VERCEL.md`](decisions/DEC-149-CANONICAL-DEPLOY-RAILWAY-VERCEL.md).
- **CI-08 GHCR field-verify (2026-08-02, devops/ci-worker):** Package↔repo link **confirmed** via `gh api user/packages` (`repository=ragheeda-boop/SalesOS` for backend+frontend). Re-ran Deploy Staging [`30721601875`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30721601875) @ tip `7f1482e` — Build&Push BE+FE still **403 Forbidden** (login succeeded). CI Stage 6 skipped (Stage 2 mypy). **Do not CLOSE** CI-08 / 3.10 / 3.6. CI-09 / VPS_* not requested (DEC-149). DEC-085 untouched. Validation: **build validated** (negative — push 403).
- **backend-types mypy residual (2026-08-02):** Stage 2 FAILURE @ [`30720732268`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30720732268) — `roles_permissions.py` 4× `arg-type` (`description`/`group` `str | None` → response `str`). Minimal coalesce at call sites (`or ""` / `or "general"`); DEC-085 untouched. Tip Stages 1–5 same-run still PENDING field-verify post-push. **No Production GO / CI GREEN / ARB invent. CI-08 ops-blocked; CI-09 governance-blocked (DEC-149).**
- **Phase 1 Ops push field (2026-08-02, devops/ci-worker):** Pushed tip `d1dcce4` (`c842245..d1dcce4`). **Actions tip runs:** CI [`30720732268`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30720732268) **FAILURE**; Security Scan [`30720732260`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30720732260) **SUCCESS**; Deploy Staging [`30720732248`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30720732248) **FAILURE** (GHCR push 403 both images); Deploy Production [`30720732236`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30720732236) **FAILURE**; Docker Smoke [`30720732238`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30720732238) (observe). **CI stages @ tip:** Stage 1 Backend Lint **SUCCESS** (DEC-148 path); Stage 2 Backend Types **FAILURE** (mypy `roles_permissions.py` 4x arg-type) -> Stage 3 BE / 4 / 6 / 7 **SKIPPED**. **CI-08 partial ops:** repo Actions `default_workflow_permissions` set **read->write** via API (verified); packages `salesos/backend` + `salesos/frontend` still `repository:null` — link API **404**; field Deploy Staging still **403 Forbidden** on GHCR push -> **CI-08 remains BLOCKED** (human package-repo Actions write link required; do not CLOSE). **CI-09:** repo + env secrets **empty** — missing `VPS_HOST` / `VPS_USER` / `VPS_SSH_KEY` (rollback log: `missing server host`). **3.8 tip field-verify:** Stages 1-5 same-run **not** met (Stage 2 red); CONDITIONAL residual stands. No Production GO / full CI GREEN claim. DEC-085 untouched.
- **Phase 0 BLOCKED inventory — Cursor no-closeable remainder (2026-08-02, backend/api-worker):** **BLOCKED** (docs inventory only). Phase 0 remains **45/54**. Hard OPEN ⬜ **7** + scoreboard Blocked **2** / push residuals = **remaining 9** with owners — **not** CLOSED. Inventory: **3.6** ops CI-08 · **3.7** Stage-6-dep · **3.9** ops CI-08 · **3.10** ops/human CI-08 · **3.11** governance CI-09 (DEC-149) · **4.1** ARB · **4.8** ARB · **3.8** tip Stages 1–5 PENDING push `14fce5f` · **3.5/1.5** Security Scan pip-audit PENDING push `fa266b5`. Skipped inventing EOS ARB; skipped ops CI-08; CI-09 reframed DEC-149 (not closed); optional contract tests / Jest 30 parked (do not close any of the 9). DEC-085 untouched. Checklist crumb: [`PHASE_0_EXIT_CHECKLIST.md`](PHASE_0_EXIT_CHECKLIST.md) §Remaining 9. **No fake CLOSE. Production GO not claimed. CI GREEN not met.**
- **CI/CD criterion 3.8 CI GREEN (code path) (2026-08-02, DEC-148a):** **VERIFIED/CLOSED CONDITIONAL**. Arch PASS_CONDITIONAL + Validation PASS_CONDITIONAL @ `14fce5f` / DEC-148. Local ruff 0.4.10 `check`+`format --check` exit 0. Last push `c842245` run [`30704321096`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30704321096): Stage 1 **FAILURE** (6× E501) → Stages 3 BE/4 **SKIPPED**; Stage 2/5 SUCCESS. Residual: *tip Stages 1–5 same-run field-verify PENDING until tip containing `14fce5f` is pushed* (Stage 3/4 may still fail when unblocked). Historical corroboration Stages 1–5 SUCCESS @ `7ba137b` / [`30689682988`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30689682988) (not tip). Does **not** close **3.6–3.11** ops / **3.9** full CI GREEN. DEC-085 untouched. Phase 0 **44/54 → 45/54**. CI/CD Complete **5 → 6** / Open **4 → 3**. Residuals: **3.7** · ops CI-08/CI-09 · EOS **4.1/4.8** ARB. Companion [`decisions/DEC-148-CRITERION-3-8-CI-GREEN-CODE-PATH.md`](decisions/DEC-148-CRITERION-3-8-CI-GREEN-CODE-PATH.md). **Not unconditional CLOSED. Production GO not claimed. CI GREEN not met.**
- **CI/CD criterion 3.5 Stage 5 Security Scan (2026-08-01, DEC-147a):** **VERIFIED/CLOSED CONDITIONAL**. Arch PASS_CONDITIONAL + Validation PASS_CONDITIONAL @ `5d558af` / pin `a6488f2` / DEC-147. CI Stage 5 + Security Scan SUCCESS @ pushed tip `c842245` (runs [`30704321096`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30704321096) / [`30704321107`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30704321107)); pip-audit `1 ignored` (ecdsa DEC-090); Semgrep residual **11** alembic (DEC-105). Residual: *post-align Security Scan pip-audit PENDING until tip containing `fa266b5` is pushed*. Does **not** auto-close **3.8**. DEC-085 untouched. Phase 0 **43/54 → 44/54** (superseded to **45/54** by DEC-148a). CI/CD Complete **4 → 5** / Open **5 → 4** (superseded to Complete **6** / Open **3**). Residuals: CI **3.6/3.7/3.9** · ops CI-08/CI-09 · EOS **4.1/4.8** ARB. Companion [`decisions/DEC-147-CRITERION-3-5-STAGE5-SECURITY-SCAN.md`](decisions/DEC-147-CRITERION-3-5-STAGE5-SECURITY-SCAN.md). **Not unconditional CLOSED. Production GO not claimed. CI GREEN not met.**
- **ADR-036 Applied criterion 9.3 `.ai/` runtime deferred (2026-08-01, DEC-146a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `922528f` / tip pin `1f99628` / DEC-146. Org baseline (ARB-003 / DEC-145) ≠ live Agent OS; triggers pinned in ADR-036 §`.ai/` Runtime deferral + `.ai/README`; `runtime-spec.yaml` remains `status: SPECIFICATION`. DEC-085 untouched. Phase 0 **42/54 → 43/54** (superseded to **45/54** by DEC-148a). ADR-036 Applied Complete **3 → 4** / Open **1 → 0** (cluster **COMPLETE 4/4**). Residuals: EOS **4.1/4.8** ARB · CI **3.x** + CI-08/CI-09 ops. Companion [`decisions/DEC-146-CRITERION-9-3-AI-RUNTIME-DEFERRED.md`](decisions/DEC-146-CRITERION-9-3-AI-RUNTIME-DEFERRED.md). **Production GO not claimed. CI GREEN not met.**
- **Engineering Stability criterion 8.2 agent coordination (2026-08-01, DEC-145a):** **VERIFIED/CLOSED CONDITIONAL**. Arch PASS_CONDITIONAL + Validation PASS_CONDITIONAL @ `5bc0bf2` / DEC-145. Caps pinned (`max_parallel_workers=8`, `max_agents_total=12`, permanent roles **4**, DEC-107 min/prefer READY **2/3**); namespaced workers + conflict/lock rules; `.ai/` org baseline committed (ARB-003). Light exercise: DEC-107 swarm + namespaced workers in `21`. Residual: *at-scale live soak at max_parallel_workers=8 not field-proven*. DEC-085 untouched. Phase 0 **41/54 → 42/54** (superseded to **43/54** by DEC-146a). Eng Stability Complete **3 → 4** / Open **1 → 0** (cluster **COMPLETE 4/4**). Residual EOS **4.1/4.8** ARB. Companion [`decisions/DEC-145-CRITERION-8-2-AGENT-COORDINATION.md`](decisions/DEC-145-CRITERION-8-2-AGENT-COORDINATION.md). **Not unconditional CLOSED. Production GO not claimed. CI GREEN not met.**
- **Engineering Stability criterion 8.3 arch rules in CI (2026-08-01, DEC-144a):** **VERIFIED/CLOSED CONDITIONAL**. Arch CONDITIONAL + Validation PASS_CONDITIONAL @ `868a98c` / DEC-144. Independent CI job `test-architecture` wired critical; existing `arch-compliance.ps1` retained critical. Docker **36 passed**; local ps1 **95.8%**; gh Arch Compliance success @ run `30704321096`. Residual: *tip `test-architecture` SUCCESS PENDING until tip containing `868a98c` is pushed*. DEC-085 untouched. Phase 0 **40/54 → 41/54** (superseded to **43/54** by DEC-146a). Eng Stability Complete **2 → 3** / Open **2 → 1** (superseded to Complete **4** / Open **0**). Residual EOS **4.1/4.8** ARB. Companion [`decisions/DEC-144-CRITERION-8-3-ARCH-RULES-IN-CI.md`](decisions/DEC-144-CRITERION-8-3-ARCH-RULES-IN-CI.md). **Not unconditional CLOSED. Production GO not claimed. CI GREEN not met.**
- **Engineering Stability criterion 8.1 engineering-os clean (2026-08-01, DEC-143a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light: pin `b82b9fb`, clean tree) @ `89502ef` / DEC-143. Submodule working tree clean; discarded malformed unreviewed `kernel/capability-registry.yaml` append (+365 outside YAML fence); parent gitlink unchanged; no submodule push. DEC-085 untouched. Phase 0 **39/54 → 40/54** (superseded to **43/54** by DEC-146a). Eng Stability Complete **1 → 2** / Open **3 → 2** (superseded to Complete **4** / Open **0**). Residuals: EOS **4.1/4.8** ARB. Companion [`decisions/DEC-143-CRITERION-8-1-ENGINEERING-OS-CLEAN.md`](decisions/DEC-143-CRITERION-8-1-ENGINEERING-OS-CLEAN.md). **Production GO not claimed. CI GREEN not met.**
- **EOS Audit criteria 4.2 / 4.4 / 4.7 fingerprint re-pin (2026-08-01, DEC-142a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `637d051` / DEC-142. Tip pin `9fa8e9f`; Alembic head `a4f7c29e1b80`; EvidenceLevel **Measured**; Revalidation **Active**; script `.engineering/measure_fingerprint.py`; DEC-085 untouched. Phase 0 **36/54 → 39/54** (superseded to **43/54** by DEC-146a). EOS Audit Complete **3 → 6** / Open **5 → 2**. Residuals: **4.1/4.8** ARB · Eng Stability **COMPLETE 4/4** (DEC-145a) · ADR-036 Applied **COMPLETE 4/4** (DEC-146a). Companion [`decisions/DEC-142-CRITERION-4-2-4-4-4-7-FINGERPRINT-REPIN.md`](decisions/DEC-142-CRITERION-4-2-4-4-4-7-FINGERPRINT-REPIN.md). **Production GO not claimed. CI GREEN not met.**
- **ADR-036 Applied criterion 9.2 program↔engineering bridges (2026-08-01, DEC-141a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `7b618da` / DEC-141. Bridges `docs/program/ENGINEERING_LAYER_BRIDGE.md` ↔ `.engineering/33_PROGRAM_LAYER_BRIDGE.md` (pointers only; no catalog/sprint duplication). DEC-085 untouched. Phase 0 **35/54 → 36/54** (superseded to **39/54** by DEC-142a). ADR-036 Applied Complete **2 → 3** / Open **2 → 1**. Residuals: EOS **4.1/4.8** · Eng Stability **8.1–8.3** (**4.2/4.4/4.7 CLOSED** DEC-142a). Companion [`decisions/DEC-141-CRITERION-9-2-PROGRAM-ENGINEERING-BRIDGES.md`](decisions/DEC-141-CRITERION-9-2-PROGRAM-ENGINEERING-BRIDGES.md). **Production GO not claimed. CI GREEN not met.**
- **EOS Audit criterion 4.5 `.engineering/` committed (2026-08-01, DEC-140a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light: 33 tracked, 0 untracked; DEC-085 untouched) @ `5b2e4c2` / DEC-140. Broader EOS v3.1 tree committed; fingerprint pin residual cleared by DEC-142a. Phase 0 **34/54 → 35/54** (superseded to **39/54** by DEC-142a). EOS Audit Complete **2 → 3** / Open **6 → 5** (superseded to Complete **6** / Open **2**). Residuals: **4.1/4.8** ARB · Eng Stability **8.1–8.3** (**9.2 CLOSED** DEC-141a; **4.2/4.4/4.7 CLOSED** DEC-142a). Companion [`decisions/DEC-140-CRITERION-4-5-ENGINEERING-TREE-COMMIT.md`](decisions/DEC-140-CRITERION-4-5-ENGINEERING-TREE-COMMIT.md). **Production GO not claimed. CI GREEN not met.**
- **ADR Drift criterion 6.5 ADR-036 multi-index (2026-08-01, DEC-139a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `aaeaff3` / DEC-139. Body `docs/adr/0036-engineering-organization-layer-separation.md` (Status **Accepted** matches file header + criterion 9.1; not invented); `docs/adr/index.md` Active ADRs row; `.engineering/27_ADR_INDEX.md` master row + conflict #13 RESOLVED; engineering-os has no separate ADR index (N/A). DEC-085 untouched. Phase 0 **33/54 → 34/54** (superseded to **39/54** by DEC-142a). ADR Drift Complete **4 → 5** / Open **1 → 0** (cluster **COMPLETE 5/5**). Residual note superseded: **4.5 CLOSED** (DEC-140a); **9.2 CLOSED** (DEC-141a); **4.2/4.4/4.7 CLOSED** (DEC-142a). Companion [`decisions/DEC-139-CRITERION-6-5-ADR-036-MULTI-INDEX.md`](decisions/DEC-139-CRITERION-6-5-ADR-036-MULTI-INDEX.md). **Production GO not claimed. CI GREEN not met.**
- **ADR Drift criterion 6.4 ADR-032/0032 naming (2026-08-01, DEC-138a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `8a3c92e` / DEC-138. Naming bridge `docs/adr/0032-widget-sdk-reconciliation.md` (canonical ID **ADR-032**; alias ADR-0032); `docs/adr/index.md` File → bridge + body path; Status → **📝 Proposed** (matches body header; no invented Accepted); date → `2026-07-17`; submodule `ADR-0032-*` retained as documented alias (no rename). DEC-085 untouched. Phase 0 **32/54 → 33/54** (superseded to **34/54** by DEC-139a). ADR Drift Complete **3 → 4** / Open **2 → 1** (superseded to Complete **5** / Open **0**). Residual note superseded: ADR Drift COMPLETE (DEC-139a); broader EOS tree re-pin (**4.5**). Companion [`decisions/DEC-138-CRITERION-6-4-ADR-032-NAMING.md`](decisions/DEC-138-CRITERION-6-4-ADR-032-NAMING.md). **Production GO not claimed. CI GREEN not met.**
- **ADR Drift criterion 6.3 ADR-033/034 status (2026-08-01, DEC-137a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light: index Status == file header Proposed for 033/034; no Accepted-without-evidence) @ `bcd7aa6` / DEC-137. Index Status for ADR-033/034 → **📝 Proposed** (matches file headers); dates → `2026-07-17`; no invented Accepted. DEC-085 untouched. Phase 0 **31/54 → 32/54** (superseded to **34/54** by DEC-139a). ADR Drift Complete **2 → 3** / Open **3 → 2** (superseded to Complete **5** / Open **0**). Residual note superseded: ADR Drift COMPLETE (DEC-139a); broader EOS (**4.5**). Companion [`decisions/DEC-137-CRITERION-6-3-ADR-033-034-STATUS.md`](decisions/DEC-137-CRITERION-6-3-ADR-033-034-STATUS.md). **Production GO not claimed. CI GREEN not met.**
- **ADR Drift criterion 6.2 ADR-029 phantom (2026-08-01, DEC-136a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light: path-exists + Status Not Issued + no Accepted-without-file) @ `a1ce473` / DEC-136. Disposition **Not Issued** @ `docs/adr/0029-number-never-issued.md`; `docs/adr/index.md` row registered (not Accepted); numbering gap documented (no invented architecture; no renumber). DEC-085 untouched. Phase 0 **30/54 → 31/54** (superseded to **34/54** by DEC-139a). ADR Drift Complete **1 → 2** / Open **4 → 3** (superseded to Complete **5** / Open **0**). Residual note superseded: ADR Drift COMPLETE (DEC-139a); broader EOS (**4.5**). Companion [`decisions/DEC-136-CRITERION-6-2-ADR-029-PHANTOM.md`](decisions/DEC-136-CRITERION-6-2-ADR-029-PHANTOM.md). **Production GO not claimed. CI GREEN not met.**
- **ADR Drift criterion 6.1 ADR-025..028 index paths (2026-08-01, DEC-135a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light: path-exists + Status Accepted) @ `4997ae4` / DEC-135. Files @ `salesos/backend/docs/adr/0025-entity-resolution.md` / `0026-hybrid-search.md` / `0027-feature-store.md` / `0028-knowledge-graph-integration.md` (each `**Status:** Accepted`); `docs/adr/index.md` File column + location registered. DEC-085 untouched. Phase 0 **29/54 → 30/54** (superseded to **34/54** by DEC-139a). ADR Drift Complete **0 → 1** / Open **5 → 4** (superseded to Complete **5** / Open **0**). Residual note superseded: ADR Drift COMPLETE (DEC-139a); broader EOS (**4.5**). Companion [`decisions/DEC-135-CRITERION-6-1-ADR-025-028-INDEX.md`](decisions/DEC-135-CRITERION-6-1-ADR-025-028-INDEX.md). **Production GO not claimed. CI GREEN not met.**
- **Capability Drift criterion 5.3 SoT-oriented validate (2026-08-01, DEC-134a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `6a98999` / DEC-134. SoT-oriented gate: exit **0** = joined secondaries subset-of decorator SoT via join map (NOT 4-way identity equality). Host+Docker default exit **0**; `--legacy-equality` exit **2** (diagnostic). DEC-085 untouched. Phase 0 **28/54 → 29/54** (superseded to **34/54** by DEC-139a). Capability Drift Complete **3 → 4** / Open **1 → 0** (cluster COMPLETE 4/4). Non-blocking INFO: SDK/YAML extras + unmapped CAPs + CAP-037 refine. Companion [`decisions/DEC-134-CRITERION-5-3-SOT-ORIENTED-VALIDATE.md`](decisions/DEC-134-CRITERION-5-3-SOT-ORIENTED-VALIDATE.md). **Production GO not claimed. CI GREEN not met.**
- **Capability Drift criterion 5.2 CAP-###→kebab join map (2026-08-01, DEC-133a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `81b593f` / DEC-133. Join map `runtime/capability_framework/cap_to_kebab_join.yaml` (10 direct / 30 unmapped / 3 decorator-only); `--join-map-only` exit 0; DEC-085 untouched. Residual note superseded: **5.3 CLOSED** (DEC-134a); cluster COMPLETE. Non-blocking: CAP-037→`capability-framework` semantic-join refine. Phase 0 **27/54 → 28/54** (superseded to **34/54** by DEC-139a). Capability Drift Complete **2 → 3** / Open **2 → 1** (superseded to Complete **4** / Open **0**). Companion [`decisions/DEC-133-CRITERION-5-2-CAP-TO-KEBAB-JOIN.md`](decisions/DEC-133-CRITERION-5-2-CAP-TO-KEBAB-JOIN.md). **Production GO not claimed. CI GREEN not met.**
- **Capability Drift criterion 5.1 single SoT (2026-08-01, DEC-132a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (light) @ `8e105fe` / DEC-132. Decorator framework `runtime/capability_framework` designated **canonical runtime SoT** (kebab IDs); pins `CAPABILITY_REGISTRY_SOT*`; catalog/SDK/YAML = secondary. DEC-085 untouched. Residual note superseded: **5.2 CLOSED** (DEC-133a); **5.3 CLOSED** (DEC-134a); cluster COMPLETE. Phase 0 **26/54 → 27/54** (superseded to **34/54** by DEC-139a). Capability Drift Complete **1 → 2** / Open **3 → 2** (superseded to Complete **4** / Open **0**). Companion [`decisions/DEC-132-CRITERION-5-1-CAPABILITY-SOT.md`](decisions/DEC-132-CRITERION-5-1-CAPABILITY-SOT.md). **Production GO not claimed. CI GREEN not met.**
- **Capability Drift criterion 5.4 `/api/v1/capabilities` tested (2026-08-01, DEC-131a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS @ `65e82cc` / DEC-131. ASGI contract `tests/contract/test_capabilities_api.py` (auth 401, list core kebab IDs, by-id 200/404, status filter). Narrow Docker pytest **4 passed**. App router unchanged; DEC-085 untouched. Residual note superseded: Capability Drift cluster COMPLETE (DEC-134a). Phase 0 **25/54 → 26/54** (superseded to **34/54** by DEC-139a). Capability Drift Complete **0 → 1** / Open **4 → 3** (superseded to Complete **4** / Open **0**). Companion [`decisions/DEC-131-CRITERION-5-4-CAPABILITIES-API-TEST.md`](decisions/DEC-131-CRITERION-5-4-CAPABILITIES-API-TEST.md). **Production GO not claimed. CI GREEN not met.**
- **DB-05 criterion 7.6 alembic check (2026-08-01, DEC-130h):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS @ `250bcb5` / DEC-130g. Phased Slices **5a–5g** (DEC-130…DEC-130g). Live Docker `alembic check` **exit 0** @ head `a4f7c29e1b80`. True DROP DEC **0**. DEC-085 untouched. Residual `ix_graph_nodes_search` KEEP via `include_object` (non-blocking). Phase 0 **24/54 → 25/54**. DB Schema Complete **5 → 6** / Open **1 → 0**. DB-05 story **COMPLETE**. **Production GO not claimed. CI GREEN not met.**
- **DB-05 criterion 7.6 alembic check Slice 5g (2026-08-01, DEC-130g):** **COMPLETE** (index/FK/comment/type residual → check exit 0). Metadata register + rename-twin KEEP + `include_object` KEEP for `ix_graph_nodes_search`; head **unchanged** `a4f7c29e1b80`; no DDL / no DROP. Live Docker `alembic check` **exit 0**. Closed via DEC-130h. Companion [`decisions/DEC-130g-DB-05-SLICE-5G-INDEX-FK-COMMENT.md`](decisions/DEC-130g-DB-05-SLICE-5G-INDEX-FK-COMMENT.md).
- **DB-05 criterion 7.6 alembic check Slice 5f (2026-08-01, DEC-130f):** **READY FOR REVIEW** (orphan KEEP register + vectors residual). Metadata KEEP stubs for 15 live orphan tables (`app/db05_orphan_keep.py`); Core `vectors.created_at`/`updated_at` restore; head **unchanged** `a4f7c29e1b80`; no DDL / no DROP. Live Docker `alembic check` still **FAILED** exit 255; `remove_table` **15->0**; `remove_column` **2->0**. True DROP DEC **0**. DEC-085 untouched. Phase 0 remains **24/54**. Companion [`decisions/DEC-130f-DB-05-SLICE-5F-ORPHAN-KEEP-REGISTER.md`](decisions/DEC-130f-DB-05-SLICE-5F-ORPHAN-KEEP-REGISTER.md). Next Slice **5g** → **landed DEC-130g** (check exit 0). **Do not claim VERIFIED/CLOSED / Production GO / CI GREEN / check clean.**
- **DB-05 criterion 7.6 alembic check Slice 5e (2026-08-01, DEC-130e):** **READY FOR REVIEW** (companies residual KEEP). ORM restore `do_not_contact` + `embedding_vector` on `Company` (DEC-129 pattern); head **unchanged** `a4f7c29e1b80`; no DDL / no DROP. Live Docker `alembic check` still **FAILED** exit 255; `remove_column` **4->2** (companies pair cleared; residual vectors cols). 7.4 KEEP stands. DEC-085 untouched. Phase 0 remains **24/54**. Companion [`decisions/DEC-130e-DB-05-SLICE-5E-COMPANIES-RESIDUAL-KEEP.md`](decisions/DEC-130e-DB-05-SLICE-5E-COMPANIES-RESIDUAL-KEEP.md). Next Slice **5f+** → **landed DEC-130f**. **Do not claim VERIFIED/CLOSED / Production GO / CI GREEN / check clean.**
- **DB-05 criterion 7.6 alembic check Slice 5d (2026-08-01, DEC-130d):** **READY FOR REVIEW** (index/type/null). Revision `a4f7c29e1b80` (down `e2b9d46f8a10`) additive CREATE INDEX x37 + contacts VARCHAR widen; ORM type/nullable/index-register. Live Docker `alembic check` @ `a4f7c29e1b80` still **FAILED** exit 255; `added_index` **37->0**; type **13->1**; NOT NULL **33->0**; `removed_index` **84->61**. No DROP / no SET NOT NULL. DEC-085 untouched. Phase 0 remains **24/54**. Companion [`decisions/DEC-130d-DB-05-SLICE-5D-INDEX-TYPE-NULLABLE.md`](decisions/DEC-130d-DB-05-SLICE-5D-INDEX-TYPE-NULLABLE.md). Next Slice **5e** → DEC-130e. **Do not claim VERIFIED/CLOSED / Production GO / CI GREEN / check clean.**
- **DB-05 criterion 7.6 alembic check Slice 5c (2026-08-01, DEC-130c):** **READY FOR REVIEW** (admin CREATE). Revision `e2b9d46f8a10` (down `d1a8c35e7f09`) creates `admin_plans` / `admin_feature_flags` / `admin_health_snapshots`. Live Docker `alembic check` @ `e2b9d46f8a10` still **FAILED** exit 255; `add_table` **3→0**. No RLS / no DROP. DEC-085 untouched. Phase 0 remains **24/54**. Companion [`decisions/DEC-130c-DB-05-SLICE-5C-ADMIN-GLOBAL-CREATE.md`](decisions/DEC-130c-DB-05-SLICE-5C-ADMIN-GLOBAL-CREATE.md). Next Slice **5d** index/type/null. **Do not claim VERIFIED/CLOSED / Production GO / CI GREEN / check clean.**
- **DB-05 criterion 7.6 alembic check Slice 5b (2026-08-01, DEC-130b):** **COMPLETE** (metadata classify). Registered **13** FPs (`Base.metadata` **70→83**); orphan KEEP **15** residual. `remove_table` **28→15**. Superseded for `add_table` by Slice 5c. Companion [`decisions/DEC-130b-DB-05-SLICE-5B-METADATA-CLASSIFY.md`](decisions/DEC-130b-DB-05-SLICE-5B-METADATA-CLASSIFY.md).
- **DB-05 criterion 7.6 alembic check Slice 5a (2026-08-01, DEC-130):** **COMPLETE** (plan honesty). Live check re-baseline FAILED; phased plan 5b–5f+. Companion [`decisions/DEC-130-DB-05-CRITERION-7-6-ALEMBIC-CHECK-PHASED.md`](decisions/DEC-130-DB-05-CRITERION-7-6-ALEMBIC-CHECK-PHASED.md).
- **DB-05 criterion 7.4 companies dead-column KEEP (2026-08-01, DEC-129a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS @ `4aacd6d` / DEC-129. Disposition **KEEP** — no DROP; `search_vector` FTS preserved (GENERATED ALWAYS); ORM restore + unit guard. Alembic head unchanged `d1a8c35e7f09`. Narrow Docker pytest **4 passed** (DEC-129 + DEC-085). DEC-085 untouched. Phase 0 **23/54 → 24/54**. DB Schema Complete **4 → 5**. Residual DB-05 = **7.6** (OPEN). Companion [`decisions/DEC-129-DB-05-COMPANIES-DEAD-COLUMN-KEEP.md`](decisions/DEC-129-DB-05-COMPANIES-DEAD-COLUMN-KEEP.md). **Production GO not claimed. CI GREEN not met.**
- **Security P0 criterion 1.5 SAST + dependency scan (2026-08-01, DEC-128a):** **VERIFIED/CLOSED CONDITIONAL**. Arch PASS + Validation PASS_CONDITIONAL @ `fa266b5` / DEC-128. Residual: *post-align Security Scan pip-audit field-verify PENDING until tip containing `fa266b5` is pushed and Security Scan pip-audit SUCCESS with poetry export + 1 ignored (ecdsa)*. DEC-085 untouched. Phase 0 **23/54** (superseded to **24/54** by DEC-129a). Security P0 Complete **5/5**. Companion [`decisions/DEC-128-CRITERION-1-5-SAST-DEPS-RESIDUAL.md`](decisions/DEC-128-CRITERION-1-5-SAST-DEPS-RESIDUAL.md). **Not unconditional CLOSED. Production GO not claimed. CI GREEN not met.**
- **Security P0 criterion 1.3 CSRF X-API-Key (2026-08-01, DEC-127a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (Docker CSRF suite **11 passed**; DEC-085 untouched) @ `5db0756` / DEC-127. Phase 0 **22/54** (superseded to **24/54** by DEC-129a). Companion [`decisions/DEC-127-CSRF-X-API-KEY-BYPASS.md`](decisions/DEC-127-CSRF-X-API-KEY-BYPASS.md). **Production GO not claimed. CI GREEN not met.**
- **Security P0 criterion 1.2 Webhook SSRF (2026-08-01, DEC-125a):** **VERIFIED/CLOSED**. Arch PASS + Validation PASS (Docker SSRF suite **32 passed**, 54 deselected) @ `fd8699d` / DEC-125. Staging SSRF pentest residual OPEN (non-blocking). Phase 0 **21/54** (superseded to **24/54** by DEC-129a). Companion [`decisions/DEC-125-WEBHOOK-SSRF-URL-ALLOWLIST.md`](decisions/DEC-125-WEBHOOK-SSRF-URL-ALLOWLIST.md). **Production GO not claimed. CI GREEN not met.**
- **DB-05 Slice 3 index/nullable (2026-08-01, DEC-122):** **COMPLETE** (safe additive). Revision `c9f4a21b6e08`: rename `ix_rev_*`→`ix_*` + webhook/scheduled_jobs name align; drop workflow short-name twins; additive notification/tasks indexes. Companies dead-column DROP **STOPPED** (`search_vector` live FTS). No ENABLE RLS on deferred-8. DEC-085 untouched. Companion [`decisions/DEC-122-DB-05-SLICE-3-INDEX-NULLABLE-TRIAGE.md`](decisions/DEC-122-DB-05-SLICE-3-INDEX-NULLABLE-TRIAGE.md). Validation **build validated** (narrow Docker pytest + local upgrade). **Production GO not claimed. CI GREEN not met.** Next: Slice 4+ companies DEC / contacts naming / deferred-8 RLS.
- **DB-05 Slice 2 type authority (2026-08-01, DEC-121):** **COMPLETE**. emails/meetings ORM aligned to Alembic/live **UUID** (`UUID(as_uuid=False)` / `Mapped[str]`); no DDL ALTER; alembic head was `b7e2f65a3f07` (superseded by Slice 3). No ENABLE RLS on deferred-8. DEC-085 untouched. Railway/APP_POSTGRES not touched (DEC-120). Companion [`decisions/DEC-121-DB-05-SLICE-2-EMAILS-MEETINGS-UUID.md`](decisions/DEC-121-DB-05-SLICE-2-EMAILS-MEETINGS-UUID.md). Validation **build validated** (narrow Docker pytest). **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B7 (2026-08-01, DEC-119):** **CLOSED** (execution). Join RLS on `admin_role_permissions` via `admin_roles` (`b7e2f65a3f07`, down `a6d1e54f2e06`; String(100) FK). Nullable parent fail-closed (no `OR IS NULL` bypass). Live `POLICY_COUNT` **59**. Deferred-8 admin billing untouched (no ENABLE RLS). DEC-085 untouched. **Category B execution track COMPLETE (B1–B7).** Companion [`decisions/DEC-119-CATEGORY-B7-ADMIN-ROLE-PERMISSIONS-RLS.md`](decisions/DEC-119-CATEGORY-B7-ADMIN-ROLE-PERMISSIONS-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.** Does not restore Phase 0 R-14 GO (DEC-120).
- **Category B Slice B6 (2026-08-01, DEC-118):** **CLOSED** (execution). Join RLS on `webhook_deliveries` via `webhook_subscriptions` (`a6d1e54f2e06`, down `f5c0d43e1d05`; String(36) FK). Live `POLICY_COUNT` **58** (superseded to **59** by DEC-119). Deferred-8 `webhook_endpoints` untouched (no ENABLE RLS). DEC-085 untouched. Companion [`decisions/DEC-118-CATEGORY-B6-WEBHOOK-DELIVERIES-RLS.md`](decisions/DEC-118-CATEGORY-B6-WEBHOOK-DELIVERIES-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B5 (2026-08-01, DEC-117):** **CLOSED** (execution). Join RLS on `password_reset_tokens` + `refresh_token_families` via `users` (`f5c0d43e1d05`, down `e4b9c32d0c04`; UUID FK). Live `POLICY_COUNT` **57** (superseded to **58** by DEC-118). Auth-path careful: own-tenant + fail-closed; no permissive bypass. DEC-085 untouched. Companion [`decisions/DEC-117-CATEGORY-B5-IDENTITY-TOKEN-CHILDREN-RLS.md`](decisions/DEC-117-CATEGORY-B5-IDENTITY-TOKEN-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B4 (2026-08-01, DEC-116):** **CLOSED** (execution). Join RLS on `decision_center_audits` + `decision_center_feedback` via `decision_center_decisions` (`e4b9c32d0c04`, down `d3f8a21c9b03`; `p.id::text` cast for UUID/varchar FK). Live `POLICY_COUNT` **55** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-116-CATEGORY-B4-DECISION-CENTER-CHILDREN-RLS.md`](decisions/DEC-116-CATEGORY-B4-DECISION-CENTER-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B3 (2026-08-01, DEC-115):** **CLOSED** (execution). Join RLS on `analytics_report_executions` + `analytics_report_shares` via `analytics_reports` (`d3f8a21c9b03`, down `c221d15f8b02`). Live `POLICY_COUNT` **53** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-115-CATEGORY-B3-ANALYTICS-CHILDREN-RLS.md`](decisions/DEC-115-CATEGORY-B3-ANALYTICS-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B2 (2026-08-01, DEC-114):** **CLOSED** (execution). Join RLS on `commercial_activities` + `commercial_quote_lines` via sessions/quotes (`c221d15f8b02`, down `b110c04e7a01`). Live `POLICY_COUNT` **51** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-114-CATEGORY-B2-COMMERCIAL-CHILDREN-RLS.md`](decisions/DEC-114-CATEGORY-B2-COMMERCIAL-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B1 (2026-08-01, DEC-112):** **CLOSED** (execution). Join RLS on `branches` + `licenses` via `companies` (`b110c04e7a01`, down `b8d4f02a1c06` after DEC-113 rebase). Live `POLICY_COUNT` **49** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-112-CATEGORY-B1-COMPANY-CHILDREN-RLS.md`](decisions/DEC-112-CATEGORY-B1-COMPANY-CHILDREN-RLS.md). Validation **build validated** (pytest 7 passed). **Production GO not claimed. CI GREEN not met.**
- **DB-05 Slice 1 CREATE (2026-08-01, DEC-113):** **COMPLETE** (CREATE TABLE only). Revisions `a7c3e91f0b05` -> `b8d4f02a1c06` (head). Tables: admin_licenses/invoices/transactions/ai_costs/jobs; webhook_endpoints; scoring_scorecards; revenue_analytics_snapshots. Remaining P0 CREATE **0**. No RLS. DEC-085 untouched. Companion [`decisions/DEC-113-DB-05-SLICE-1-P0-CREATE.md`](decisions/DEC-113-DB-05-SLICE-1-P0-CREATE.md). Validation **light validated**. **Production GO not claimed. CI GREEN not met.**
- **DB-05 Slice 0 inventory (2026-08-01, DEC-111):** **COMPLETE** (inventory only). Alembic single head `065d1d3a466b`; ORM **80** / create_table **89**; P0 = **8** R-09 missing CREATE (DEC-110 deferred); P1 = emails/meetings UUID vs String(36) + companies/index/nullable clusters. Next: Slice 1 additive CREATE (no RLS yet). DEC-085 untouched. Companion [`decisions/DEC-111-DB-05-SCHEMA-DRIFT-INVENTORY.md`](decisions/DEC-111-DB-05-SCHEMA-DRIFT-INVENTORY.md). Validation **docs / light validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B RLS planning (2026-08-01, DEC-110):** **CLOSED** (planning). Canonical inventory: Category A live **47**; Category B join **12**; Category A deferred **8** (DB-05). Execution slices B1?B7 READY ? no Alembic RLS SQL this land. DEC-085 untouched. Companion [`decisions/DEC-110-CATEGORY-B-RLS-INVENTORY.md`](decisions/DEC-110-CATEGORY-B-RLS-INVENTORY.md). Validation **docs / light validated**. **Production GO not claimed. CI GREEN not met.**
- **CI-22 executive close (2026-08-01, DEC-109):** Starlette floor **MET**; field pip-audit clear of starlette (`30688863161` / `91339902722` @ `3084e5b`; Stages 1-5 green corroboration `30689682988` @ `7ba137b`). Compatibility residuals burned (Request C3 + FastAPI 0.141 RBAC override). **CLOSED**. ecdsa = DEC-057 residual (not CI-22). Validation **build validated**. **whole-pipeline CI GREEN not met** (CI-08).
- **Contract tests slice 4 (2026-08-01, DEC-106):** `GET /api/v1/decisions` **401** (`DetailStringError` / `UnauthorizedError` string detail) + **422** (`HTTPValidationError` for `limit` bounds). Host `pytest tests/contract/ -m contract` ? **14 passed**. DEC-085 untouched. No invented ErrorResponse. Validation **light validated**. **CI GREEN not met** (CI-08).
- **CI-08 ops package (2026-08-01, DEC-104):** Recommended **Option A** (org/account GHCR Packages write for Actions). Interim **Option D** honesty: **CI GREEN (code path)** ? **CI GREEN (full incl. publish)**. Option B (build vs push / continue-on-error) **not authorized** without separate AC DEC. Option C (alternate registry) **rejected** ? docs/runbooks assume `ghcr.io/ragheeda-boop/salesos/*`. Workflow already has `packages: write` ? not a YAML omission. CI-08 stays **BLOCKED** pending human ops field push SUCCESS. Validation: **docs only / not validated** (pipeline). **whole-pipeline CI GREEN (full) not met**.
- **Stage 6 Frontend Docker install (2026-08-01):** Prior `30689682988` Frontend Build FAILED on eslint **10** peer vs `eslint-config-next@15.5.22` (Dockerfile omitted CI-14 Slice 2 `.npmrc` `legacy-peer-deps`). Fix `53fe214` copy `.npmrc`; follow-up `927276f` copy `scripts/ci14-stub-rushstack-eslint-patch.js` for postinstall. Field re-run `30690622307` / job `91345633450` @ `927276f`: Docker **npm install DONE** + **next build DONE** (73.6s); job **FAILED** only on GHCR push **403** (same CI-08/R-17 as Backend). Backend Stage 6 job `91345633439`: image build OK, push **403**. Stage 7 E2E **skipped** (needs both Stage 6 green). No `--force`; DEC-100 Jest STOP intact; DEC-085 untouched. Validation **build validated** (image build proven; push ops-blocked). **whole-pipeline CI GREEN not met** (CI-08).
- **Stage 4 Integration xdist (2026-08-01):** Prior run `30689313205` / job `91341467519` FAILED ? `pytest: error: unrecognized arguments: -n` (workflow `-n auto`, Poetry lacked `pytest-xdist`). Fix `7ba137b`: add `pytest-xdist` ^3.6 to Poetry `dev` (+ lock/execnet). Field verify run `30689682988` / job `91342461119` **SUCCESS** ? **2700 passed** / 2 skipped (xdist gw0?gw3). Stages 1?5 green on same run. Stage 6 **both FAILED** (new cluster): Backend Build push GHCR **403** (CI-08/R-17); Frontend Build Docker `npm install` peer conflict eslint **10** vs `eslint-config-next@15.5.22` (needs `^7||^8||^9`; Dockerfile omits `.npmrc` legacy-peer-deps). Stage 7 E2E **skipped**. DEC-085 `set_config` untouched. Validation **build validated**. **whole-pipeline CI GREEN not met**.
- **Frontend Unit burn (2026-08-01):** Field Stage 3 @ **3084e5b** run `30688863161` / job `91339981968` ? **152 failed** / 44 passed suites (413 tests passed; suites never loaded). Root cause: CI-14 Slice 2 `.npmrc` `legacy-peer-deps=true` omits peer `@testing-library/dom` of `@testing-library/react` 16.x; prior direct pin `f0b0d6a` dropped by Slice 2 land `240f9a8`. Fix `36e59f5`: re-add `@testing-library/dom` ^10.4.1 as direct `devDependency` + lock. Field verify @ tip **36e59f5** run `30689313205` / job `91341151533` ? **196/196 suites passed**, **2279 passed** / 1 skipped. **Not R-23 reopen** (install/peer, not Jest assertion debt). Validation **build validated**. **whole-pipeline CI GREEN not met** (downstream stages may still run/fail).
- **Backend Unit burn-down follow-on (2026-08-01):** Field Unit @ tip **3084e5b** run `30688863161` / job `91339985115` ? **0 failed** / 2700 passed / 2 skipped (prior **19** @ e7a18a9/91336593923; baseline **85** @ 6077af8). Root cause: FastAPI **0.141** `_IncludedRouter` hid route dependants ? RBAC `NotFoundError` user 404 (not missing mount); `abd7e03` mount assert ERRORed on `app.routes`. Fix walks `wr.router.routes` for `_require_permission` overrides. Workflow cluster cleared. DEC-085 intact. Validation **build validated**. Frontend Unit still red on same run; **whole-pipeline CI GREEN not met**.
- **R-26 / DEC-085 HARD STOP (2026-08-01 @ `d0e80f9`):** `get_db()` must use `SELECT set_config('app.tenant_id', :tenant_id, true)` ? **never** `SET LOCAL ? = :tenant_id`. Parallel agents reintroduced SET LOCAL a **4th** time; restored + pushed. Host RLS suites **30/30 PASS**. Do not revert for mypy/ruff/style. **CI guard:** `tests/unit/test_dec085_set_config_guard.py` fails if `get_db` reintroduces `SET LOCAL` for tenant GUC.
- **DEC-120 prod tip RLS align (2026-08-01):** Prod alembic **`c9f4a21b6e08`→`d1a8c35e7f09`**; POLICY_COUNT **59→67**; RLS_ON **67**; deferred-8 policies=1 each. Method: SSH upload tip `scripts/` + `d1a8` rev; owner `alembic upgrade head`. Image still **`9664e9fc`/`b62252a`**. `APP_ENGINE salesos_app` / `rolbypassrls=False` retained; `/health` 200. **Does not mark 2.3 VERIFIED/CLOSED.** **Production GA = NO-GO.** Validation **light validated**.
- **DEC-120 Slice D+E production (2026-08-01):** **D DONE** — deploy **`9664e9fc`** from **`b62252a`**; `/health` **5.1.0-rc1**; `salesos_app` session proof; alembic **`0051`→`c9f4a21b6e08`**, POLICY_COUNT **0→59**. **E DONE** — bare/wrong-tenant **0** vs owner **141221**; **PASS_WITH_SINGLE_TENANT_CAVEAT** (1 live tenant). Superseded for tip head by **prod tip RLS align** (`d1a8`/67). **Production GA = NO-GO.** Validation **light validated**.
- **DEC-120 Slice C staging Alembic (2026-08-01):** Staging upgraded **`0049` → `b7e2f65a3f07` (head)**; **POLICY_COUNT 0 → 59**; RLS_ON 59. Method: SSH + tip `scripts/` + B7 revision (image had excluded `/scripts/` via `.dockerignore`). Post: `APP_ENGINE salesos_app`; `/health` 200. Dockerfile/`.dockerignore` fixed for future preDeploy. **Prod not migrated.** Next: **D** prod image + role proof → **E** bypass-probe. Validation **light validated**. Bypass-probe **not** claimed. **Production GA = NO-GO.**
- **DEC-120 Slice B staging (2026-08-01):** Deploy **`98bf85bf` SUCCESS** via `railway up` tip **`0bd73fc`** backend (pre-B7). SSH: `APP_ENGINE salesos_app`. (Superseded for alembic/policies by Slice C.) Prod **not** promoted (`3.1.0`). Validation **light validated**. **Production GA = NO-GO.**
- **DEC-120 Slice A (2026-08-01):** **VERIFIED** — `app_database_url` / `APP_POSTGRES_*` + engine split (`5e7023f` ancestor). Companion §7. Validation **light validated**. **Production GA = NO-GO.**
- **DEC-120 Railway R-14 reopen (2026-08-01):** Principal Audit Tier-1 **CONTRADICTED** DEC-016. S04-04 **REOPENED**; R-14 **REOPENED**; Phase 0 R-14 = **NO-GO**. Remediation: A **DONE** → B **DONE** → **C DONE** (staging alembic/policies 59) → D (prod image) → E (bypass-probe). **Rotate Postgres passwords** — human/ops. Validation **docs / light validated**.
- **Phase 0 Architecture Validation (2026-08-01, DEC-086):** **SUPERSEDED for R-14 GO by DEC-120.** Historical: S04-04 CLOSED under DEC-016 at `7232979`. **Current:** Phase 0 (DEC-008 RLS / R-14) exit = **NO-GO**. **STORY-02-01 CLOSED** (DEC-044 @ **47**) — **do not reopen**. **Production GA / External pilot = NO-GO**. **CI GREEN not met**. Remaining board **P0:** **S04-04** (REOPENED). CI-08 = **GOVERNANCE COMPLETED** (DEC-150 B). See `EXECUTION_DAG.md`.
- Complete/Closed: **26/26** (CI-01, CI-11, S04-01, CI-15, CI-02, CI-03, CI-04, CI-05, CI-18, CI-17, CI-07, CI-06, CI-12, CI-13, CI-10, compose prod name fix, S04-05, CI-21, S04-06, **CI-16**, **CI-20**, **CI-19**, **CI-14**, **CI-22**, **CI-09 CLOSED CONDITIONAL** DEC-149a, **CI-08 GOVERNANCE COMPLETED** DEC-150 B). **S04-04 CLOSED CONDITIONAL** DEC-154. Adjacent closed (not board fraction): **Jest-debt / R-23** (DEC-077); Category B planning (DEC-110); **Category B execution B1–B7 COMPLETE** (DEC-119); checklist **3.9 CLOSED CONDITIONAL** DEC-152; **4.1/4.8 CLOSED** DEC-153. Canonical deploy = DEC-149 Railway+Vercel. Stage 6 GHCR **off critical path**. **Operating State: PHASE 1 PARALLEL EXECUTION ACTIVE** (plan **ACTIVE** @ evidence tip `53a4aa7`). Phase 0 checklist **54/54 COMPLETE**. Hard OPEN: **none**. Streams A/B/C/D ACTIVE. **No Production GO.**
- STORY-02-02 browser/E2E verify (DEC-095): **DONE / CLOSED** ? live Next `next dev` unauthenticated redirect probe **PASS**: `GET /dashboard` ? **307** `location: ?/login?callbackUrl=%2Fdashboard`; `GET /login` + `GET /` ? **200**. Prior DEC-088 Jest **14/14**. Optional authenticated Playwright/`smoke-ui.ps1` **not run**. Validation **browser-validated** (redirect AC only). **Production GO not claimed. CI GREEN not met.** See [`decisions/DEC-095-STORY-02-02-BROWSER-VERIFY-CLOSED.md`](decisions/DEC-095-STORY-02-02-BROWSER-VERIFY-CLOSED.md). Supersedes DEC-085/DEC-088 PARTIAL.
- STORY-02-03 JWT suite / audience consumption (DEC-093): **CLOSED** ? Platform admin (/api/v1/admin via app/modules/admin) wires require_owner_role_dep -> verify_owner_token -> decode_owner_access_token (app/owner_auth.py). Tenant verify_token remains salesos-api only. Groundwork DONE (2379e5f). Host poetry run pytest tests/unit/test_jwt_audience_split.py -> **14/14 PASS** (**light validated**). DEC-091 consumption-OPEN superseded. **Production GO not claimed.**
- S04-05 (DEC-039): write-protection adversarial suite on master (`8699796`); validation **build validated** ? Docker `pytest` **8/8 PASS in 4.88s**. Related Sprint 03 landings on master (DEC-040): STORY-02-03 `2379e5f`, STORY-02-02 `3f4b3c8`, STORY-03-04 `623077c`, STORY-02-04 `932f722` (already on master). Card primitives `9577c98` ? related Jest-debt progress (foundation `card.tsx` for suite recovery); Jest-debt later **CLOSED** DEC-077; **CI GREEN not met**.
- S04-06 (DEC-045): remaining adversarial RLS suite on master (`119df9e`); file `tests/integration/test_adversarial_rls_remaining.py` ? 7 tables; validation **build validated** ? Docker pytest **15/15 PASS**; POLICY_COUNT **47** intact; S04-01/S04-05 unchanged; RLS inventory **not** reopened (DEC-044); **CI GREEN not met**.
- Jest-debt / R-23 (DEC-077): **CLOSED** ? ?10 field verify `30679804383` / `465c638` (Stage 3 job `91314523292`) = **0** failing suites (196/196; 2278 passed / 1 skipped / 2279); Stage 1 Frontend Lint **success**; Stage 3 Frontend Unit Tests **success**. Path: `4fdc1d8` + `5bba606` + `556304d` + Prettier Onboarding gate `11470b1`. Supersedes ?8 **11**. **Whole-pipeline CI GREEN not met** (Backend Lint/Types + Stage 5 / security still red).
- CI-19 Wave 1 (DEC-043): **COMPLETE** at `d5c9b57` ? 8 GHA `run-shell-injection`/`github-script-injection` alerts remediated via `env:`/`process.env` (deploy workflows + legacy `sales-os` run.yml).
- CI-19 Wave 3 SHA-pin (DEC-069): **COMPLETE** at `556304d` ? 115 `uses:` Action refs pinned to full commit SHAs in `.github/workflows/{security-scan,ci,docker-smoke,deploy,deploy-staging,deploy-production}.yml` + `sales-os/.github/workflows/run.yml`; `trivy-action@master` ? `v0.29.0` SHA; Semgrep severity/upload path **unchanged** (no gate weaken). Wave 2 SKIPPED/deferred.
- CI-19 Wave 3 residual (DEC-074): **COMPLETE** at `465c638` ? 15 K8s container `securityContext.allowPrivilegeEscalation: false` (`salesos/infra/k8s/`); app images also `runAsNonRoot` + drop ALL caps; Dockerfile USER on backup + alertmanager images; DynamoDB SSE + Secrets Manager CMK in `salesos/infra/terraform/main.tf`. Architecture STOP: data-store pods omit `runAsNonRoot` (official image entrypoint/volume UID risk). Triage: `CI_19_SEMGREP_TRIAGE.md`. Risk **R-24** still Open. Validation: **light validated** (manifest inventory vs Code Scanning alert paths #591?#609). **CI GREEN not met**.
- CI-19 Wave 3 residual field-verify (Security Scan / Semgrep / Trivy): run 30679804395 @ 465c638 ? Semgrep (sast-scan) **SUCCESS**; Trivy/sbom **FAILURE** at setup (setup-trivy@v0.2.2 unresolvable under 	rivy-action v0.29.0 pin) ? **pre-existing pin residual** (same on 30679776654 / 30679589739); no manifest fix from DEC-074. Tip corroboration 30680090950 @ 95041f Security Scan **success** after pin bump. **CI GREEN not met**.
- CI-19 Wave 4 (DEC-076): **COMPLETE** ? repo-root `.semgrepignore` path excludes for out-of-GA noise (`taqeem_scraper/`, root scrape JSON, `sales-os/`, `crm_pipeline.py`, `salesos/backend/demo/`, `salesos/frontend/scripts/`, `salesos/scripts/`) + redact `PILOT_SECRETS_GUIDE.md` to `CHANGE_ME_*`. Semgrep ERROR/WARNING severity + SARIF upload **unchanged**. Does **not** exclude `salesos/backend` app/runtime or product FE packages. Wave 2 still SKIPPED/deferred. Validation: **light validated**. **CI GREEN not met**.
- CI-19 Wave 4 follow-up (DEC-083): **FIXED** ? root `.semgrepignore` had replaced Semgrep built-ins ? live open findings regression **~116 ? ~130**. Fix prepends upstream defaults (`tests/`, `node_modules/`, `.venv/`, `build/`, `dist/`, VCS dirs, package-manager caches, etc.) then keeps DEC-076 Wave 4 excludes. Severity gates **unchanged**. Does **not** claim finding-zero GREEN. Validation: **light validated**. **CI GREEN not met**.
- CI-19 Wave 5 (DEC-082): **COMPLETE** ? residual singletons cleared with real fixes (8 alerts): Cobertura regex parse (no `xml.etree`); pentest brief `wss://`; `website_li_pipeline.py` ? `http.client`; FE forms `pattern?: RegExp`; search-highlight / session-test without `new RegExp`; `StateRuntime` prototype-pollution guards. Architecture STOP: none. Semgrep gates unchanged. Validation: **light validated**. **CI GREEN not met**.
- CI-19 Wave 2 Slice 3 (DEC-099): **COMPLETE** ? Core honesty for postgres_repo (6) + 	imeline_runtime (5). Expected clear **11**; remainder ~**32**. Search timeout uses set_config (not SET LOCAL); get_db DEC-085 untouched. Narrow pytest **50 passed**. **CI-19 OPEN.** **CI GREEN not met**.
- CI-19 Wave 2 Slice 4 (DEC-101): **COMPLETE** ? Core honesty for search_runtime (4) + sdk/search (3) + vector_store (3) + tasks (3) + contact search_repository (1); admin_router local. Expected clear **14**; remainder ~**18**. Timeouts via set_config; get_db DEC-085 untouched. Narrow pytest **14 passed**. **CI-19 OPEN.** **CI GREEN not met**.
- CI-19 Wave 2 Slice 5 (DEC-102): **COMPLETE** ? Core honesty for activity_runtime (3) + kg service (2) + memory postgres_store (2) + kg sql_repository full Core. Expected clear **7**; remainder ~**11** (alembic RLS out-of-scope; init_db/benchmark/mcp). get_db DEC-085 untouched. Narrow pytest **58 passed**. **CI-19 OPEN.** **CI GREEN not met**.
- CI-19 Wave 2 Slice 6 (DEC-103): **COMPLETE** ? residual package: init_db DDL exec_driver_sql (1) + benchmark (2) + mcp Core (1); **alembic RLS accepted residual (7)** ? do not churn. Wave 2 app-honesty COMPLETE. get_db DEC-085 untouched. Narrow pytest DEC-085 **2 passed**. **CI-19 OPEN**. **No Slice 7**. **CI GREEN not met**.
- CI-19 Wave 2 field-verify (post DEC-103): Security Scan run `30686789458` @ tip `abaae85` (includes land `3d49ae1`) **SUCCESS**; sast-scan job `91334080531` **SUCCESS**. Live CS: app `avoid-sqlalchemy-text` **0** (#513/#523/#524/#542 **fixed**); alembic residual **7** text + **4** raw/formatted on `0020`. Wave 2 **PARKED COMPLETE**. **No Slice 7**. **CI-19 remains OPEN** (executive residual-close; non-SQL Semgrep leftovers). Validation: **build validated** (field CS + Security Scan). **CI GREEN not met**.
- CI-19 executive residual-close (DEC-105): **CLOSED with residual**. Pre-land open Semgrep OSS **19** @ `a02c8f1`. Burned **8** non-alembic (logger x4 identity+scraper; StateRuntime prototype helpers; triage-doc WS FP; DynamoDB lock-table CMK; remove unused workflow-level `SLACK_WEBHOOK`). Accepted residual **11** alembic (DEC-103 ? do not churn). R-24 Closed ? mitigating residual. Semgrep ERROR/WARNING + SARIF **unchanged**. DEC-085 untouched. **Field-verify (2026-08-01):** Security Scan [`30693735860`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30693735860) **SUCCESS** / sast-scan `91352893256` **SUCCESS** @ tip `b9062d6` ? Semgrep CLI **11** (11 blocking, alembic-only); live CS Semgrep open **11** (`0afbf3e6ae53` x4 + `065d1d3a466b` x3 text + `0020` raw/formatted x4); burns **fixed** #417/#515/#516/#546/#547/#608/#834/#835 (`fixed_at=2026-08-01T09:27:31Z`); app unexpected **0**. Do **not** reopen CI-19. Validation: **build validated**. **CI GREEN not met** (CI-08).
- CI-21 (DEC-041): Gitleaks false-positive closed on `b03ffbf` ? jwt.io sample JWT replaced with non-JWT Bearer stub in `test_post_middleware.py`; field-verify Security Scan run `30671556546` **SUCCESS** (Gitleaks step PASS; all jobs green); no `.gitleaksignore` / allowlist / global disable; **does not claim full CI green**.
- compose prod name fix (DEC-037): commit `cb707be` adds `name: salesos-prod` to `docker-compose.prod.yml`; validation light ? YAML name valid; full `config --quiet` blocked by missing production env secrets (not a compose-name defect).
- CI-20 (DEC-038 register; DEC-046?050 Phases 1?5; DEC-053 Phase 6; DEC-055 Phase 7; DEC-058 Phase 8; DEC-059 Phase 9; DEC-060 Phase 10; DEC-061 Phase 11; DEC-064 Phase 12; DEC-066 Phase 13; DEC-068 Phase 14; DEC-071 Phase 15; DEC-070 Phase 16; DEC-078 Phase 17; DEC-079 Phase 18; DEC-080 Phase 19; DEC-084 Phase 20; DEC-092 Phase 21): Backend Types gate baseline **308 mypy errors** on CI run `30670339985`. **Phase 1?9** as previously recorded (overall expected **~308 ? ~100**; field-verify `30677025355` **104** / 0 demo_mode). **Phase 10 COMPLETE** at `ca76f9c` ? module `app/modules/communication_hub` **11 ? 0**; overall expected **~100 ? ~89** (field **104 ? ~93**); field-verify `30677381493` **93** / 0 hub. **Phase 11 COMPLETE** at `86b4094` ? module `app/modules/work_intelligence` **5 ? 0**; overall expected **~89 ? ~84** (field **93 ? ~88**); field-verify `30677732725` **88** / 0 WI. **Phase 12 COMPLETE** at `e44b7f3` ? `app/boot/startup.py` **7 ? 0** + `app/database.py` **7 ? 0**; overall expected **~84 ? ~70** (field **88 ? ~74**). **Phase 13 COMPLETE** at `5edb6d6` ? `app/application` **9 ? 0** (`cast` / `BaseException` narrow / `Request | None`); overall expected **~70 ? ~61** (field **~74 ? ~65**). **Phase 14 COMPLETE** at `1f14337` ? SSO remnants **11 ? 0**; overall expected **~61 ? ~50** (field **~65 ? ~54**); field-verify `30679062993` **54** / **0** SSO (baseline `30678653664` **65** / **11** SSO; tip `30679150001` **54** / 0 SSO). **Phase 15 COMPLETE** at `7fed3dc` ? sla_monitor+redis_client **5 ? 0**; overall expected **~46 ? ~41** (field **~50 ? ~45**). **Phase 16 COMPLETE** at `26156df` ? `app/startup.py` **4 ? 0**; overall expected **~50 ? ~46** (field **~54 ? ~50**). **Phase 17 COMPLETE** at `d5e4de2` ? `app/modules/decision` **5 ? 0**; overall expected **~41 ? ~36** (field **~45 ? ~41**); host mypy decision `--follow-imports=skip` **0**. **Phase 18 COMPLETE** ? `app/modules/entity_resolution` remnants **10 ? 0**; overall expected **~36 ? ~26** (field **~41 ? ~31**); host mypy entity `--follow-imports=skip` **0**. **Phase 19 COMPLETE** ? `app/modules/notion_sync` **4 ? 0** + `app/graphql` **4 ? 0**; overall expected **~26 ? ~18** (field **~31 ? ~23**); host mypy notion+graphql `--follow-imports=skip` **0**. **Phase 20 COMPLETE** at c4fb067 ? webhooks+identity hosts+employee_360 **8 ? 0**; overall expected **~18 ? ~10** (field **~23 ? ~15**); host mypy skip **0**. **Phase 21 COMPLETE** (DEC-092) - residual clusters -> host CI-equivalent mypy **0**. **CLOSED DEC-096** - field Backend Types **0** on tip `220d91a` (CI run `30684023356`, job `91326366120` SUCCESS; 0 `error:` lines). Tip corroboration `af4835f` (`30684308678`/`91327119501`) and `844548e` (`30684181874`/`91326794076`). Post-P21 clears: `5588bb7` (syntax), `a636c69` (audit imports), `220d91a` (`getattr` EVENT_REGISTRY). DEC-085 `set_config` intact. Validation **build validated** (field Types). Story **CLOSED**. **Whole-pipeline CI GREEN not met**.
- CI-16 Slice 1 (DEC-051): `python-multipart` bumped **0.0.9 ? 0.0.32** at `1e73a2f` (`pyproject.toml` constraint `>=0.0.27,<0.1.0`; Poetry lock update of that package only). Smoke: `poetry run` import `multipart` + `app.main` ? FastAPI OK (**light validated**). Explicitly **not** bumped at that time: `strawberry-graphql`, `starlette`, `ecdsa`. **CI GREEN not met**.
- CI-16 Slice 2 STOP (DEC-052 at `8323c84`): starlette bump blocked ? no pip-audit-clean version under `fastapi ^0.111` / `pydantic <2.9`. Follow-on **CI-22** registered (DEC-054) for FastAPI/Starlette/Pydantic modernization; **not** CI-16 slice work. **CI GREEN not met**.
- CI-16 Slice 3 (DEC-056): `strawberry-graphql` bumped **0.243.1 ? 0.323.2** at `d3f1eef` (`pyproject.toml` constraint `>=0.315.7,<1.0.0`; Poetry lock update; transitive `cross-web 0.7.0`). FastAPI/starlette/pydantic/multipart/ecdsa lock versions unchanged. Smoke: GraphQL schema import + `test_graphql_schema_introspection` **1 passed**; `pip-audit` **NO strawberry findings** (**light validated**). Explicitly **not** bumped: `ecdsa`, starlette/FastAPI/pydantic. **CI GREEN not met**.
- CI-16 ecdsa disposition (DEC-057): **Option A Accepted** ? residual risk on `ecdsa` (PYSEC-2026-1325 / CVE-2024-23342 Minerva; upstream no planned fix). Evidence: JWT paths **RS256** / **HS256** only; no `ES*` / no direct `ecdsa` import; transitive via `python-jose` only. Option B (PyJWT migration) deferred ? not clearly small/safe for unattended residual close. Starlette ? **CI-22**. **CI-16 CLOSED** for slice scope. No code / no `pip-audit` ignore in the DEC land. **CI GREEN not met**.
- CI Stage 5 pip-audit policy (DEC-090): Follow-on to DEC-057 Option A. Evidence run `30681284601` @ `f2c7587` + tip `e993e83` host ? sole residual `ecdsa 0.19.2` **PYSEC-2026-1325** (NO starlette). Lands **named** `--ignore-vuln PYSEC-2026-1325` on CI `security-pip-audit`; `--strict` retained (no silent weaken). Stage 5 pip-audit expected **green**; **whole-pipeline CI GREEN not met**.
- CI Stage 5 Secrets Scan / Trivy policy (DEC-098): Follow-on to DEC-090 for same residual. Evidence run `30684813480` @ `c8c1bce` ? sole HIGH `ecdsa` **CVE-2024-23342** (frontend lock 0). Lands **named** `CVE-2024-23342` in `.trivyignore` + `trivyignores` on CI Secrets Scan; `exit-code: 1` / CRITICAL,HIGH retained. Stage 5 Secrets Scan expected **green**; **whole-pipeline CI GREEN not met**. DEC-085 preserved.
- CI-22 Phase 1 follow-up observer (`e993e83` over `442af64`, 2026-08-01): CI run **30681693636** FAIL (not whole-pipeline GREEN). **Backend Types** FAIL (~27) ? sole clear CI-22 persist vs pre-CI-22 `cf94057`/`30680804636`: `excel_import/router.py` `Request = Request` (later fixed `4f035dd`). Remaining Types = R-22/CI-20 (+ post-442 admin `list` UP006). **Backend Lint** FAIL (3x `entity_resolution` E501/B010) ? **CI-20** residual (`c223857`), not CI-22. **pip-audit** FAIL sole **ecdsa** PYSEC-2026-1325 ? **starlette cleared** (same as `442af64`/`30681046523`; pre had 10 vulns/2 pkgs). DEC-090 ignore landed later `7aa65b3` (not on `e993e83`). **Docker Smoke** **30681693640 SUCCESS** (clears `442af64` smoke **30681046515** backend-unhealthy). Secrets Scan still ecdsa via Trivy. **No whole-pipeline GREEN claim**.
- CI-22 Phase 1 (DEC-081 @ `442af64`): **COMPLETE** ? fastapi **0.141.1** / starlette **1.3.1** / pydantic **2.13.4**; Request C3 fixes; host smoke + middleware. **CLOSED DEC-109**. **CI GREEN not met**.
- CI-22 executive close (DEC-109): **CLOSED** ? starlette floor met; field `30688863161` pip-audit SUCCESS (1 ignored=ecdsa); Unit RBAC override field-green. Companion [`decisions/DEC-109-CI-22-EXECUTIVE-CLOSE.md`](decisions/DEC-109-CI-22-EXECUTIVE-CLOSE.md). **whole-pipeline CI GREEN not met** (CI-08).
- CI-14 plan (DEC-062): **PLAN COMPLETE** ? frontend major inventory + safe vs STOP; companion [`DEC-062-CI-14-FRONTEND-DEPS-PLAN.md`](decisions/DEC-062-CI-14-FRONTEND-DEPS-PLAN.md).
- CI-14 Slice 1 (DEC-063): **COMPLETE** ? `overrides.sharp >=0.35.0`; lock **sharp 0.34.5 ? 0.35.3** under next **15.5.22** (react/eslint/jest unchanged). Evidence: `npm ls sharp` + `npx tsc --noEmit` exit 0. Validation **light validated**. Story later **CLOSED** DEC-108. **CI GREEN not met**.
- CI-14 Slice 2 STOP (DEC-065): ESLint **9 ? 10** initially STOPPED (silent major) ? **superseded** by DEC-072 EXECUTE PASS @ `240f9a8` (eslint **10.8.0** + Stage 1 lint green).
- CI-14 Slice 2 PASS (DEC-072 @ `240f9a8`): eslint **10.8.0** + eslint-config-next **15.5.22**; Docker Stage 1 lint+prettier exit 0. Validation **build validated**.
- CI-14 Slice 3 STOP (DEC-100): Jest **29 ? 30** STOPPED ? silent major; no jest **29.x** patch beyond **29.7.0**; host `npm audit` **0** (no security driver). **No package/lock land.** Companion [`DEC-100-CI-14-SLICE-3-JEST-STOP.md`](decisions/DEC-100-CI-14-SLICE-3-JEST-STOP.md). R-18 **Closed**. Next: dedicated Jest 30 evidence (Stage 3 **0** fail) **or** executive CI-14 AC close. Validation **light validated**. **CI GREEN not met**.
- CI-14 executive AC close (DEC-108): **CLOSED** - revised AC: sharp + eslint 10 + audit **0** = security modernization complete; Jest 30 optional backlog (not close gate). Companion [`DEC-108-CI-14-EXECUTIVE-AC-CLOSE.md`](decisions/DEC-108-CI-14-EXECUTIVE-AC-CLOSE.md). No package/lock land. Validation **docs only / light validated**. **whole-pipeline CI GREEN not met** (CI-08).
- S04-01 status detail (CLOSED): RC1 (`:id::uuid` bind bug) and RC2 (cross-loop pool reuse) fixed and proven; 7th test was blocked on R-19 (analytics ORM?DB drift), resolved by CI-15 migration `07e3ec4084fc`; suite now 7/7 PASS ? see DEC-020/DEC-021/DEC-022.
- CI-15 Phase 1 (approved ACs, DEC-021): analytics-only scope; systemic drift OUT of scope, registered as R-20 with Program Story DB-05. Principle: **Local Story fixes Local Drift. Systemic Drift becomes a Program Initiative.** Phase 2 (DEC-022): pushed `4793b08`, CI run `30652813475` ? job matrix identical to baseline, no functional/test/RLS/integration regression; 10 new Ruff style violations from the migration file disclosed and transferred to the lint backlog (not fixed within CI-15).
- CI-02 Phase 1 (DEC-023): toolchain remediation only ? `pipx install poetry` added to `security-pip-audit`; security-scan `-f` parse error fixed; YAML valid; local proof: `poetry export` exit 0 (98 deps) ? `pip-audit` runs ? real findings (R-21). Principle ratified: **CI Stories fix CI Infrastructure, not application dependencies.**
- CI-10 (DEC-036): whole-tree Ruff remediation on `3801151` ? 3,611 violations ? 0 (ruff 0.4.10, `app/alembic/versions/` excluded per triage #2 AC); field-verify CI run `30670339985` ? **Backend Lint SUCCESS**; sibling jobs on same run still fail (MyPy, pip-audit, npm audit, Jest, Trivy fs) ? **CI GREEN not met**.
