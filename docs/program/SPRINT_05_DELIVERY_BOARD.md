# Sprint 05 ? Enterprise Delivery Board (Program)

> **Canonical living board** for the Sprint 05 program entry package delivered under the Enterprise Delivery Mode governance (Sprint 04 ? Sprint 05 transition package). Each story follows the governed cycle: pre-task package ? executive approval ? Phase 1 (implement + validate + local commit) ? Phase 2 (controlled push/CI evidence) ? executive close ? record updates (DECISION_LOG / RISK_REGISTER).
> Status values: `PENDING`, `IN PROGRESS`, `BLOCKED` (with reason), `REGISTERED` (standalone story created by executive decision), `COMPLETE`.

| ID | Story | Priority | Status | Blocked on / Notes |
|---|---|---|---|---|
| CI-01 | Deploy Production branch guard (`deploy.yml`) | P0 | COMPLETE | Closed DEC-017; commit `61e08d4`, run `30648063788`; R-16 closed |
| CI-08 | GHCR 403 (staging image push) | P0 | BLOCKED | Org-level GHCR access, outside repo scope; R-17. **DEC-104:** prefer Option A (Packages write); interim honesty D (code path vs full+publish); no unauthorized push soften. Stage 6 build proven @ `927276f` / `30690622307`; push 403 only |
| CI-11 | npm audit remediation (patch-only) | P0 | COMPLETE | Closed DEC-019; commit `060c946`, run `30649799993`; residual 30 high ? CI-14 |
| S04-04 | Railway R-14 closure | P0 | REOPENED | **REOPENED DEC-120** — DEC-016 security closure **CONTRADICTED** by Principal Audit Tier-1 ([`PRINCIPAL_AUDIT_2026-08-01_DEC016_RAILWAY_CI.md`](../audit/PRINCIPAL_AUDIT_2026-08-01_DEC016_RAILWAY_CI.md)). Deploy IDs `7d33a0bc` / `1328309a` + `APP_POSTGRES_*` still VERIFIED; runtime sessions = `postgres` (BYPASSRLS); prod policies=0; `salesos_app` SELECT companies **141221** w/o tenant → bypass-probe PASS claim **FALSE**; prod health image **3.1.0** (pre-APP_POSTGRES). Phase 0 R-14 **NO-GO**. Remediation A–E: [`DEC-120`](decisions/DEC-120-DEC016-RAILWAY-R14-CONTRADICTED.md). STORY-02-01 CLOSED (DEC-044) — do not reopen. **Rotate Postgres passwords** (tunnel exposure) — human/ops |
| S04-01 | Adversarial RLS suite `tests/integration/test_adversarial_rls.py` | P0 | COMPLETE | 7/7 PASS after CI-15 (07e3ec4084fc); closed DEC-021; uncommitted fixes committed with CI-15 |
| CI-02 | pip-audit (Poetry) in CI | P0 | CLOSED | Closed DEC-025; commits `b330d52`+`a4e880c`; re-run `30655650484` proves chain: Poetry 2.x ? plugin ? `poetry export` ? `pip-audit` ? **24 vulns / 4 packages** ? `--strict` exit 1 on findings (toolchain failure eliminated); Security Scan `pip-audit` SUCCESS (`30655650490`); residual findings ? R-21/CI-16 |
| CI-03 | docker-smoke env var (`GF_SECURITY_ADMIN_PASSWORD`) | P0 | CLOSED | Closed DEC-027; commit `83e703e`; run `30656335600` ? **full Docker E2E Smoke Test job SUCCESS** (Validate Compose File 1s ? Build 111s ? Start 79s ? Smoke Tests 30s); interpolation issue eliminated |
| CI-07 | MyPy/Ruff `cli/` path in CI (triage #1/#2 partial) ? remove non-existent `cli/` from `mypy`/`ruff` invocations | P1 | CLOSED | Closed DEC-032; commit `b0c0069`; CI run `30661932842` (contains CI-06 change; superseded the CI-07-only run via concurrency) ? Backend Types + Backend Lint logs **0** `cannot read file 'cli'` / **0** E902 (both previously present in every run); gates now fail only on real code debt (Ruff body 3,611 ? CI-10; mypy findings to classify) |
| CI-04 | Bandit SARIF format (triage #5/#10): install `bandit[sarif]` in both workflows | P1 | CLOSED | Closed DEC-028; commit `ce59efd`; runs `30658782384`/`30658782394` ? `Upload bandit results` **SUCCESS in both**; no `Path does not exist` for Bandit; surfaced pre-existing high/high finding B324 (weak MD5, `roles_permissions.py:40`) ? gate had been SKIPPED in baseline, now executes ? CI-17; semgrep upload failure separate ? CI-18 |
| CI-05 | Trivy SARIF category collision (triage #11): distinct `category` per `upload-sarif` in `secret-scan` | P1 | CLOSED | Closed DEC-029; commit `f34bef2`; run `30659372944` ? `secret-scan` **SUCCESS** (Upload Trivy results + Upload Trivy config results PASS); 0 `Aborting upload` messages; only collision in repo was the Trivy pair |
| CI-06 | Deploy Production commit-comment permissions (triage #14) ? add `permissions: contents: write` | P1 | CLOSED | Closed DEC-033; commit `20f88bc`; Deploy Production run `30661932918` ? `Deploy Notification` **SUCCESS**, commit comment actually created by `github-actions[bot]` on `20f88bc` (0 HttpError/403); job-level least-privilege scope (notify only); remaining run failures are pre-existing blocked items (Deploy Blue GHCR 403 ? CI-08/R-17; Rollback SSH ? CI-09) |
| CI-13 | Jest suite baseline | P1 | CLOSED | Closed DEC-035; baseline captured from real CI run `30664173050` (commit `d48cc80`) - 33/194 suites failing (163 failed / 1 skipped / 2092 passed / 2256 total), full failing-suite inventory + root-cause categories in `salesos/docs/audit/ga-engineering-audit/JEST_BASELINE.md`; dependency contract for CI-14: failing suites <= 33, no new failures beyond inventory; remediation of the 33 suites = Sprint 01 Jest-debt story (not CI-14) |
| S04-05 | Adversarial write-protection tests | P1 | COMPLETE | Closed DEC-039; commit `8699796` ? suite `tests/integration/test_adversarial_write_protection.py` on master; validation **build validated** ? Docker `pytest tests/integration/test_adversarial_write_protection.py` ? **8/8 PASS in 4.88s**; **CI GREEN not met** |
| S04-06 | Adversarial suite (remaining) | P2 | COMPLETE | Closed DEC-045; commit `119df9e` ? suite `tests/integration/test_adversarial_rls_remaining.py` on master (7 tables: contacts, company_features, commercial_opportunities, opportunities, tasks, tenant_configs, webhook_subscriptions); validation **build validated** ? Docker pytest **15/15 PASS**; POLICY_COUNT **47** intact (DEC-044); S04-01/S04-05 unchanged; RLS inventory **not** reopened; **CI GREEN not met** |
| CI-12 | Trivy filesystem scan silent failure (triage #3) ? investigate/verbose reproduce before fix | P2 | CLOSED | Closed DEC-034; root cause reproduced locally on clean `git archive` snapshot with exact CI flags ? `--format sarif --output <file>` suppresses the findings table (stdout shows only npm/poetry detecting lines, exit 1); gate split into visible blocking table (exit-code 1) + SARIF export leg (exit-code 0, upload preserved); 11 real HIGH surfaced: poetry.lock 10 (ecdsa CVE-2024-23342 ? CI-16/R-21) + package-lock.json 1 (sharp GHSA-f88m-g3jw-g9cj ? CI-14); gate stays red while those exist, now visibly |
| CI-09 | VPS SSH/secrets provisioning | P2 | BLOCKED | Ops-side secret provisioning; R-17 |
| CI-10 | Backend Lint (Ruff) remediation ? 3,611 violations body (triage #2) | P2 | CLOSED | Closed DEC-036; commit `3801151`; local ruff 0.4.10 pass (3,611?0, `app/alembic/versions/` excluded); CI run `30670339985` ? **Stage 1: Backend Lint SUCCESS**; overall CI workflow still red (MyPy, pip-audit, npm audit, Jest, etc.) ? **CI GREEN not met** |
| compose prod name fix | docker-compose production project name (`name: salesos-prod`) | P2 | CLOSED | Closed DEC-037; commit `cb707be`; validation light ? `docker compose config --quiet` fails on missing env secrets (expected); YAML `name:` valid; resolves implicit project-name collision with dev stack (DEC-015 hazard) |
| CI-20 | Backend Types remediation ? 308 mypy errors (triage #1 body, post CI-07) | P2 | CLOSED | Surfaced by CI-10 field-verify run `30670339985` (Backend Lint SUCCESS, Backend Types FAILURE); **Phase 1 COMPLETE** at `65296174` ? `app/modules/admin` **34 ? 0**; **Phase 2 COMPLETE** at `01b6a8ae` ? `app/modules/company` **25 ? 0**; **Phase 3 COMPLETE** at `e5b4471` ? `app/modules/entity_resolution` **14 ? 0**; **Phase 4 COMPLETE** at `4b3a436` ? `app/modules/identity` **16 ? 0**; **Phase 5 COMPLETE** at `7d8126e` ? `app/modules/revenue_execution` **10 ? 0**; **Phase 6 COMPLETE** at `38127af` ? `app/modules/sso` **8 ? 0**; **Phase 7 COMPLETE** at `802cde5` ? `app/routers` **44 ? 0**; **Phase 8 COMPLETE** at `3e7fadb` ? `app/main.py` **15 ? 0** + `sdk/` **31 ? 0** (host light: main **3 ? 0**, sdk **33 ? 0**); **Phase 9 COMPLETE** at `821aad5` ? `app/modules/demo_mode` **11 ? 0**; **Phase 10 COMPLETE** at `ca76f9c` ? `app/modules/communication_hub` **11 ? 0**; **Phase 11 COMPLETE** at `86b4094` ? `app/modules/work_intelligence` **5 ? 0**; **Phase 12 COMPLETE** at `e44b7f3` ? `app/boot/startup.py` **7 ? 0** + `app/database.py` **7 ? 0**; **Phase 13 COMPLETE** at `5edb6d6` ? `app/application` **9 ? 0**; **Phase 14 COMPLETE** at `1f14337` ? SSO remnants **11 ? 0**; **Phase 15 COMPLETE** at `7fed3dc` ? sla_monitor+redis_client **5 ? 0**; **Phase 16 COMPLETE** at `26156df` ? `app/startup.py` **4 ? 0**; **Phase 17 COMPLETE** at `d5e4de2` ? `app/modules/decision` **5 ? 0**; **Phase 18 COMPLETE** ? `app/modules/entity_resolution` remnants **10 ? 0**; overall expected **~308 ? ? ? ~36 ? ~26** (field **~41 ? ~31**); Phases 1?21 COMPLETE; **CLOSED DEC-096** ? field Backend Types **0** on tip `220d91a` (run `30684023356` / job `91326366120` SUCCESS); tip corroboration `af4835f` run `30684308678` / job `91327119501` SUCCESS). Path: Phase 21 `17c1eee` ? syntax `5588bb7` ? audit imports `a636c69` ? `getattr` registry `220d91a`. DEC-085 `set_config` intact. Owner Backend Lead; **whole-pipeline CI GREEN not met** |
| CI-14 | Frontend Dependency Modernization | P1 | CLOSED | **CLOSED DEC-108** (executive AC). Security modernization MET: Slice 1 sharp **0.35.3** (DEC-063 @ `435ba5d`); Slice 2 eslint **10.8.0** (DEC-072 @ `240f9a8`); host npm audit **0**; R-18 **Closed**. Slice 3 Jest 29->30 **STOPPED** (DEC-100) - optional backlog, not close gate. No silent Jest major. Not CI-08/CI-22/Railway. **whole-pipeline CI GREEN not met** |
| CI-15 | Analytics Schema Reconciliation (add `metrics`, `dimensions`, `filters`, `visualization_type`, `created_by` to `analytics_reports` via Alembic) | P0 | CLOSED | Closed DEC-022; migration `07e3ec4084fc`; Phase 1 validation PASS + Phase 2 push `4793b08` ? CI run `30652813475` (matrix identical to baseline `30649799993`; only delta = 10 new Ruff style violations from the migration file in the pre-existing red Backend Lint gate, transferred to the lint backlog, not fixed in CI-15); R-19 closed |
| DB-05 | Repository Schema Reconciliation Program - systemic ORM/DB drift (R-20) | P1 | IN PROGRESS | **Slice 1 COMPLETE (DEC-113)** - additive CREATE for all **8** R-09 P0 tables; head `b8d4f02a1c06`. Slice 0 inventory DEC-111 retained. Still **no RLS** on deferred-8; no prod migrate / no Prisma. Companion [`decisions/DEC-113-DB-05-SLICE-1-P0-CREATE.md`](decisions/DEC-113-DB-05-SLICE-1-P0-CREATE.md). **CI GREEN not met** |
| CI-16 | Backend Dependency Security Remediation ? remediate `python-multipart`, `strawberry-graphql`, `starlette`, `ecdsa` vulnerabilities found by `pip-audit` (R-21) | P1 | CLOSED | Standalone, DEC-023; NOT part of CI-02. **Slice 1 COMPLETE** at `1e73a2f` (DEC-051): multipart **0.0.9 ? 0.0.32**. **Slice 2 BLOCKED ? CI-22** (DEC-052/DEC-054; Phase 1 DEC-081 cleared starlette floor). **Slice 3 COMPLETE** at `d3f1eef` (DEC-056): strawberry **0.243.1 ? 0.323.2**. **ecdsa:** Accepted residual **Option A** (DEC-057). **CI policy (DEC-090):** named `--ignore-vuln PYSEC-2026-1325` on CI Stage 5 pip-audit; `--strict` retained. **Trivy (DEC-098):** named `CVE-2024-23342` in `.trivyignore` (same residual; evidence `30684813480`). Package [`DEC-057-CI-16-ECDSA-DISPOSITION.md`](decisions/DEC-057-CI-16-ECDSA-DISPOSITION.md). **Stage 5 pip-audit + Secrets Scan expected green**; **whole-pipeline CI GREEN not met** |
| CI-17 | Bandit B324 remediation ? weak MD5 in `roles_permissions.py:40` (`hashlib.md5` used to derive `role_id`; add `usedforsecurity=False`) | P1 | CLOSED | Closed DEC-031; commit `b87fb22`; run `30661609555` ? **Stage 5: Bandit SAST fully GREEN** (incl. fail-on-high gate, 0 high findings); digest byte-identical, 45 admin tests pass; no regression |
| CI-18 | Semgrep SARIF upload ? fix `--severity ERROR,WARNING` (invalid comma list ? repeatable flags) in `sast-scan` | P2 | CLOSED | Closed DEC-030; commit `6ab1d0e`; run `30660116232` ? **entire Security Scan workflow GREEN for first time** (secret-scan, sbom, sast-scan, pip-audit, npm-audit, report all SUCCESS); `Upload semgrep results` PASS, 0 CLI parse errors; semgrep surfaced **253 blocking findings** (2806 files, 595 rules) ? CI-19 |
| CI-19 | Semgrep findings remediation ? 253 blocking findings surfaced by CI-18 run 30660116232 (2806 files, 595 rules); now visible in GitHub code scanning | P2 | CLOSED | Triage + Waves 1/3/4/5 COMPLETE; Wave 2 Slice 1?6 + field-verify PARKED (DEC-103). **DEC-105 CLOSED with residual** + **field-verify** Security Scan [`30693735860`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30693735860) / sast `91352893256` @ `b9062d6`: Semgrep CLI **11** blocking; CS open Semgrep **11** alembic-only; burned **8** **fixed** (`fixed_at=2026-08-01T09:27:31Z`); unexpected non-alembic **0**. Risk **R-24** Closed. Semgrep gates unchanged. **CI GREEN not met** (CI-08) |
| CI-21 | Gitleaks JWT fixture leak ? `test_post_middleware.py` jwt.io sample Bearer token tripped secret-scan (`leaks found: 1`) | P1 | CLOSED | Closed DEC-041; commit `b03ffbf`; Security Scan run `30671556546` ? **entire workflow SUCCESS** (secret-scan Gitleaks PASS); fixture ? `Bearer test-fake-token-not-a-jwt`; no allowlist/global disable; **CI GREEN not met** (main CI workflow separate) |
| CI-22 | FastAPI / Starlette / Pydantic modernization program ? scoped cascade to clear starlette `pip-audit` (floor ?1.3.1) via FastAPI ~0.135+ and pydantic ?2.9 | P1 | CLOSED | **CLOSED DEC-109**. Phase 1 (DEC-081 @ `442af64`): fastapi **0.141.1** / starlette **1.3.1** / pydantic **2.13.4**. Field pip-audit `30688863161` @ `3084e5b`: **NO starlette** (`No known vulnerabilities found, 1 ignored` = ecdsa DEC-090). Unit RBAC override field-green same run. ecdsa residual ? DEC-057 (not CI-22). **whole-pipeline CI GREEN not met** (CI-08) |

## Progress
- **Category B Slice B6 (2026-08-01, DEC-118):** **CLOSED** (execution). Join RLS on `webhook_deliveries` via `webhook_subscriptions` (`a6d1e54f2e06`, down `f5c0d43e1d05`; String(36) FK). Live `POLICY_COUNT` **58**. Deferred-8 `webhook_endpoints` untouched (no ENABLE RLS). DEC-085 untouched. B7 READY. Companion [`decisions/DEC-118-CATEGORY-B6-WEBHOOK-DELIVERIES-RLS.md`](decisions/DEC-118-CATEGORY-B6-WEBHOOK-DELIVERIES-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B5 (2026-08-01, DEC-117):** **CLOSED** (execution). Join RLS on `password_reset_tokens` + `refresh_token_families` via `users` (`f5c0d43e1d05`, down `e4b9c32d0c04`; UUID FK). Live `POLICY_COUNT` **57** (superseded to **58** by DEC-118). Auth-path careful: own-tenant + fail-closed; no permissive bypass. DEC-085 untouched. Companion [`decisions/DEC-117-CATEGORY-B5-IDENTITY-TOKEN-CHILDREN-RLS.md`](decisions/DEC-117-CATEGORY-B5-IDENTITY-TOKEN-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B4 (2026-08-01, DEC-116):** **CLOSED** (execution). Join RLS on `decision_center_audits` + `decision_center_feedback` via `decision_center_decisions` (`e4b9c32d0c04`, down `d3f8a21c9b03`; `p.id::text` cast for UUID/varchar FK). Live `POLICY_COUNT` **55** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-116-CATEGORY-B4-DECISION-CENTER-CHILDREN-RLS.md`](decisions/DEC-116-CATEGORY-B4-DECISION-CENTER-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B3 (2026-08-01, DEC-115):** **CLOSED** (execution). Join RLS on `analytics_report_executions` + `analytics_report_shares` via `analytics_reports` (`d3f8a21c9b03`, down `c221d15f8b02`). Live `POLICY_COUNT` **53** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-115-CATEGORY-B3-ANALYTICS-CHILDREN-RLS.md`](decisions/DEC-115-CATEGORY-B3-ANALYTICS-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B2 (2026-08-01, DEC-114):** **CLOSED** (execution). Join RLS on `commercial_activities` + `commercial_quote_lines` via sessions/quotes (`c221d15f8b02`, down `b110c04e7a01`). Live `POLICY_COUNT` **51** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-114-CATEGORY-B2-COMMERCIAL-CHILDREN-RLS.md`](decisions/DEC-114-CATEGORY-B2-COMMERCIAL-CHILDREN-RLS.md). Validation **build validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B Slice B1 (2026-08-01, DEC-112):** **CLOSED** (execution). Join RLS on `branches` + `licenses` via `companies` (`b110c04e7a01`, down `b8d4f02a1c06` after DEC-113 rebase). Live `POLICY_COUNT` **49** (superseded to **58** by DEC-118). DEC-085 untouched. Companion [`decisions/DEC-112-CATEGORY-B1-COMPANY-CHILDREN-RLS.md`](decisions/DEC-112-CATEGORY-B1-COMPANY-CHILDREN-RLS.md). Validation **build validated** (pytest 7 passed). **Production GO not claimed. CI GREEN not met.**
- **DB-05 Slice 1 CREATE (2026-08-01, DEC-113):** **COMPLETE** (CREATE TABLE only). Revisions `a7c3e91f0b05` -> `b8d4f02a1c06` (head). Tables: admin_licenses/invoices/transactions/ai_costs/jobs; webhook_endpoints; scoring_scorecards; revenue_analytics_snapshots. Remaining P0 CREATE **0**. No RLS. DEC-085 untouched. Companion [`decisions/DEC-113-DB-05-SLICE-1-P0-CREATE.md`](decisions/DEC-113-DB-05-SLICE-1-P0-CREATE.md). Validation **light validated**. **Production GO not claimed. CI GREEN not met.**
- **DB-05 Slice 0 inventory (2026-08-01, DEC-111):** **COMPLETE** (inventory only). Alembic single head `065d1d3a466b`; ORM **80** / create_table **89**; P0 = **8** R-09 missing CREATE (DEC-110 deferred); P1 = emails/meetings UUID vs String(36) + companies/index/nullable clusters. Next: Slice 1 additive CREATE (no RLS yet). DEC-085 untouched. Companion [`decisions/DEC-111-DB-05-SCHEMA-DRIFT-INVENTORY.md`](decisions/DEC-111-DB-05-SCHEMA-DRIFT-INVENTORY.md). Validation **docs / light validated**. **Production GO not claimed. CI GREEN not met.**
- **Category B RLS planning (2026-08-01, DEC-110):** **CLOSED** (planning). Canonical inventory: Category A live **47**; Category B join **12**; Category A deferred **8** (DB-05). Execution slices B1?B7 READY ? no Alembic RLS SQL this land. DEC-085 untouched. Companion [`decisions/DEC-110-CATEGORY-B-RLS-INVENTORY.md`](decisions/DEC-110-CATEGORY-B-RLS-INVENTORY.md). Validation **docs / light validated**. **Production GO not claimed. CI GREEN not met.**
- **CI-22 executive close (2026-08-01, DEC-109):** Starlette floor **MET**; field pip-audit clear of starlette (`30688863161` / `91339902722` @ `3084e5b`; Stages 1-5 green corroboration `30689682988` @ `7ba137b`). Compatibility residuals burned (Request C3 + FastAPI 0.141 RBAC override). **CLOSED**. ecdsa = DEC-057 residual (not CI-22). Validation **build validated**. **whole-pipeline CI GREEN not met** (CI-08).
- **Contract tests slice 4 (2026-08-01, DEC-106):** `GET /api/v1/decisions` **401** (`DetailStringError` / `UnauthorizedError` string detail) + **422** (`HTTPValidationError` for `limit` bounds). Host `pytest tests/contract/ -m contract` ? **14 passed**. DEC-085 untouched. No invented ErrorResponse. Validation **light validated**. **CI GREEN not met** (CI-08).
- **CI-08 ops package (2026-08-01, DEC-104):** Recommended **Option A** (org/account GHCR Packages write for Actions). Interim **Option D** honesty: **CI GREEN (code path)** ? **CI GREEN (full incl. publish)**. Option B (build vs push / continue-on-error) **not authorized** without separate AC DEC. Option C (alternate registry) **rejected** ? docs/runbooks assume `ghcr.io/ragheeda-boop/salesos/*`. Workflow already has `packages: write` ? not a YAML omission. CI-08 stays **BLOCKED** pending human ops field push SUCCESS. Validation: **docs only / not validated** (pipeline). **whole-pipeline CI GREEN (full) not met**.
- **Stage 6 Frontend Docker install (2026-08-01):** Prior `30689682988` Frontend Build FAILED on eslint **10** peer vs `eslint-config-next@15.5.22` (Dockerfile omitted CI-14 Slice 2 `.npmrc` `legacy-peer-deps`). Fix `53fe214` copy `.npmrc`; follow-up `927276f` copy `scripts/ci14-stub-rushstack-eslint-patch.js` for postinstall. Field re-run `30690622307` / job `91345633450` @ `927276f`: Docker **npm install DONE** + **next build DONE** (73.6s); job **FAILED** only on GHCR push **403** (same CI-08/R-17 as Backend). Backend Stage 6 job `91345633439`: image build OK, push **403**. Stage 7 E2E **skipped** (needs both Stage 6 green). No `--force`; DEC-100 Jest STOP intact; DEC-085 untouched. Validation **build validated** (image build proven; push ops-blocked). **whole-pipeline CI GREEN not met** (CI-08).
- **Stage 4 Integration xdist (2026-08-01):** Prior run `30689313205` / job `91341467519` FAILED ? `pytest: error: unrecognized arguments: -n` (workflow `-n auto`, Poetry lacked `pytest-xdist`). Fix `7ba137b`: add `pytest-xdist` ^3.6 to Poetry `dev` (+ lock/execnet). Field verify run `30689682988` / job `91342461119` **SUCCESS** ? **2700 passed** / 2 skipped (xdist gw0?gw3). Stages 1?5 green on same run. Stage 6 **both FAILED** (new cluster): Backend Build push GHCR **403** (CI-08/R-17); Frontend Build Docker `npm install` peer conflict eslint **10** vs `eslint-config-next@15.5.22` (needs `^7||^8||^9`; Dockerfile omits `.npmrc` legacy-peer-deps). Stage 7 E2E **skipped**. DEC-085 `set_config` untouched. Validation **build validated**. **whole-pipeline CI GREEN not met**.
- **Frontend Unit burn (2026-08-01):** Field Stage 3 @ **3084e5b** run `30688863161` / job `91339981968` ? **152 failed** / 44 passed suites (413 tests passed; suites never loaded). Root cause: CI-14 Slice 2 `.npmrc` `legacy-peer-deps=true` omits peer `@testing-library/dom` of `@testing-library/react` 16.x; prior direct pin `f0b0d6a` dropped by Slice 2 land `240f9a8`. Fix `36e59f5`: re-add `@testing-library/dom` ^10.4.1 as direct `devDependency` + lock. Field verify @ tip **36e59f5** run `30689313205` / job `91341151533` ? **196/196 suites passed**, **2279 passed** / 1 skipped. **Not R-23 reopen** (install/peer, not Jest assertion debt). Validation **build validated**. **whole-pipeline CI GREEN not met** (downstream stages may still run/fail).
- **Backend Unit burn-down follow-on (2026-08-01):** Field Unit @ tip **3084e5b** run `30688863161` / job `91339985115` ? **0 failed** / 2700 passed / 2 skipped (prior **19** @ e7a18a9/91336593923; baseline **85** @ 6077af8). Root cause: FastAPI **0.141** `_IncludedRouter` hid route dependants ? RBAC `NotFoundError` user 404 (not missing mount); `abd7e03` mount assert ERRORed on `app.routes`. Fix walks `wr.router.routes` for `_require_permission` overrides. Workflow cluster cleared. DEC-085 intact. Validation **build validated**. Frontend Unit still red on same run; **whole-pipeline CI GREEN not met**.
- **R-26 / DEC-085 HARD STOP (2026-08-01 @ `d0e80f9`):** `get_db()` must use `SELECT set_config('app.tenant_id', :tenant_id, true)` ? **never** `SET LOCAL ? = :tenant_id`. Parallel agents reintroduced SET LOCAL a **4th** time; restored + pushed. Host RLS suites **30/30 PASS**. Do not revert for mypy/ruff/style. **CI guard:** `tests/unit/test_dec085_set_config_guard.py` fails if `get_db` reintroduces `SET LOCAL` for tenant GUC.
- **DEC-120 Railway R-14 reopen (2026-08-01):** Principal Audit Tier-1 **CONTRADICTED** DEC-016 security closure. S04-04 **REOPENED**; R-14 Railway **REOPENED**; Phase 0 (DEC-008 / R-14) exit = **NO-GO** (DEC-086 Phase 0 GO **withdrawn**). Dual honesty: env provision ≠ runtime RLS. Evidence: [`PRINCIPAL_AUDIT_2026-08-01_DEC016_RAILWAY_CI.md`](../audit/PRINCIPAL_AUDIT_2026-08-01_DEC016_RAILWAY_CI.md); companion [`DEC-120`](decisions/DEC-120-DEC016-RAILWAY-R14-CONTRADICTED.md). **Production GA = NO-GO.** Remediation READY: A (wiring=`5e7023f`) → B (image promote; GHCR alt) → C (alembic) → D (`salesos_app` runtime) → E (bypass-probe + `pg_stat_activity`). **Rotate Postgres passwords** — human/ops. Validation **docs / light validated**.
- **Phase 0 Architecture Validation (2026-08-01, DEC-086):** **SUPERSEDED for R-14 GO by DEC-120.** Historical: S04-04 CLOSED under DEC-016 at `7232979`. **Current:** Phase 0 (DEC-008 RLS / R-14) exit = **NO-GO**. **STORY-02-01 CLOSED** (DEC-044 @ **47**) — **do not reopen**. **Production GA / External pilot = NO-GO**. **CI GREEN not met**. Remaining board **P0:** **S04-04** (REOPENED) + CI-08 (GHCR, BLOCKED). See `EXECUTION_DAG.md`.
- Complete/Closed: **24/25** (CI-01, CI-11, S04-01, CI-15, CI-02, CI-03, CI-04, CI-05, CI-18, CI-17, CI-07, CI-06, CI-12, CI-13, CI-10, compose prod name fix, S04-05, CI-21, S04-06, **CI-16**, **CI-20**, **CI-19**, **CI-14**, **CI-22**). **S04-04 REOPENED (DEC-120).** Adjacent closed (not board fraction): **Jest-debt / R-23** (DEC-077); Category B planning (DEC-110). **Phase 0 critical-path blocked:** **S04-04 / Railway R-14**. Also blocked (ops): CI-08 (P0), CI-09 (P2). In progress: **DB-05** (Slice 0 DONE DEC-111; Slice 1 DONE DEC-113; next Slice 2 type drift). Backlog READY/PARALLEL: Category B execution B7 (B1–B6 CLOSED); optional Jest 30 evidence (not CI-14 reopen); S04-04 remediation slices A–E. Pending: none.
- STORY-02-02 browser/E2E verify (DEC-095): **DONE / CLOSED** ? live Next `next dev` unauthenticated redirect probe **PASS**: `GET /dashboard` ? **307** `location: ?/login?callbackUrl=%2Fdashboard`; `GET /login` + `GET /` ? **200**. Prior DEC-088 Jest **14/14**. Optional authenticated Playwright/`smoke-ui.ps1` **not run**. Validation **browser-validated** (redirect AC only). **Production GO not claimed. CI GREEN not met.** See [`decisions/DEC-095-STORY-02-02-BROWSER-VERIFY-CLOSED.md`](decisions/DEC-095-STORY-02-02-BROWSER-VERIFY-CLOSED.md). Supersedes DEC-085/DEC-088 PARTIAL.
- STORY-02-03 JWT suite / audience consumption (DEC-093): **CLOSED** ? Platform admin (/api/v1/admin via app/modules/admin) wires require_owner_role_dep -> verify_owner_token -> decode_owner_access_token (app/owner_auth.py). Tenant verify_token remains salesos-api only. Groundwork DONE (2379e5f). Host poetry run pytest tests/unit/test_jwt_audience_split.py -> **14/14 PASS** (**light validated**). DEC-091 consumption-OPEN superseded. **Production GO not claimed.**
- S04-05 (DEC-039): write-protection adversarial suite on master (`8699796`); validation **build validated** ? Docker `pytest` **8/8 PASS in 4.88s**. Related Sprint 03 landings on master (DEC-040): STORY-02-03 `2379e5f`, STORY-02-02 `3f4b3c8`, STORY-03-04 `623077c`, STORY-02-04 `932f722` (already on master). Card primitives `9577c98` ? related Jest-debt progress (foundation `card.tsx` for suite recovery); Jest-debt later **CLOSED** DEC-077; **CI GREEN not met**.
- S04-06 (DEC-045): remaining adversarial RLS suite on master (`119df9e`); file `tests/integration/test_adversarial_rls_remaining.py` ? 7 tables; validation **build validated** ? Docker pytest **15/15 PASS**; POLICY_COUNT **47** intact; S04-01/S04-05 unchanged; RLS inventory **not** reopened (DEC-044); **CI GREEN not met**.
- Jest-debt / R-23 (DEC-077): **CLOSED** ? ?10 field verify `30679804383` / `465c638` (Stage 3 job `91314523292`) = **0** failing suites (196/196; 2278 passed / 1 skipped / 2279); Stage 1 Frontend Lint **success**; Stage 3 Frontend Unit Tests **success**. Path: `4fdc1d8` + `5bba606` + `556304d` + Prettier Onboarding gate `11470b1`. Supersedes ?8 **11**. **Whole-pipeline CI GREEN not met** (Backend Lint/Types + Stage 5 / security still red).
- CI-19 Wave 1 (DEC-043): **COMPLETE** at `d5c9b57` ? 8 GHA `run-shell-injection`/`github-script-injection` alerts remediated via `env:`/`process.env` (deploy workflows + legacy `sales-os` run.yml).
- CI-19 Wave 3 SHA-pin (DEC-069): **COMPLETE** at `556304d` ? 115 `uses:` Action refs pinned to full commit SHAs in `.github/workflows/{security-scan,ci,docker-smoke,deploy,deploy-staging,deploy-production}.yml` + `sales-os/.github/workflows/run.yml`; `trivy-action@master` ? `v0.29.0` SHA; Semgrep severity/upload path **unchanged** (no gate weaken). Wave 2 SKIPPED/deferred.
- CI-19 Wave 3 residual (DEC-074): **COMPLETE** at `465c638` ? 15 K8s container `securityContext.allowPrivilegeEscalation: false` (`salesos/infra/k8s/`); app images also `runAsNonRoot` + drop ALL caps; Dockerfile USER on backup + alertmanager images; DynamoDB SSE + Secrets Manager CMK in `salesos/infra/terraform/main.tf`. Architecture STOP: data-store pods omit `runAsNonRoot` (official image entrypoint/volume UID risk). Triage: `CI_19_SEMGREP_TRIAGE.md`. Risk **R-24** still Open. Validation: **light validated** (manifest inventory vs Code Scanning alert paths #591?#609). **CI GREEN not met**.
- CI-19 Wave 3 residual field-verify (Security Scan / Semgrep / Trivy): run 30679804395 @ 465c638 ? Semgrep (sast-scan) **SUCCESS**; Trivy/sbom **FAILURE** at setup (setup-trivy@v0.2.2 unresolvable under 	rivy-action v0.29.0 pin) ? **pre-existing pin residual** (same on 30679776654 / 30679589739); no manifest fix from DEC-074. Tip corroboration 30680090950 @ 95041f Security Scan **success** after pin bump. **CI GREEN not met**.
- CI-19 Wave 4 (DEC-076): **COMPLETE** ? repo-root `.semgrepignore` path excludes for out-of-GA noise (`taqeem_scraper/`, root scrape JSON, `sales-os/`, `crm_pipeline.py`, `salesos/backend/demo/`, `salesos/frontend/scripts/`, `salesos/scripts/`) + redact `PILOT_SECRETS_GUIDE.md` to `CHANGE_ME_*`. Semgrep ERROR/WARNING severity + SARIF upload **unchanged**. Does **not** exclude `salesos/backend` app/runtime or product FE packages. Wave 2 still SKIPPED/deferred. Validation: **light validated**. **CI GREEN not met**.
- CI-19 Wave 4 follow-up (DEC-083): **FIXED** ? root `.semgrepignore` had replaced Semgrep built-ins ? live open findings regression **~116 ? ~130**. Fix prepends upstream defaults (`tests/`, `node_modules/`, `.venv/`, `build/`, `dist/`, VCS dirs, package-manager caches, etc.) then keeps DEC-076 Wave 4 excludes. Severity gates **unchanged**. Does **not** claim finding-zero GREEN. Validation: **light validated**. **CI GREEN not met**.
- CI-19 Wave 5 (DEC-082): **COMPLETE** ? residual singletons cleared with real fixes (8 alerts): Cobertura regex parse (no `xml.etree`); pentest brief `wss://`; `website_li_pipeline.py` ? `http.client`; FE forms `pattern?: RegExp`; search-highlight / session-test without `new RegExp`; `StateRuntime` prototype-pollution guards. Architecture STOP: none. Semgrep gates unchanged. Validation: **light validated**. **CI GREEN not met**.
- CI-19 Wave 2 Slice 3 (DEC-099): **COMPLETE** ? Core honesty for postgres_repo (6) + 	imeline_runtime (5). Expected clear **11**; remainder ~**32**. Search timeout uses set_config (not SET LOCAL); get_db DEC-085 untouched. Narrow pytest **50 passed**. **CI-19 OPEN.** **CI GREEN not met**.
- CI-19 Wave 2 Slice 4 (DEC-101): **COMPLETE** ? Core honesty for search_runtime (4) + sdk/search (3) + vector_store (3) + tasks (3) + contact search_repository (1); admin_router local. Expected clear **14**; remainder ~**18**. Timeouts via set_config; get_db DEC-085 untouched. Narrow pytest **14 passed**. **CI-19 OPEN.** **CI GREEN not met**.
- CI-19 Wave 2 Slice 5 (DEC-102): **COMPLETE** ? Core honesty for activity_runtime (3) + kg service (2) + memory postgres_store (2) + kg sql_repository full Core. Expected clear **7**; remainder ~**11** (alembic RLS out-of-scope; init_db/benchmark/mcp). get_db DEC-085 untouched. Narrow pytest **58 passed**. **CI-19 OPEN.** **CI GREEN not met**.
- CI-19 Wave 2 Slice 6 (DEC-103): **COMPLETE** ? residual package: init_db DDL exec_driver_sql (1) + benchmark (2) + mcp Core (1); **alembic RLS accepted residual (7)** ? do not churn. Wave 2 app-honesty COMPLETE. get_db DEC-085 untouched. Narrow pytest DEC-085 **2 passed**. **CI-19 OPEN**. **No Slice 7**. **CI GREEN not met**.
- CI-19 Wave 2 field-verify (post DEC-103): Security Scan run `30686789458` @ tip `abaae85` (includes land `3d49ae1`) **SUCCESS**; sast-scan job `91334080531` **SUCCESS**. Live CS: app `avoid-sqlalchemy-text` **0** (#513/#523/#524/#542 **fixed**); alembic residual **7** text + **4** raw/formatted on `0020`. Wave 2 **PARKED COMPLETE**. **No Slice 7**. **CI-19 remains OPEN** (executive residual-close; non-SQL Semgrep leftovers). Validation: **build validated** (field CS + Security Scan). **CI GREEN not met**.
- CI-19 executive residual-close (DEC-105): **CLOSED with residual**. Pre-land open Semgrep OSS **19** @ `a02c8f1`. Burned **8** non-alembic (logger x4 identity+scraper; StateRuntime prototype helpers; triage-doc WS FP; DynamoDB lock-table CMK; remove unused workflow-level `SLACK_WEBHOOK`). Accepted residual **11** alembic (DEC-103 ? do not churn). R-24 Closed ? mitigating residual. Semgrep ERROR/WARNING + SARIF **unchanged**. DEC-085 untouched. **Field-verify (2026-08-01):** Security Scan [`30693735860`](https://github.com/ragheeda-boop/SalesOS/actions/runs/30693735860) **SUCCESS** / sast-scan `91352893256` **SUCCESS** @ tip `b9062d6` ? Semgrep CLI **11** (11 blocking, alembic-only); live CS Semgrep open **11** (`0afbf3e6ae53` x4 + `065d1d3a466b` x3 text + `0020` raw/formatted x4); burns **fixed** #417/#515/#516/#546/#547/#608/#834/#835 (`fixed_at=2026-08-01T09:27:31Z`); app unexpected **0**. Do **not** reopen CI-19. Validation: **build validated**. **CI GREEN not met** (CI-08).
- CI-21 (DEC-041): Gitleaks false-positive closed on `b03ffbf` ? jwt.io sample JWT replaced with non-JWT Bearer stub in `test_post_middleware.py`; field-verify Security Scan run `30671556546` **SUCCESS** (Gitleaks step PASS; all jobs green); no `.gitleaksignore` / allowlist / global disable; **does not claim full CI green**.
- compose prod name fix (DEC-037): commit `cb707be` adds `name: salesos-prod` to `docker-compose.prod.yml`; validation light ? YAML name valid; full `config --quiet` blocked by missing production env secrets (not a compose-name defect).
- CI-20 (DEC-038 register; DEC-046?050 Phases 1?5; DEC-053 Phase 6; DEC-055 Phase 7; DEC-058 Phase 8; DEC-059 Phase 9; DEC-060 Phase 10; DEC-061 Phase 11; DEC-064 Phase 12; DEC-066 Phase 13; DEC-068 Phase 14; DEC-071 Phase 15; DEC-070 Phase 16; DEC-078 Phase 17; DEC-079 Phase 18; DEC-080 Phase 19; DEC-084 Phase 20; DEC-092 Phase 21): Backend Types gate baseline **308 mypy errors** on CI run `30670339985`. **Phase 1?9** as previously recorded (overall expected **~308 ? ~100**; field-verify `30677025355` **104** / 0 demo_mode). **Phase 10 COMPLETE** at `ca76f9c` ? module `app/modules/communication_hub` **11 ? 0**; overall expected **~100 ? ~89** (field **104 ? ~93**); field-verify `30677381493` **93** / 0 hub. **Phase 11 COMPLETE** at `86b4094` ? module `app/modules/work_intelligence` **5 ? 0**; overall expected **~89 ? ~84** (field **93 ? ~88**); field-verify `30677732725` **88** / 0 WI. **Phase 12 COMPLETE** at `e44b7f3` ? `app/boot/startup.py` **7 ? 0** + `app/database.py` **7 ? 0**; overall expected **~84 ? ~70** (field **88 ? ~74**). **Phase 13 COMPLETE** at `5edb6d6` ? `app/application` **9 ? 0** (`cast` / `BaseException` narrow / `Request | None`); overall expected **~70 ? ~61** (field **~74 ? ~65**). **Phase 14 COMPLETE** at `1f14337` ? SSO remnants **11 ? 0**; overall expected **~61 ? ~50** (field **~65 ? ~54**); field-verify `30679062993` **54** / **0** SSO (baseline `30678653664` **65** / **11** SSO; tip `30679150001` **54** / 0 SSO). **Phase 15 COMPLETE** at `7fed3dc` ? sla_monitor+redis_client **5 ? 0**; overall expected **~46 ? ~41** (field **~50 ? ~45**). **Phase 16 COMPLETE** at `26156df` ? `app/startup.py` **4 ? 0**; overall expected **~50 ? ~46** (field **~54 ? ~50**). **Phase 17 COMPLETE** at `d5e4de2` ? `app/modules/decision` **5 ? 0**; overall expected **~41 ? ~36** (field **~45 ? ~41**); host mypy decision `--follow-imports=skip` **0**. **Phase 18 COMPLETE** ? `app/modules/entity_resolution` remnants **10 ? 0**; overall expected **~36 ? ~26** (field **~41 ? ~31**); host mypy entity `--follow-imports=skip` **0**. **Phase 19 COMPLETE** ? `app/modules/notion_sync` **4 ? 0** + `app/graphql` **4 ? 0**; overall expected **~26 ? ~18** (field **~31 ? ~23**); host mypy notion+graphql `--follow-imports=skip` **0**. **Phase 20 COMPLETE** at c4fb067 ? webhooks+identity hosts+employee_360 **8 ? 0**; overall expected **~18 ? ~10** (field **~23 ? ~15**); host mypy skip **0**. **Phase 21 COMPLETE** (DEC-092) - residual clusters -> host CI-equivalent mypy **0**. **CLOSED DEC-096** - field Backend Types **0** on tip `220d91a` (CI run `30684023356`, job `91326366120` SUCCESS; 0 `error:` lines). Tip corroboration `af4835f` (`30684308678`/`91327119501`) and `844548e` (`30684181874`/`91326794076`). Post-P21 clears: `5588bb7` (syntax), `a636c69` (audit imports), `220d91a` (`getattr` EVENT_REGISTRY). DEC-085 `set_config` intact. Validation **build validated** (field Types). Story **CLOSED**. **Whole-pipeline CI GREEN not met**.
- CI-16 Slice 1 (DEC-051): `python-multipart` bumped **0.0.9 ? 0.0.32** at `1e73a2f` (`pyproject.toml` constraint `>=0.0.27,<0.1.0`; Poetry lock update of that package only). Smoke: `poetry run` import `multipart` + `app.main` ? FastAPI OK (**light validated**). Explicitly **not** bumped at that time: `strawberry-graphql`, `starlette`, `ecdsa`. **CI GREEN not met**.
- CI-16 Slice 2 STOP (DEC-052 at `8323c84`): starlette bump blocked ? no pip-audit-clean version under `fastapi ^0.111` / `pydantic <2.9`. Follow-on **CI-22** registered (DEC-054) for FastAPI/Starlette/Pydantic modernization; **not** CI-16 slice work. **CI GREEN not met**.
- CI-16 Slice 3 (DEC-056): `strawberry-graphql` bumped **0.243.1 ? 0.323.2** at `d3f1eef` (`pyproject.toml` constraint `>=0.315.7,<1.0.0`; Poetry lock update; transitive `cross-web 0.7.0`). FastAPI/starlette/pydantic/multipart/ecdsa lock versions unchanged. Smoke: GraphQL schema import + `test_graphql_schema_introspection` **1 passed**; `pip-audit` **NO strawberry findings** (**light validated**). Explicitly **not** bumped: `ecdsa`, starlette/FastAPI/pydantic. **CI GREEN not met**.
- CI-16 ecdsa disposition (DEC-057): **Option A Accepted** ? residual risk on `ecdsa` (PYSEC-2026-1325 / CVE-2024-23342 Minerva; upstream no planned fix). Evidence: JWT paths **RS256** / **HS256** only; no `ES*` / no direct `ecdsa` import; transitive via `python-jose` only. Option B (PyJWT migration) deferred ? not clearly small/safe for unattended residual close. Starlette ? **CI-22**. **CI-16 CLOSED** for slice scope. No code / no `pip-audit` ignore in the DEC land. **CI GREEN not met**.
- CI Stage 5 pip-audit policy (DEC-090): Follow-on to DEC-057 Option A. Evidence run `30681284601` @ `f2c7587` + tip `e993e83` host ? sole residual `ecdsa 0.19.2` **PYSEC-2026-1325** (NO starlette). Lands **named** `--ignore-vuln PYSEC-2026-1325` on CI `security-pip-audit`; `--strict` retained (no silent weaken). Stage 5 pip-audit expected **green**; **whole-pipeline CI GREEN not met**.
- CI Stage 5 Secrets Scan / Trivy policy (DEC-098): Follow-on to DEC-090 for same residual. Evidence run `30684813480` @ `c8c1bce` ? sole HIGH `ecdsa` **CVE-2024-23342** (frontend lock 0). Lands **named** `CVE-2024-23342` in `.trivyignore` + `trivyignores` on CI Secrets Scan; `exit-code: 1` / CRITICAL,HIGH retained. Stage 5 Secrets Scan expected **green**; **whole-pipeline CI GREEN not met**. DEC-085 preserved.
- CI-22 Phase 1 follow-up observer (`e993e83` over `442af64`, 2026-08-01): CI run **30681693636** FAIL (not whole-pipeline GREEN). **Backend Types** FAIL (~27) ? sole clear CI-22 persist vs pre-CI-22 `cf94057`/`30680804636`: `excel_import/router.py` `Request = Request` (later fixed `4f035dd`). Remaining Types = R-22/CI-20 (+ post-442 admin `list` UP006). **Backend Lint** FAIL (3x `entity_resolution` E501/B010) ? **CI-20** residual (`c223857`), not CI-22. **pip-audit** FAIL sole **ecdsa** PYSEC-2026-1325 ? **starlette cleared** (same as `442af64`/`30681046523`; pre had 10 vulns/2 pkgs). DEC-090 ignore landed later `7aa65b3` (not on `e993e83`). **Docker Smoke** **30681693640 SUCCESS** (clears `442af64` smoke **30681046515** backend-unhealthy). Secrets Scan still ecdsa via Trivy. **No whole-pipeline GREEN claim**.
- CI-22 Phase 1 (DEC-081 @ `442af64`): **COMPLETE** ? fastapi **0.141.1** / starlette **1.3.1** / pydantic **2.13.4**; Request C3 fixes; host smoke + middleware. **CLOSED DEC-109**. **CI GREEN not met**.
- CI-22 executive close (DEC-109): **CLOSED** ? starlette floor met; field `30688863161` pip-audit SUCCESS (1 ignored=ecdsa); Unit RBAC override field-green. Companion [`decisions/DEC-109-CI-22-EXECUTIVE-CLOSE.md`](decisions/DEC-109-CI-22-EXECUTIVE-CLOSE.md). **whole-pipeline CI GREEN not met** (CI-08).
- CI-14 plan (DEC-062): **PLAN COMPLETE** ? frontend major inventory + safe vs STOP; companion [`DEC-062-CI-14-FRONTEND-DEPS-PLAN.md`](decisions/DEC-062-CI-14-FRONTEND-DEPS-PLAN.md).
- CI-14 Slice 1 (DEC-063): **COMPLETE** ? `overrides.sharp >=0.35.0`; lock **sharp 0.34.5 ? 0.35.3** under next **15.5.22** (react/eslint/jest unchanged). Evidence: `npm ls sharp` + `npx tsc --noEmit` exit 0. Validation **light validated**. Story later **CLOSED** DEC-108. **CI GREEN not met**.
- CI-14 Slice 2 STOP (DEC-065): ESLint **9 ? 10** initially STOPPED (silent major) ? **superseded** by DEC-072 EXECUTE PASS @ `240f9a8` (eslint **10.8.0** + Stage 1 lint green).
- CI-14 Slice 2 PASS (DEC-072 @ `240f9a8`): eslint **10.8.0** + eslint-config-next **15.5.22**; Docker Stage 1 lint+prettier exit 0. Validation **build validated**.
- CI-14 Slice 3 STOP (DEC-100): Jest **29 ? 30** STOPPED ? silent major; no jest **29.x** patch beyond **29.7.0**; host `npm audit` **0** (no security driver). **No package/lock land.** Companion [`DEC-100-CI-14-SLICE-3-JEST-STOP.md`](decisions/DEC-100-CI-14-SLICE-3-JEST-STOP.md). R-18 **Closed**. Next: dedicated Jest 30 evidence (Stage 3 **0** fail) **or** executive CI-14 AC close. Validation **light validated**. **CI GREEN not met**.
- CI-14 executive AC close (DEC-108): **CLOSED** - revised AC: sharp + eslint 10 + audit **0** = security modernization complete; Jest 30 optional backlog (not close gate). Companion [`DEC-108-CI-14-EXECUTIVE-AC-CLOSE.md`](decisions/DEC-108-CI-14-EXECUTIVE-AC-CLOSE.md). No package/lock land. Validation **docs only / light validated**. **whole-pipeline CI GREEN not met** (CI-08).
- S04-01 status detail (CLOSED): RC1 (`:id::uuid` bind bug) and RC2 (cross-loop pool reuse) fixed and proven; 7th test was blocked on R-19 (analytics ORM?DB drift), resolved by CI-15 migration `07e3ec4084fc`; suite now 7/7 PASS ? see DEC-020/DEC-021/DEC-022.
- CI-15 Phase 1 (approved ACs, DEC-021): analytics-only scope; systemic drift OUT of scope, registered as R-20 with Program Story DB-05. Principle: **Local Story fixes Local Drift. Systemic Drift becomes a Program Initiative.** Phase 2 (DEC-022): pushed `4793b08`, CI run `30652813475` ? job matrix identical to baseline, no functional/test/RLS/integration regression; 10 new Ruff style violations from the migration file disclosed and transferred to the lint backlog (not fixed within CI-15).
- CI-02 Phase 1 (DEC-023): toolchain remediation only ? `pipx install poetry` added to `security-pip-audit`; security-scan `-f` parse error fixed; YAML valid; local proof: `poetry export` exit 0 (98 deps) ? `pip-audit` runs ? real findings (R-21). Principle ratified: **CI Stories fix CI Infrastructure, not application dependencies.**
- CI-10 (DEC-036): whole-tree Ruff remediation on `3801151` ? 3,611 violations ? 0 (ruff 0.4.10, `app/alembic/versions/` excluded per triage #2 AC); field-verify CI run `30670339985` ? **Backend Lint SUCCESS**; sibling jobs on same run still fail (MyPy, pip-audit, npm audit, Jest, Trivy fs) ? **CI GREEN not met**.
