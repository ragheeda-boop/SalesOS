# Agent-closable P1 closures — 2026-08-13

**Classification:** **production no-go** (unchanged)  
**Scope:** U02 workspace navigation, U04 Company 360 documents/tasks, S10 HSTS preload, 360 contacts tenant predicate  
**Validation:** Jest **8 passed** (U02+U04); Docker pytest **5 passed** (`TestSecurityHeaders` + company isolation including 360 contacts); focused ESLint **exit 0** on touched FE files. Host Poetry pytest **not used** (fastapi missing).  
**AI flag:** `feature_ai_copilot` remains **false**  
**A-09 / Vercel / OAuth / PITR:** not in this slice (human / parallel)

## Closed this session

| ID | Finding | Change | Validation |
|----|---------|--------|------------|
| **REMAINING_GAPS U02** | WorkspaceSwitcher only set React state | `workspaceSelectHref` + layout `router.push` to workspace home | Jest **4 passed** (`workspaces.test.ts`) |
| **REMAINING_GAPS U04** | Documents / next-steps used sample data | Wire `company360.documents` / `tasks`; drop sample toggle | Jest **4 passed** (`company360Lists.test.ts`) |
| **REMAINING_GAPS S10** | HSTS missing `preload` | Backend middleware + Next `headers()` | Pytest `TestSecurityHeaders` (Docker; see commands) |
| **C07** | Next.js `images.domains` deprecated | `images.remotePatterns` for localhost | config-only |
| **360 contacts tenant** | 360 listed contacts by `company_id` only | Add `tenant_id` predicate | `test_company_360_contacts_exclude_foreign_tenant_rows` |

## Explicit non-claims

- Production / GA **GO** — **NO**
- A-09 staging parity / OAuth / PITR — **not closed**
- DUP-01 engines deleted — **NO**
- MetaData islands Fixed — **NO** (ceiling 13)
- Live Railway multi-tenant — **NO**
- Full npm lint-build / full pytest — **not claimed**

## Residual next (agent-closable)

1. MetaData live-table consolidations (DEC required; freeze ceiling 13)
2. DUP-01 engine deletion still deferred
3. Company 360 settings panel still EmptyState
4. Human: A-09 soak unlock, SSO revoke, OPS-01 PITR

**Authority:** executable evidence + `docs/audit/ga-engineering-audit/`
