# Agent-closable P1 closures — 2026-08-12

**Classification:** **production no-go** (unchanged)  
**Scope:** ga-engineering-audit / PRODUCTION_PLAN residuals that are **not** ResearchAgent live LLM, `feature_ai_copilot=True`, prod `alembic upgrade head`, or human OAuth/token revoke  
**Validation:** fitness FF subset **PASS**; Docker pytest decision_center + forecast_demo_gate **54 passed** (narrow); FE changes **not** browser-validated

## Closed / narrowed this session

| ID | Finding | Change | Evidence |
|----|---------|--------|----------|
| **EAB-001-P1-DRIFT-01** | Orphan `MetaData()` islands | `pgvector_migration._embedding_table` → `table()`/`column()`; FF-09 ceiling **18→17** | `salesos/backend/runtime/knowledge_graph_runtime/pgvector_migration.py`; `METADATA-ISLAND-FREEZE.md`; fitness scripts |
| **U03 / REMAINING_GAPS** | Company 360 quick actions href-only | `?action=add-contact` opens contact modal; `?action=new-deal` opens create-deal modal; activity actions deep-link to `/activities?company_id=…` | `salesos/frontend/src/app/(dashboard)/companies/[id]/page.tsx` |
| **CTO window P1** | Honesty banners on end-user chrome | `/decisions` multi-engine honesty banner visible only for `admin`/`owner` | `salesos/frontend/src/app/(dashboard)/decisions/page.tsx` |

## Explicit non-claims

- Production / GA **GO** — **NO**
- Staging parity (A-09) — still **OPEN**
- Human secret rotation (`SSO_GOOGLE_CLIENT_SECRET`, `RAILWAY_API_TOKEN`, `GH_TOKEN`) — still **OPEN**
- MetaData islands **Fixed** — **NO** (Partial; ceiling 17 remains)
- Company 360 empty panels / full activity composers — **not** closed (handlers wired only)
- Full `npm` lint/build / full pytest suite — **not run**

## Residual top blockers (still open)

1. **A-09 staging parity** + soak claim false  
2. **Human secret rotation** — see [`HUMAN-SECRET-ROTATION-CHECKLIST.md`](./HUMAN-SECRET-ROTATION-CHECKLIST.md)  
3. **OPS-01 / DR gate ink** + managed schedule auth (human)  
4. **EAB DUP-01** multi-engine decision surfaces (HTTP remount held; engines retained)  
5. **Cross-tenant isolation live proof** / role topology (human/ops)

**Authority:** executable evidence + [`docs/audit/ga-engineering-audit/`](../audit/ga-engineering-audit/)
