# Agent-closable P1 / DUP closures — 2026-08-12 (evening advance)

**Classification:** **production no-go** (unchanged)  
**Scope:** DUP-01 advance (engines kept) + MetaData ceiling drop + automated cross-tenant proofs  
**Validation:** fitness FF subset **PASS** (host); Docker pytest Platform isolation **3 passed** + CompanyService harness **1 passed** (narrow)  
**AI flag:** `feature_ai_copilot` remains **false**

## Closed / narrowed this session

| ID | Finding | Change | Evidence |
|----|---------|--------|----------|
| **EAB-001-P0-DUP-01** | Multi-engine + FE hybrid residual | FE `/decisions` Center feedback only (fitness-guarded); Platform `explain(decision_id, tenant_id)` + feedback ownership check; engines **retained** | `engine.py`, `router.py`, `fitness-ci-subset.*`, `DECISION-API-SOT.md`, `test_decision_platform_tenant_isolation.py` |
| **EAB-001-P1-DRIFT-01** | MetaData islands | Ceiling **17→13** — admin COUNT stubs + benchmark seed/count → `table()`/`column()` | `runtime/admin_router.py`, `benchmark/*`, `METADATA-ISLAND-FREEZE.md` |
| **Cross-tenant (automated)** | App-layer isolation proof | CompanyService harness + Platform engine explain/history/feedback isolation | `test_company_service_tenant_isolation.py`, `test_decision_platform_tenant_isolation.py` |

## Explicit non-claims

- Production / GA **GO** — **NO**
- DUP-01 **Fixed** (engines deleted) — **NO** (Partial narrowed)
- Live Railway **multi-tenant** isolation proof — **NO** (prod topology still single shared tenant; human/ops)
- MetaData islands **Fixed** — **NO** (Partial; ceiling 13)
- Full pytest / npm lint-build — **not claimed**

## Residual top blockers (still open)

1. **A-09 staging parity** + human Railway token / ENV mislabel  
2. **Human secret rotation** — SSO browser + provider revoke unverified  
3. **OPS-01 / DR gate** — human CLOSE ink + managed PITR  
4. **EAB DUP-01** — ≥3 BE engines + FE STUB / Platform alternate queries remain  
5. **Live cross-tenant** — needs second tenant topology (human)

**Authority:** executable evidence + [`docs/audit/ga-engineering-audit/`](../audit/ga-engineering-audit/)
