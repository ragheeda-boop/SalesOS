# Journeys

## J1 — Login to first value
Invite/Login → MFA → Org select → Shell → Sales/Exec home → open Company via CmdK.

## J2 — Work a deal
Sales home → Pipeline widget drilldown → Deal 360 → log activity → NBA Preview action.

## J3 — Company investigate
Search company → 360 Overview → Graph → AI Summary Preview → create task.

## J4 — CS renewal
CS home health alert → Account health → Success Plan → approval notification.

## J5 — Admin invite
Admin → Users → Invite → role assign → audit event.
