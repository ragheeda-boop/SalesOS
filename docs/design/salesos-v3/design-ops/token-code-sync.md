# Token ↔ Code Sync

1. Source of truth: design tokens package (code) after bootstrap; Figma variables mirror.
2. CI check: no raw hex in `src/app/(v3)`.
3. Publish `@salesos/design-system` versions with changelog.
