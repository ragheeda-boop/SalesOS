# A-09 steps 1–2 retry — 2026-08-13

**Validation:** **light validated** (GH Actions failed-log + staging HTTP probe)  
**Claims:** `staging_parity_complete=false` · `soak_complete_claim=false` · `production_go=false`  
**Constraints:** No `feature_ai_copilot` flip · No prod alembic · No secret dumps · No invented `RAILWAY_TOKEN`

---

## Verdict

| Step | Result |
|------|:------:|
| 1. Verify/rotate `RAILWAY_TOKEN` | **FAIL** |
| 2. `deploy-staging.yml` SUCCESS | **FAIL** |

**Run:** https://github.com/ragheeda-boop/SalesOS/actions/runs/31647956116  
**Ref:** `staging` · `confirm_staging=CONFIRM-STAGING-DEPLOY` · `skip_frontend_cli=true`

### Failure evidence

- Staging Availability Gate: **SUCCESS** (secrets present)
- Deploy Backend → `railway up --environment staging`: **FAIL**

```text
Unauthorized. Please check that your RAILWAY_TOKEN is valid and has access
to the resource you're trying to use.
```

Agent cannot invent or rotate the token. Human rotate required.

### Informational (not post-deploy SUCCESS)

- `GET https://salesos-staging.up.railway.app/health` → **200**
- `version=5.1.0-rc1`; database/cache/graph/redis connected
- Host is the **prior** staging deploy — this CI run did not update it
- Worker/beat/decision smoke not re-run as step-2 SUCCESS path (blocked)

---

## Human rotate (exact)

1. Railway → Account Settings → Tokens → create/regenerate **Project Token** for `responsible-comfort` (`96032c9a-38cf-4792-8168-b78d5353e26b`) with access to env **staging** (`5ce7864a-27c5-43c7-847d-667aecfbf773`).
2. GitHub → `ragheeda-boop/SalesOS` → Settings → Environments → **staging** → update secret `RAILWAY_TOKEN`.
3. Also update **repository** secret `RAILWAY_TOKEN` if the workflow still resolves the repo-level value.
4. Confirm Environment `staging` still has: `RAILWAY_TOKEN`, `RAILWAY_PROJECT_ID`, `RAILWAY_STAGING_SERVICE_ID`, `RAILWAY_STAGING_ENVIRONMENT_ID`.
5. Actions → **Deploy Staging** → Run workflow on ref `staging` with `confirm_staging=CONFIRM-STAGING-DEPLOY`.
6. Expect: Gate SUCCESS → Deploy Backend SUCCESS → Staging Health Gate HTTP 200.

Do **not** paste token values into chat, commits, or evidence docs.

---

*A-09 remains OPEN. Steps 1–2 Human-Gate.*
