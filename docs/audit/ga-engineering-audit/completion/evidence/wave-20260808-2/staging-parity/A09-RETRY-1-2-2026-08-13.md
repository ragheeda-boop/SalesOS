# A-09 steps 1–2 retry — 2026-08-13

**Validation:** **light validated** (GH Actions failed-log + staging HTTP probe)  
**Claims:** `staging_parity_complete=false` · `soak_complete_claim=false` · `production_go=false`  
**Constraints:** No `feature_ai_copilot` flip · No prod alembic · No secret dumps · No invented `RAILWAY_TOKEN`

---

## Verdict (post-rotate re-dispatch)

| Step | Result |
|------|:------:|
| 1. Verify/rotate `RAILWAY_TOKEN` | **FAIL** — still Unauthorized after human «تم التدوير» |
| 2. `deploy-staging.yml` SUCCESS | **FAIL** |

**Run:** https://github.com/ragheeda-boop/SalesOS/actions/runs/31648777919  
**Ref:** `staging` · `confirm_staging=CONFIRM-STAGING-DEPLOY` · `skip_frontend_cli=true` · head `df5028cc649a22ff7616938c2a8dab55d1f4e563`

### Failure evidence (31648777919)

| Job | Outcome |
|-----|---------|
| Staging Availability Gate | **SUCCESS** (secrets present) |
| Deploy Backend → `railway up --environment staging` | **FAIL** |
| Frontend (Vercel) | skipped |
| Staging Health Gate | skipped (needs backend) |
| Deploy Notification | SUCCESS |

```text
Unauthorized. Please check that your RAILWAY_TOKEN is valid and has access
to the resource you're trying to use.
```

Same failure class as prior retries. Agent cannot invent or paste a token.

### Prior retries (same class)

| Run | Outcome |
|-----|---------|
| [31647956116](https://github.com/ragheeda-boop/SalesOS/actions/runs/31647956116) | Gate PASS · Unauthorized |
| [31638994692](https://github.com/ragheeda-boop/SalesOS/actions/runs/31638994692) | Gate PASS · Unauthorized |
| [31648777919](https://github.com/ragheeda-boop/SalesOS/actions/runs/31648777919) | Gate PASS · Unauthorized (**post «تم التدوير»**) |

### Informational (not post-deploy SUCCESS)

- `GET https://salesos-staging.up.railway.app/health` → **200**
- `version=5.1.0-rc1`; database/cache/graph/redis connected; `status=ok`
- Host is **prior** staging deploy — this CI run did **not** update it
- Worker/beat/decision/login smoke **not** re-run as step-2 SUCCESS path (blocked)

---

## Human residual (exact — do not paste token values)

Rotation was reported done, but GH Actions still rejects `RAILWAY_TOKEN` on `railway up`. Re-check:

1. Railway → Tokens → create **Project Token** for `responsible-comfort` (`96032c9a-38cf-4792-8168-b78d5353e26b`) with env **staging** (`5ce7864a-27c5-43c7-847d-667aecfbf773`). Account tokens without project/env scope often fail here.
2. GitHub → Environments → **staging** → Environment secret `RAILWAY_TOKEN` = that new value (overwrite, full paste, no truncation/whitespace).
3. Also check **repository** secret `RAILWAY_TOKEN` if it was updated instead of (or conflicts with) the Environment secret — jobs use `environment: staging`, so Environment secret must be correct.
4. Confirm Environment still has: `RAILWAY_TOKEN`, `RAILWAY_PROJECT_ID`, `RAILWAY_STAGING_SERVICE_ID`, `RAILWAY_STAGING_ENVIRONMENT_ID`.
5. Re-dispatch **Deploy Staging** on `staging` with `CONFIRM-STAGING-DEPLOY`.
6. Expect: Gate SUCCESS → Deploy Backend SUCCESS → Health Gate HTTP 200.

Do **not** paste token values into chat, commits, or evidence docs.

---

*A-09 remains OPEN. Steps 1–2 Human-Gate. Post-rotate proof: FAIL.*
