# A-09 checklist steps 1–5 — 2026-08-12 (late)

**Validation:** **light validated** (GH Actions + live staging HTTP + Railway CLI logs)  
**Claims:** `staging_parity_complete=false` · `soak_complete_claim=false` · `production_go=false`  
**Constraints:** No `feature_ai_copilot` flip · No prod alembic head · No secret dumps · No invented `RAILWAY_TOKEN`

**Host:** `https://salesos-staging.up.railway.app`  
**CLI staging env:** `5ce7864a-27c5-43c7-847d-667aecfbf773` (user-supplied `1ef5b31a…` / `29252eae…` still not in workspace)

---

## Step results

| Step | Result | Evidence |
|------|:------:|----------|
| 1. Rotate / verify `RAILWAY_TOKEN` | **FAIL** | Deploy attempt Unauthorized — token not rotated by agent |
| 2. `deploy-staging.yml` SUCCESS | **FAIL** | Blocked by step 1 Unauthorized |
| 3. Staging login (seeded muhide) | **PASS** | `POST /api/v1/identity/login` → **200**; tenant prefix `fb496e60…`; access token present |
| 4. Decision smoke | **PASS** | `POST /api/v1/decision-runtime/decision/evaluate` → **200**, `decision_type=recommend_call`, id prefix `b79c0645…` |
| 5. Worker + beat + `agent_dispatch_all` | **PASS** (light) | Worker ready; beat schedules every 1m; worker received+succeeded dispatch |

---

## Step 1 — FAIL (Unauthorized) — STOP rotate (human)

Attempted deploy to prove whether GH Environment `staging` `RAILWAY_TOKEN` works:

| Run | Outcome |
|-----|---------|
| [31630383949](https://github.com/ragheeda-boop/SalesOS/actions/runs/31630383949) (prior) | Gate PASS · `railway up` **Unauthorized** |
| [31638897907](https://github.com/ragheeda-boop/SalesOS/actions/runs/31638897907) | Gate PASS · CLI install **socket hang up** (transient; did not reach token) |
| [31638994692](https://github.com/ragheeda-boop/SalesOS/actions/runs/31638994692) | Gate PASS · CLI install OK · `railway up` **Unauthorized** |

Exact error:

```text
Unauthorized. Please check that your RAILWAY_TOKEN is valid and has access
to the resource you're trying to use.
```

Agent **cannot invent** a token. Step 1 stops here — **not SUCCESS**.

### Human rotate instructions (exact)

1. Railway dashboard → Account Settings → Tokens → create a new **Project Token** for project `responsible-comfort` (`96032c9a-38cf-4792-8168-b78d5353e26b`) with access to environment **staging** (`5ce7864a-27c5-43c7-847d-667aecfbf773`). Or regenerate the existing token if one is already bound.
2. GitHub → `ragheeda-boop/SalesOS` → Settings → Environments → **staging** → Environment secrets → update `RAILWAY_TOKEN` to the new value.
3. Also update **repository** secret `RAILWAY_TOKEN` if Actions still resolve the repo-level value for this workflow (Environment `staging` should override when the job has `environment: staging` — verify both if unsure).
4. Confirm Environment `staging` still has: `RAILWAY_TOKEN`, `RAILWAY_PROJECT_ID`, `RAILWAY_STAGING_SERVICE_ID`, `RAILWAY_STAGING_ENVIRONMENT_ID` (= CLI id `5ce7864a-…`).
5. Actions → **Deploy Staging** → Run workflow on ref `staging` with `confirm_staging=CONFIRM-STAGING-DEPLOY`.
6. Expect: Staging Availability Gate SUCCESS → Deploy Backend SUCCESS → Staging Health Gate HTTP 200.

Do **not** paste token values into chat, commits, or evidence docs.

---

## Step 2 — FAIL

`deploy-staging.yml` did not reach SUCCESS. Fail reason: **`RAILWAY_TOKEN` Unauthorized** on `railway up --environment staging` after gate cleared. Re-run after human rotate (step 1).

---

## Step 3 — PASS (login)

- Email used: `ragheed.a@muhide.com` (seed script target; password **not** recorded here)
- `POST https://salesos-staging.up.railway.app/api/v1/identity/login` → **200**
- Tenant id prefix: `fb496e60`
- Companies list: **5** (seeded Decision path)

---

## Step 4 — PASS (Decision path)

| Probe | HTTP | Notes |
|-------|------|-------|
| `GET /api/v1/identity/csrf-token` | 200 | Cookie jar + `X-CSRF-Token` used |
| `GET /api/v1/companies?limit=5` | 200 | company prefix `ee12787b` |
| `POST /api/v1/decision/evaluate` | 422 | Schema requires `tenant_id`/`actor_id` — not used as smoke SoT |
| `POST /api/v1/decision-runtime/decision/evaluate` | **200** | `recommend_call` — Decision path proven |
| `GET /api/v1/decisions?limit=3` | 200 | list OK (count 0 — Center ledger empty; Runtime evaluate still proves path) |

`FEATURE_AI_COPILOT=false` / `ENV=staging` confirmed via Railway variable list (names+values for non-secret flags only).

---

## Step 5 — PASS (light) — worker / beat / dispatch

| Check | Evidence |
|-------|----------|
| Staging `/health` | **200** — database/cache/redis/graph connected |
| celery-worker | Online · deploy `f423f787…` · log `celery@… ready` · registered `agent_dispatch_all` |
| celery-beat | Online · deploy `bb5876c1…` · `beat: Starting…` · `Scheduler: Sending due task agent-dispatch-every-1m (agent_dispatch_all)` |
| `agent_dispatch_all` | Worker received task `5af88cc2…` and **succeeded** (`tenants_processed=0`, `tasks_claimed=0`, `errors=[]`) |

Honest note: dispatch success with zero tenants claimed is still a healthy scheduler→worker loop. Prior Neo4j `:6432` reachability residual on some dispatch paths remains separate from beat/worker online.

---

## Commands run (redacted)

```text
gh workflow run deploy-staging.yml --ref staging -f confirm_staging=CONFIRM-STAGING-DEPLOY -f skip_frontend_cli=true
gh run watch <id>
Invoke-WebRequest https://salesos-staging.up.railway.app/health
railway status --environment staging
railway logs --environment staging --service "celery-worker (Copy 3091)" --lines 80
railway logs --environment staging --service "celery-beat (Copy 5338)" --lines 40
# login + CSRF + decision-runtime evaluate (password via env only; cleared after)
```

---

*A-09 remains OPEN. Steps 1–2 Human-Gate. Steps 3–5 agent-closed light validated.*
