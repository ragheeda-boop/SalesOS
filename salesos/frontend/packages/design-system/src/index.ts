/**
 * @salesos/design-system — SalesOS Design System v3
 *
 * This package re-exports tokens and provides component primitives.
 * Tokens are the single source of truth — always import from @salesos/tokens.
 */

export * from "@salesos/tokens";
