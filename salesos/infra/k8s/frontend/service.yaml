apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: salesos
  labels:
    app: frontend
    app.kubernetes.io/name: frontend
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: salesos
spec:
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: frontend
  type: ClusterIP
