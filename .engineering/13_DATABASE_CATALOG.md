---
EngineeringOS: v3
GeneratedAt: 2026-08-03T22:30:00Z
RepositoryCommit: bee3276
RepositoryBranch: master
Generator: Validation/Evidence reconcile (versions-dir parse)
Status: Corrected (Alembic citation refresh — audit NO-GO agree)
EvidenceLevel: Measured (file-graph parse; not live Docker alembic current)
Revalidation: Active
---

# 13 — DATABASE CATALOG

> Schema posture, migration state, RLS, and derived artifacts. **Observe-only: do not invent migrations.** Corrected per ARB audit 32 (B1/B5): the v3.0 claimed head `e4b9c32d0c04` did not exist at pin `3749c30`. **2026-08-03 citation refresh:** Phase 0 / DEC-142 pin `a4f7c29e1b80` / **69** is **STALE as current head** (mid-chain; depth 69). Current tip measure @ `bee3276`: single head **`e5f9a32b0c08`** / **82** migration files. **Production GA = NO-GO.**

## 1. Overview

- **Engine:** PostgreSQL 16 (host Poetry/asyncpg is broken on Windows per audit → Docker is the approved path).
- **ORM:** SQLAlchemy (async) via `app/database.py`.
- **Migration tool:** Alembic (`alembic.ini`, `app/alembic/`).
- **State (tip `bee3276`, versions-dir parse):** **82** migration files, **82** revision IDs parsed, **single linear head `e5f9a32b0c08`** (`e5f9a32b0c08_story_08_06_conflict_resolution_policies`; down `c4d8e21a9f07`).
- **Method:** all `salesos/backend/app/alembic/versions/*.py` parsed for `revision`/`down_revision`; heads = revisions never referenced as a down_revision → `[e5f9a32b0c08]`. (Live Docker `alembic heads` / `alembic current` not re-run this citation pass.)
- **Head history:** prior pin `c89025a` → `c9f4a21b6e08`; DEC-142 / Phase 0 fingerprint pin `9fa8e9f` → **`a4f7c29e1b80` / 69** (historical mid-chain; **do not cite as current**). Tip advanced through Phase 1 Owner Platform + story migrations to **`e5f9a32b0c08` / 82**.

## 2. Migration lineages at HEAD

| Lineage | Content | State |
|---|---|---|
| Numeric chain `0001..0052` | baseline + domain + runtime + RAG + commercial + workflow + notification + webhook + revenue + decision-center + calendar/oauth/google/telemetry | in chain (linear) |
| `0012_refresh_token_tables` | refresh token families (B5-era identity tokens) | in chain |
| RLS Category A | 47 Category-A tenant tables (DEC-044), policies generated by `scripts/generate_rls_policies.py` | in chain |
| RLS B1..B7 (DEC-114..119) | join RLS: company children (b1), commercial children (b2), analytics children (b3), decision-center children (b4), identity token children (b5), webhook_deliveries (b6), admin_role_permissions (b7) | in chain |
| DB-05 slices | `a4f7c29e1b80` slice 5d (indexes/types/nullable; DEC-130d); slices 4–5g | **mid-chain** (Phase 0 pin; **STALE as current**) |
| Phase 1 story migrations | Owner Platform + later stories through STORY-08-06 conflict policies | **head = `e5f9a32b0c08`** |

## 3. Security posture

- **RLS:** 47 Category-A tables (comment in `scripts/generate_rls_policies.py`); B1–B7 + deferred-8 join RLS landed. Exact runtime policy count at tip-align: **67** per DEC-123a (`POLICY_COUNT 67`); **not re-probed in this pass**.
- **Row-level isolation:** tenant_id scoping enforced at policy level.
- **Derived artifacts (untracked, must not rely on in CI):** `_reports/`, `RLS_POLICIES.md`, `_rls_*.sql` (verification pending).

## 4. Key tables (non-exhaustive)

| Table group | Purpose | RLS |
|---|---|---|
| `users`, `tenants`, `refresh_token_families`, `device_sessions`, `token_blacklist`, `password_reset_tokens` | identity | yes |
| `companies`, `contacts`, `branches`, `licenses` | company/contact | yes |
| `opportunities`, `tasks` | commercial | yes |
| `pipeline_*`, `forecast_*`, `analytics_*` | pipeline/forecast/analytics | yes |
| `timeline_entries` | timeline | yes |
| `feature_store_*` | feature store | yes |
| `workflow_definitions`, `workflow_executions`, `scheduled_jobs`, `job_executions`, `webhook_*` | workflows | yes |
| `decision_center_*`, `decisions`, `scoring_scorecards` | decision/scoring | yes |
| `golden_records`, `entity_resolution_conflicts`, `entity_resolution_log`, `dead_letter_queue` | entity resolution | yes |
| `nba/score_*` | revenue brain | yes |
| `meetings`, `emails`, `calendar_events` | calendar/email/meeting intelligence (DEC-121 ORM↔Alembic UUID aligned) | yes |

## 5. Non-relational / external stores

| Store | Use |
|---|---|
| Redis | cache / rate limiting |
| Meilisearch | fulltext search |
| Neo4j | knowledge graph (configured) |
| Kafka | optional event bus (degraded default in_memory) |
| Celery broker | background jobs (9 beat jobs) |

## 6. Split-brain note

Compose stacks default `EVENT_BUS_TYPE=in_memory`; K8s configmap expects kafka → event/bus behavior differs per environment (observed; `02`, `08`).

## 7. Staleness protocol (B5 fix)

`13` is **regenerate-on-commit**: whenever `git rev-parse --short=7 HEAD` differs from `RepositoryCommit`, re-measure `alembic_heads` + migration count before any DB work. Agents must run `alembic current` (Docker) on the live DB before trusting schema claims; do **not** assert a revision ID absent from `app/alembic/versions`. Do **not** cite `a4f7c29e1b80` / 69 as current after tip advanced past that mid-chain pin.

## 8. When this file changes

- On migration add, RLS policy change, or schema change. Re-verify with `alembic current` (Docker) and re-run `generate_rls_policies.py` when relevant. Always update `alembic_heads` in `23`.
