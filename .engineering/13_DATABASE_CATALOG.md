---
EngineeringOS: v3
GeneratedAt: 2026-08-01T20:10:52Z
RepositoryCommit: 9fa8e9f
RepositoryBranch: master
Generator: OpenCode
Status: Corrected (EOS v3.1 cycle)
EvidenceLevel: Measured
Revalidation: Active (DEC-142)
---

# 13 â€” DATABASE CATALOG

> Schema posture, migration state, RLS, and derived artifacts. **Observe-only: do not fix migrations in this bootstrap.** Corrected per ARB audit 32 (B1/B5): the v3.0 claimed head `e4b9c32d0c04` did not exist at pin `3749c30` and is not the head now.

## 1. Overview

- **Engine:** PostgreSQL 16 (host Poetry/asyncpg is broken on Windows per audit â†’ Docker is the approved path).
- **ORM:** SQLAlchemy (async) via `app/database.py`.
- **Migration tool:** Alembic (`alembic.ini`, `app/alembic/`).
- **State (HEAD `9fa8e9f`):** **69** migration files, **69** revision IDs parsed, **single linear head `a4f7c29e1b80`** (`a4f7c29e1b80_db05_slice5d_indexes_types_nullable`).
- **Method:** all 69 files parsed for `revision`/`down_revision`; heads = revisions never referenced as a down_revision; corroborated by Docker `alembic heads` → `a4f7c29e1b80 (head)` (DEC-142).
- **Head history:** prior pin `c89025a` head was `c9f4a21b6e08` (DB-05 slice 3). Tip advanced through DB-05 slices 4–5g (DEC-123…DEC-130g) to `a4f7c29e1b80`.

## 2. Migration lineages at HEAD

| Lineage | Content | State |
|---|---|---|
| Numeric chain `0001..0052` | baseline + domain + runtime + RAG + commercial + workflow + notification + webhook + revenue + decision-center + calendar/oauth/google/telemetry | in chain (linear) |
| `0012_refresh_token_tables` | refresh token families (B5-era identity tokens) | in chain |
| RLS Category A | 47 Category-A tenant tables (DEC-044), policies generated by `scripts/generate_rls_policies.py` | in chain |
| RLS B1..B7 (DEC-114..119) | join RLS: company children (b1), commercial children (b2), analytics children (b3), decision-center children (b4), identity token children (b5), webhook_deliveries (b6), admin_role_permissions (b7) | **landed in commits after prior pin** |
| DB-05 slices | `a4f7c29e1b80` slice 5d = current head (indexes/types/nullable; DEC-130d); slices 4–5g in chain | head |

## 3. Security posture

- **RLS:** 47 Category-A tables (comment in `scripts/generate_rls_policies.py`); B1â€“B7 + deferred-8 join RLS landed. Exact runtime policy count at tip-align: **67** per DEC-123a (`POLICY_COUNT 67`); **not re-probed in this pass**.
- **Row-level isolation:** tenant_id scoping enforced at policy level.
- **Derived artifacts (untracked, must not rely on in CI):** `_reports/`, `RLS_POLICIES.md`, `_rls_*.sql` (verification pending).

## 4. Key tables (non-exhaustive)

| Table group | Purpose | RLS |
|---|---|---|
| `users`, `tenants`, `refresh_token_families`, `device_sessions`, `token_blacklist`, `password_reset_tokens` | identity | yes |
| `companies`, `contacts`, `branches`, `licenses` | company/contact | yes |
| `opportunities`, `tasks` | commercial | yes |
| `pipeline_*`, `forecast_*`, `analytics_*` | pipeline/forecast/analytics | yes |
| `timeline_entries` | timeline | yes |
| `feature_store_*` | feature store | yes |
| `workflow_definitions`, `workflow_executions`, `scheduled_jobs`, `job_executions`, `webhook_*` | workflows | yes |
| `decision_center_*`, `decisions`, `scoring_scorecards` | decision/scoring | yes |
| `golden_records`, `entity_resolution_conflicts`, `entity_resolution_log`, `dead_letter_queue` | entity resolution | yes |
| `nba/score_*` | revenue brain | yes |
| `meetings`, `emails`, `calendar_events` | calendar/email/meeting intelligence (DEC-121 ORMâ†”Alembic UUID aligned) | yes |

## 5. Non-relational / external stores

| Store | Use |
|---|---|
| Redis | cache / rate limiting |
| Meilisearch | fulltext search |
| Neo4j | knowledge graph (configured) |
| Kafka | optional event bus (degraded default in_memory) |
| Celery broker | background jobs (9 beat jobs) |

## 6. Split-brain note

Compose stacks default `EVENT_BUS_TYPE=in_memory`; K8s configmap expects kafka â†’ event/bus behavior differs per environment (observed; `02`, `08`).

## 7. Staleness protocol (B5 fix)

`13` is **regenerate-on-commit**: whenever `git rev-parse --short=7 HEAD` differs from `RepositoryCommit`, re-measure `alembic_heads` + migration count before any DB work. Agents must run `alembic current` (Docker) on the live DB before trusting schema claims; do **not** assert a revision ID absent from `app/alembic/versions`.

## 8. When this file changes

- On migration add, RLS policy change, or schema change. Re-verify with `alembic current` (Docker) and re-run `generate_rls_policies.py` when relevant. Always update `alembic_heads` in `23`.
